# netbox-extractor — Audit de code

Scope : sources écrites à la main (`src/` 58 fichiers / 3569 lignes, `templates/` 7, config d'exemple, CLI, CI, Dockerfile, README) · Couverture : **70/70 fichiers production lus + 11 specs (méta)**, `vendor/` (2369 fichiers générés OpenAPI) exclu.
Date : 2026-07-16 · Ré-audit (le code porte les marqueurs C*/K*/S*/P* d'un audit antérieur, `AUDIT.md` supprimé).

Sémantique vérifiée empiriquement ce tour-ci (probes compilées, Crystal 1.20.3) :
- `Process.run(cmd, shell: true, args: [...])` passe `args` comme argv réel (`;`, `$HOME` sortis littéralement) → **pas d'injection shell** par les args ; lève `ArgumentError` seulement si `command` contient déjà des arguments.
- `Array(YAML::Any) & Array(String)` **matche** (`YAML::Any#hash`/`#==` délèguent à la valeur brute) → le filtre d'exclusion par tag fonctionne.
- `receiver.try &.a.b || fallback` : le `|| fallback` est **hors** du bloc `&.` → `netbox_primary_ip`/`netbox_os_name` retombent bien sur leur défaut quand le receveur est nil.

**Aucun finding CRITICAL.** Le socle défensif est solide : `each_isolated` ré-émet une `PartialFailure` (exit ≠ 0) au lieu d'avaler les échecs de fibre ; pagination correcte (`offset += page.results.size`, `break` sur page vide) ; build-then-swap Icinga ; chargements Netbox ré-émettant l'erreur avant toute régénération destructive ; noms d'hôtes filtrés (`SAFE_NAME`) avant toute construction de chemin.

---

## Résumé prioritaire (plus sévère d'abord)

1. **MEDIUM SEC-crinja_filters.cr:16** — `icinga_string` n'échappe pas `$` (macro Icinga2) → zone cassée / valeur substituée.
2. **MEDIUM COR-generic-host.j2:51** — credentials rabbit/mysql/postgres rendus en bareword non quoté → un mot de passe normal casse le chargement de la zone.
3. **MEDIUM COR-netbox_client.cr:70** — une VM sans plateforme disparaît silencieusement de toutes les sorties.
4. **MEDIUM COR-with_custom_config.cr:35** — config de check custom appariée par nom exact, sans diagnostic → monitoring silencieusement perdu.
5. **MEDIUM ERR-icinga-extractor.cr:42** — `Dotenv.load` plante si `.env` absent, même quand l'ENV est déjà présent (conteneur/CI).
6. **MEDIUM COR-cli.cr:72** — sous-commande inconnue/mal tapée → `puts help`, exit 0, aucun travail (no-op silencieux en automatisation).
7. **MEDIUM COR-site_device_role.cr:11** — champ `filename` des rôles Icinga mort/ignoré (l'exemple le renseigne).
8. **MEDIUM ERR-icinga_helper.cr:194** — `.as_h`/`.as_a` non gardés → un fichier de facts JSON valide mais mal formé plante la fibre de l'hôte.
9. **MEDIUM SEC-test_api/get.cr:184** — `tokens.list` dumpé en YAML au niveau debug → tokens Netbox écrits dans les logs.
10. **MEDIUM TEST** — filtres d'échappement (sécurité), détection OS et construction de nom de fichier : zéro spec ; CI sans `crystal tool format --check`.

---

## 1. Correctness

```
MEDIUM | COR-netbox_client.cr:70 | Une VM sans plateforme (platform: null, fréquent) → netbox_os_name="unknown" → netbox_is_os?("linux"/"windows")=false → jamais sélectionnée par fetch_vms | VM rattachée à un vm_role configuré mais sans plateforme → absente de tout inventaire Ansible ET config Icinga, sans avertissement | Classer explicitement les plateformes inconnues (bucket de repli + Log.warn) au lieu de les exclure en silence.
MEDIUM | COR-with_custom_config.cr:35 | find_custom_config_for apparie par `c.host == host.name` exact, sans normalisation ni log de non-appariement | checks_config keyé en FQDN `web1.example.com` mais nom Netbox `web1` → l'hôte est rendu avec les checks par défaut, credentials/seuils perdus, aucun warning | Normaliser les deux côtés (ou apparier FQDN/court) et logger toute entrée checks_config n'appariant aucun hôte.
MEDIUM | COR-cli.cr:72 | Les `run` parents font `puts help` ; admiral ne rejette qu'`Admiral::Error`, pas une action inconnue (aussi 113,147,192,205) | `netbox-extractor ansible genrate` (faute de frappe) affiche l'aide, exit 0, ne génère rien → un no-op total passe pour un succès en CI/cron | Garde explicite « sous-commande inconnue » (exit 1) dans chaque `run` parent.
MEDIUM | COR-config/icinga/site_device_role.cr:11 | `filename` des rôles Icinga (et site_vm_role.cr:12) n'est jamais lu — le générateur Icinga dispatche sur role.name/role.os (generators/icinga.cr:63,70) et nomme via netbox_icinga_filename | netbox-extractor.yml.example:75/81/85 posent `filename: "physical-linux"` attendant un renommage ; la valeur est silencieusement ignorée | Supprimer le champ mort, ou le propager dans icinga_dump_devices/vms comme le fait Ansible.
LOW | COR-config/base.cr:22 | `sites_config` résolu via File.expand_path contre le CWD du process, pas le dossier du config principal | binaire lancé depuis un autre dossier avec `sites_config: [sites/dc2.yml]` → ValidationError "not found" alors que le fichier est à côté du yml | Étendre les chemins relatifs contre File.dirname(config_path).
LOW | COR-icinga_helper.cr:163 | ansible_os_distro fait `.as_s` non gardé (incohérent avec ansible_os_name qui utilise `.as_s?`) | fait ansible_distribution non-string → TypeCastError non rescué dans la fibre de l'hôte → config du site non swappée | Utiliser `.as_s?`.
LOW | COR-netbox_client.cr:101 | netbox_powered_on? mappe status null → false (nil == "active") | objet avec status: null → exclu de l'inventaire par le filtre powered-on | Décider/documenter le défaut ; traiter nil comme actif si c'est l'intention.
LOW | COR-netbox_client.cr:27 | tags null (champ Array(NestedTag)?) → netbox_tags renvoie nil → slug de rôle jamais ajouté et check_by_snmp?/ping? valent nil | objet avec tags: null (vs []) → tag de rôle absent, détection SNMP/ping perdue | Amorcer `tags.try(&.map(&.slug)) || [] of String`.
NIT | COR-netbox_client.cr:59 | Détection OS par sous-chaîne `includes?`, linux testé avant windows | slug `windows-subsystem-linux` → classé linux | Apparier un jeu de slugs connus / un préfixe plutôt qu'une sous-chaîne.
NIT | COR-generic-host.j2:32 | `{%- if true -%}` garde morte toujours vraie | bloc storage émis même quand partitions_list est vide → `["storage"]["partitions"] = []` | Retirer la garde constante ou garder sur partitions_list.
```

## 2. Concurrency

Solide dans l'ensemble — analysé à travers la frontière de fibre : `icinga_staging_path` posé une seule fois sur l'objet `@site` (distinct par site) avant le fan-out, lu seulement ensuite ; `Log.context.set` rappelé dans chaque fibre (fiber-local) ; cache de templates gardé par `Mutex` ; client Netbox et env CRINJA construits avidement au load (pas de `||=` racé). Chemins de staging (`.<id>.staging`) et fichiers de sortie disjoints par site/rôle (garantis par `validate!`). `render` est CPU-bound sans point de yield → sûr sous l'ordonnanceur mono-thread par défaut.

```
LOW | CON-generators/icinga.cr:84 | swap_into_place fait `rm_rf final_path` puis `File.rename` — non atomique | crash entre les deux → le site se retrouve sans aucune config Icinga (fenêtre vide) | Renommer l'ancien vers un `.old` puis rename staging par-dessus, ou rename atomique dans le même parent puis nettoyer.
LOW | CON-Dockerfile:9 | `crystal=~1.18` flotte le patch Alpine alors que mise.toml épingle 1.18.2 | build CI/release compilé avec un patch 1.18.x différent du dev | Épingler `crystal=~1.18.2` ou documenter le flottement.
```

## 3. Error model

```
MEDIUM | ERR-src/netbox-extractor.cr:42 | Dotenv.load lève File::NotFoundError si le fichier manque (lib/dotenv/src/dotenv.cr:110), et env_path défaut = ".env" | conteneur/CI avec NETBOX_TOKEN_API déjà exporté mais sans .env sur disque → plante avant tout travail malgré tous les secrets présents | Utiliser `Dotenv.load?` (no-op si absent) pour honorer l'ENV du process.
MEDIUM | ERR-icinga_helper.cr:194 | `JSON.parse(...).as_h` ne rescue que JSON::ParseException|IO::Error, pas TypeCastError (idem `.as_a` ligne 175) | fichier de facts au JSON valide mais top-level non-objet (`[]`, `"x"`) → TypeCastError non rescué → fibre de l'hôte plante, swap du site avorté | `.as_h?`/`.as_a?` renvoyant nil, ou ajouter TypeCastError au rescue.
LOW | ERR-src/netbox-extractor.cr:98 | Rescue amical (STDERR + inspect_with_backtrace + exit 1) gardé par `Crystal.env.production?`, mais l'env défaut est "development" et rien ne pose CRYSTAL_ENV dans Dockerfile/Makefile.release | binaire livré sur erreur fatale → branche `else` non wrappée (dump runtime brut) ; exit reste 1 mais le handler propre est du code mort | Poser CRYSTAL_ENV=production au build release, ou toujours wrapper CLI.run.
LOW | ERR-src/netbox-extractor.cr:48 | `rescue ex : Crinja::Error` ré-émet un String nu (`raise "..."`), perd cause + backtrace (idem logger.cr:53) | échec de rendu de config → seul le message résumé remonte, ligne/contexte Crinja perdus | `raise RuntimeError.new("...", cause: ex)`.
```

## 4. Performance / mémoire / CPU

```
LOW | PERF-netbox/ipam_inventory.cr:34 | Charge TOUTES les IP IPAM sans filtre serveur en mémoire (générateur Bind = stub) | Netbox à grande table d'IP → chargement intégral en RAM ; inoffensif aujourd'hui car Bind ne fait que dumper sur STDOUT | Scoper/paginer paresseusement si Bind est un jour implémenté.
NIT | PERF-with_custom_config.cr:35 | find_custom_config_for refait un scan linéaire de checks_config à chaque appel (type de check, période, chaque check_*? , locals) par hôte | O(hôtes × checks_config × services) | Mémoïser la config custom résolue par hôte.
```

## 5. API design

```
MEDIUM | API-config/icinga/site_device_role.cr:11 | (voir §1) champ `filename` mort côté Icinga → API de config trompeuse : la clé est acceptée mais sans effet.
NIT | API-netbox_client.cr:9 | netbox_role renvoie String non-nil pour les devices (rôle non-nullable) mais String? pour les VMs ; le doc-comment « ou nil si aucun rôle » ne vaut que pour les VMs | lecteur ajoute une gestion nil morte côté device | Documenter l'asymétrie par type.
```

## 6. Security

```
MEDIUM | SEC-crinja_filters.cr:16 | icinga_string échappe `\ " \n \r \t` mais PAS `$`, que Icinga2 substitue en macro dans les chaînes double-quotées ; atteignable via vendor_name/vendor_model (texte libre Netbox), os_name/os_distro (facts LSB) — tous passés par icinga_string (generic-host.j2:17,29,30) | un fabricant Netbox `Dell $Node$` → `vendor_name = "...$Node$..."` substitué en macro ou échec de `icinga2 daemon -C` → zone entière rejetée | Ajouter `.gsub('$', "$$")` à la chaîne (idem icinga_array, JSON n'échappe pas `$` non plus).
MEDIUM | SEC-templates/icinga/generic-host.j2:51 | Credentials rabbit (51,52), mysql (65,66), postgres (77,78) interpolés bruts, NON quotés et NON échappés (les champs sont de simples String) | `username: admin` → `["rabbit"]["username"] = admin` (bareword → identifiant indéfini, erreur de parse) ; un mot de passe avec `"`/espace injecte ou casse la zone | Wrapper `"{{ ... | icinga_string }}"` ; idem esx (hypervisor-esx.j2:14-15) et netapp (storage-netapp.j2:25-26).
MEDIUM | SEC-controllers/test_api/get.cr:184 | test_users_api appelle `tokens.list` et log_result (l.34) dumpe le YAML complet du résultat au niveau debug | `test_api get` avec logger.level: debug → tokens d'API Netbox écrits en clair dans le fichier de log | Ne jamais dumper users/permissions/tokens ; masquer ou retirer ces endpoints du smoke-test, ou logguer seulement le count.
LOW | SEC-templates/ansible/ansible.cfg:27 | `ssh_args = {{ ssh_args }}` (et caching_prefix/connection) interpolés bruts dans l'INI | une valeur contenant un retour ligne scinde la ligne INI et injecte une directive ; source opérateur de confiance → risque faible | Documenter que ces champs sont de confiance, ou valider l'absence de newline.
```

## 7. Test coverage

```
MEDIUM | TEST-spec/ | Zéro spec pour le code critique-sécurité et à forte logique : crinja_filters (échappement Icinga), patches netbox_* (détection OS, netbox_icinga_filename, powered_on, tags), presenters, facts_fetcher, controllers | une régression d'échappement ou de nommage de fichier passe le CI vert | Ajouter des specs sur crinja_filters (cas `"`, `\`, `$`, newline), netbox_icinga_filename et la détection OS.
MEDIUM | TEST-.github/workflows/ci.yml:23 | CI lance ameba (38) et la suite spec, mais AUCUN `crystal tool format --check` ; `dev:format` existe mais n'est jamais imposé | un commit non formaté fusionne vert ; la dérive s'accumule | Ajouter une étape `crystal tool format --check src spec`.
```

Specs présents et non vacants : concurrency, config/base, config/site_fetch_facts, config/validation, generators/icinga, logger, netbox/pagination, templates, utils. CI échoue bien sur spec/ameba (pas de `|| true`/continue-on-error).

## 8. Documentation

```
LOW | DOC-README.md:3 | Badges/liens pointent `jbox-web/netbox-extractor` alors que shard.yml et Contributors référencent Nicolas Rodriguez / `n-rodriguez` | l'utilisateur suivant le badge/clone atterrit sur un autre slug, ou badge mort | Réconcilier le slug du dépôt (badges, liens, instruction de fork l.86).
LOW | DOC-netbox-extractor.yml.example:128 | check_netapp.aggregates.warning/critical typés String défaut "80%"/"85%" (netapp.cr:16) mais l'exemple fournit des entiers nus 87/90 | YAML donne "87" (pas de crash) mais le `%` du défaut est perdu → forme divergente de ce que le template attend | Quoter `"87%"`/`"90%"`.
NIT | DOC-netbox_client.cr:9 | (voir §5) doc-comment netbox_role trompeur pour les devices.
```

---

## 9. Ordre de remédiation (tous les findings)

Phase A — sécurité & pertes silencieuses (faire d'abord) :
1. SEC-crinja_filters.cr:16 — échapper `$` (filtre `icinga_string` + `icinga_array`). *Racine ; couvre l'échappement de toutes les valeurs Netbox.*
2. SEC-generic-host.j2:51 — quoter+échapper les credentials rabbit/mysql/postgres (et esx/netapp). *Même fichier de templates que #1 partiellement → grouper.*
3. SEC-test_api/get.cr:184 — cesser de dumper tokens/users/permissions.
4. COR-netbox_client.cr:70 — plateforme inconnue : bucket de repli + warning (pas d'exclusion silencieuse).
5. COR-with_custom_config.cr:35 — normaliser l'appariement + logger les entrées orphelines.
6. COR-icinga/site_device_role.cr:11 (+site_vm_role.cr:12) — supprimer ou câbler `filename`.
7. ERR-icinga_helper.cr:194 & :175 — `.as_h?`/`.as_a?` (même fichier → une passe).

Phase B — robustesse d'exécution :
8. ERR-netbox-extractor.cr:42 — `Dotenv.load?`.
9. COR-cli.cr:72 — garde sous-commande inconnue (exit 1) dans les 5 `run` parents.
10. ERR-netbox-extractor.cr:98 + :48 — poser CRYSTAL_ENV=production (release) et ré-émettre avec `cause:` (même fichier ; #10 englobe le dead-code du rescue).
11. ERR-logger.cr:53 — ré-émettre avec `cause:`.
12. COR-icinga_helper.cr:163 — `.as_s?` pour os_distro (groupé avec #7, même fichier).

Phase C — tests & CI (verrouille A/B contre la régression) :
13. TEST-spec/ — specs crinja_filters / netbox_icinga_filename / détection OS (valide #1, #2, #4).
14. TEST-ci.yml:23 — étape `crystal tool format --check`.

Phase D — LOW/NIT (corrections d'appoint) :
15. CON-generators/icinga.cr:84 — swap atomique.
16. COR-base.cr:22 — chemins sites_config relatifs au config.
17. COR-netbox_client.cr:101 & :27 — status/tags null (même fichier → grouper avec #4).
18. PERF-ipam_inventory.cr:34 — scoper si Bind implémenté.
19. PERF-with_custom_config.cr:35 — mémoïser (même fichier que #5).
20. NIT-netbox_client.cr:59 — détection OS par slug.
21. NIT-generic-host.j2:32 — retirer `{% if true %}`.
22. DOC-README.md:3, DOC-yml.example:128, DOC/NIT-netbox_client.cr:9, CON-Dockerfile:9.

Dépendances : #13 dépend de #1/#2/#4 (teste leur comportement corrigé) ; #17 et #19 se groupent avec #4 et #5 (mêmes fichiers) ; #2 partage le fichier de templates avec #1.

---

## Hypothèses vérifiées et écartées (avec preuve)

- **Injection shell via `ansible-playbook`** (facts_fetcher/ansible.cr:149) — écartée. Probe : `Process.run("echo", shell: true, args: ["c;d","$HOME"])` sort `c;d $HOME` littéralement ; les args sont de l'argv réel, pas du texte shell. `shell: true` est un surcoût inutile, pas une faille.
- **Path traversal via nom d'hôte** (icinga_host.cr:29 / netbox_device.cr:37 / netbox_vm.cr:30) — écartée. `InventoryFilters.valid_object_name?` rejette tout nom hors `/\A[A-Za-z0-9._-]+\z/` (donc tout `/`) AVANT les présenteurs (filter_objects dans fetch_devices/fetch_vms). Sans `/`, `File.join` ne peut pas s'échapper ; `..` seul devient `...conf`.
- **Nom d'hôte null → `role/.conf` en collision** (netbox_device.cr:37) — écartée. Les noms nil/vides sont supprimés par valid_object_name? avant toute construction de chemin.
- **VM rôle null → chemin `-linux/`** (netbox_vm.cr:30) — écartée. `netbox_has_role?(role)` = `netbox_role == role` ; une VM à rôle nil n'apparie aucun vm_role configuré → jamais sélectionnée → n'atteint jamais netbox_icinga_filename.
- **Exclusion par tag inopérante** (facts_fetcher:128, `Array(YAML::Any) & Array(String)`) — écartée. Probe : l'intersection renvoie l'élément (`["exclude-me"]`), `YAML::Any#hash`/`#==` délèguent à la valeur brute.
- **`netbox_primary_ip`/`netbox_os_name` ne retombent pas sur leur défaut** (précédence `try &. ... || fallback`) — écartée. Probe : receveur nil → renvoie le fallback (`|| ` hors du bloc `&.`).
- **`PartialFailure` avalée → exit 0 sur échec partiel** — écartée. `each_isolated` (concurrency.cr:50) ré-émet après le fan-out ; le runner admiral ne rescue qu'`Admiral::Error` → propagation jusqu'à un exit ≠ 0 dans les deux modes d'env.
- **Pagination sautant des blocs / bouclant à l'infini** — écartée. `offset += page.results.size` (pas `+= limit`) et `break` sur page vide ; `concat` (pas `+=`) donc pas d'O(n²).
- **Régénération destructive sur résultat vide dû à un échec de chargement** — écartée. `define_netbox_load` ré-émet ApiError/IO/Socket/ParseException avant tout `File.delete`/`rm_rf` ; le swap Icinga n'a lieu qu'après un chargement réussi.
- **Templates re-parsés par hôte** — écartée. `Utils.render_template` cache le `Crinja::Template` compilé par source sous mutex ; templates servis depuis un BakedFileSystem en mémoire.
- **`exclude["exclude_volumes"]` KeyError / type-crash** — écartée. Clé toujours présente dans `Partitions#to_h` ; `Array(String) - Array(String)` type-check.
- **Config d'exemple non chargeable (propriété requise manquante)** — écartée. Toutes les propriétés requises sans défaut (dont l'objet imbriqué `Ansible::Site.fetch_facts`) sont fournies par netbox-extractor.yml.example.
- **Héritage `site.x || global.x` lossy pour Bool/Int** — écartée. Les bools héritables utilisent `Bool?` + check `nil?` explicite (facts_fetcher:61-64) ; les `Int32?` via `||` sont sûrs (Crystal traite `0` comme truthy, seul `nil` retombe).
