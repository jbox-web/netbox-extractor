# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class WebhookRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # Cette URL sera appelée à l'aide de la méthode HTTP définie lors de l'appel du webhook. Le traitement du modèle Jinja2 est pris en charge dans le même contexte que le corps de la requête.
    @[JSON::Field(key: "payload_url", type: String, nillable: false, emit_null: false)]
    property payload_url : String

    # Optional properties
    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # * `GET` - GET * `POST` - POST * `PUT` - PUT * `PATCH` - PATCH * `DELETE` - DELETE
    @[JSON::Field(key: "http_method", type: String?, nillable: true, emit_null: false)]
    property http_method : String?

    # La liste complète des types de contenu officiels est disponible <a href=\"https://www.iana.org/assignments/media-types/media-types.xhtml\">ici</a>.
    @[JSON::Field(key: "http_content_type", type: String?, nillable: true, emit_null: false)]
    property http_content_type : String?

    # En-têtes HTTP fournis par l'utilisateur à envoyer avec la demande en plus du type de contenu HTTP. Les en-têtes doivent être définis au format <code>Nom : Value</code>. Le traitement du modèle Jinja2 est pris en charge dans le même contexte que le corps de la requête (ci-dessous).
    @[JSON::Field(key: "additional_headers", type: String?, nillable: true, emit_null: false)]
    property additional_headers : String?

    # Modèle Jinja2 pour un corps de requête personnalisé. Si ce champ est vide, un objet JSON représentant la modification sera inclus. Les données contextuelles disponibles incluent : <code>event</code>, <code>model</code>, <code>timestamp</code>, <code>username</code>, <code>request_id</code>, et <code>data</code>.
    @[JSON::Field(key: "body_template", type: String?, nillable: true, emit_null: false)]
    property body_template : String?

    # Lorsqu'elle sera fournie, la demande comprendra un <code>X-Hook-Signature</code> en-tête contenant un condensé hexadécimal HMAC du corps de la charge utile en utilisant le secret comme clé. Le secret n'est pas transmis dans la demande.
    @[JSON::Field(key: "secret", type: String?, nillable: true, emit_null: false)]
    property secret : String?

    # Activez la vérification des certificats SSL. Désactivez avec précaution !
    @[JSON::Field(key: "ssl_verification", type: Bool?, nillable: true, emit_null: false)]
    property ssl_verification : Bool?

    # Le fichier de certificat CA spécifique à utiliser pour la vérification SSL. Laissez ce champ vide pour utiliser les paramètres par défaut du système.
    @[JSON::Field(key: "ca_file_path", type: String?, nillable: true, emit_null: false)]
    property ca_file_path : String?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForHttp_method < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "http_method"
        @allowable_values = ["GET", "POST", "PUT", "PATCH", "DELETE"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@name : String, @payload_url : String, @description : String? = nil, @http_method : String? = nil, @http_content_type : String? = nil, @additional_headers : String? = nil, @body_template : String? = nil, @secret : String? = nil, @ssl_verification : Bool? = nil, @ca_file_path : String? = nil, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)? = nil, @tags : Array(NestedTagRequest)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 150
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 150.")
      end

      if @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      if @payload_url.try &.to_s.try &.size.try &.> 500
        invalid_properties.push("invalid value for \"payload_url\", the character length must be smaller than or equal to 500.")
      end

      if @payload_url.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"payload_url\", the character length must be greater than or equal to 1.")
      end

      http_method_validator = EnumAttributeValidatorForHttp_method.new
      if !http_method_validator.valid?(@http_method)
        message = http_method_validator.message
        invalid_properties.push(message)
      end

      if !@http_content_type.nil? && @http_content_type.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"http_content_type\", the character length must be smaller than or equal to 100.")
      end

      if !@http_content_type.nil? && @http_content_type.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"http_content_type\", the character length must be greater than or equal to 1.")
      end

      if !@secret.nil? && @secret.try &.to_s.try &.size.try &.> 255
        invalid_properties.push("invalid value for \"secret\", the character length must be smaller than or equal to 255.")
      end

      if !@ca_file_path.nil? && @ca_file_path.try &.to_s.try &.size.try &.> 4096
        invalid_properties.push("invalid value for \"ca_file_path\", the character length must be smaller than or equal to 4096.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 150
      return false if @name.try &.to_s.try &.size.try &.< 1
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      return false if @payload_url.try &.to_s.try &.size.try &.> 500
      return false if @payload_url.try &.to_s.try &.size.try &.< 1
      http_method_validator = EnumAttributeValidatorForHttp_method.new
      return false unless http_method_validator.valid?(@http_method)
      return false if !@http_content_type.nil? && @http_content_type.try &.to_s.try &.size.try &.> 100
      return false if !@http_content_type.nil? && @http_content_type.try &.to_s.try &.size.try &.< 1
      return false if !@secret.nil? && @secret.try &.to_s.try &.size.try &.> 255
      return false if !@ca_file_path.nil? && @ca_file_path.try &.to_s.try &.size.try &.> 4096
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 150
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 150.")
      end

      if name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method with validation
    # @param [Object] payload_url Value to be assigned
    def payload_url=(payload_url)
      if payload_url.to_s.size > 500
        raise ArgumentError.new("invalid value for \"payload_url\", the character length must be smaller than or equal to 500.")
      end

      if payload_url.to_s.size < 1
        raise ArgumentError.new("invalid value for \"payload_url\", the character length must be greater than or equal to 1.")
      end

      @payload_url = payload_url
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] http_method Object to be assigned
    def http_method=(http_method)
      validator = EnumAttributeValidatorForHttp_method.new
      unless validator.valid?(http_method)
        raise ArgumentError.new(validator.message)
      end
      @http_method = http_method
    end

    # Custom attribute writer method with validation
    # @param [Object] http_content_type Value to be assigned
    def http_content_type=(http_content_type)
      if !http_content_type.nil? && http_content_type.to_s.size > 100
        raise ArgumentError.new("invalid value for \"http_content_type\", the character length must be smaller than or equal to 100.")
      end

      if !http_content_type.nil? && http_content_type.to_s.size < 1
        raise ArgumentError.new("invalid value for \"http_content_type\", the character length must be greater than or equal to 1.")
      end

      @http_content_type = http_content_type
    end

    # Custom attribute writer method with validation
    # @param [Object] secret Value to be assigned
    def secret=(secret)
      if !secret.nil? && secret.to_s.size > 255
        raise ArgumentError.new("invalid value for \"secret\", the character length must be smaller than or equal to 255.")
      end

      @secret = secret
    end

    # Custom attribute writer method with validation
    # @param [Object] ca_file_path Value to be assigned
    def ca_file_path=(ca_file_path)
      if !ca_file_path.nil? && ca_file_path.to_s.size > 4096
        raise ArgumentError.new("invalid value for \"ca_file_path\", the character length must be smaller than or equal to 4096.")
      end

      @ca_file_path = ca_file_path
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        name == other.name &&
        description == other.description &&
        payload_url == other.payload_url &&
        http_method == other.http_method &&
        http_content_type == other.http_content_type &&
        additional_headers == other.additional_headers &&
        body_template == other.body_template &&
        secret == other.secret &&
        ssl_verification == other.ssl_verification &&
        ca_file_path == other.ca_file_path &&
        custom_fields == other.custom_fields &&
        tags == other.tags
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [name, description, payload_url, http_method, http_content_type, additional_headers, body_template, secret, ssl_verification, ca_file_path, custom_fields, tags].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["name"] = _to_h(name)
      hash["description"] = _to_h(description)
      hash["payload_url"] = _to_h(payload_url)
      hash["http_method"] = _to_h(http_method)
      hash["http_content_type"] = _to_h(http_content_type)
      hash["additional_headers"] = _to_h(additional_headers)
      hash["body_template"] = _to_h(body_template)
      hash["secret"] = _to_h(secret)
      hash["ssl_verification"] = _to_h(ssl_verification)
      hash["ca_file_path"] = _to_h(ca_file_path)
      hash["custom_fields"] = _to_h(custom_fields)
      hash["tags"] = _to_h(tags)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
