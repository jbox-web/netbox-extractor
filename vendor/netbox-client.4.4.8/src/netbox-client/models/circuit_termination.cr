# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class CircuitTermination
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display_url", type: String, nillable: false, emit_null: false)]
    property display_url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    @[JSON::Field(key: "circuit", type: BriefCircuit, nillable: false, emit_null: false)]
    property circuit : BriefCircuit

    # * `A` - A * `Z` - Z
    @[JSON::Field(key: "term_side", type: String, nillable: false, emit_null: false)]
    property term_side : String

    @[JSON::Field(key: "termination", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)), nillable: false, emit_null: false)]
    property termination : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))

    @[JSON::Field(key: "cable", type: BriefCable, nillable: false, emit_null: false)]
    property cable : BriefCable

    @[JSON::Field(key: "cable_end", type: String, nillable: false, emit_null: false)]
    property cable_end : String

    @[JSON::Field(key: "link_peers", type: Array(Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))), nillable: false, emit_null: false)]
    property link_peers : Array(Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)))

    # Return the type of the peer link terminations, or None.
    @[JSON::Field(key: "link_peers_type", type: String, nillable: false, emit_null: false)]
    property link_peers_type : String

    @[JSON::Field(key: "created", type: Time, nillable: false, emit_null: false)]
    property created : Time

    @[JSON::Field(key: "last_updated", type: Time, nillable: false, emit_null: false)]
    property last_updated : Time

    @[JSON::Field(key: "_occupied", type: Bool, nillable: false, emit_null: false)]
    property _occupied : Bool

    # Optional properties
    @[JSON::Field(key: "termination_type", type: String?, nillable: true, emit_null: false)]
    property termination_type : String?

    @[JSON::Field(key: "termination_id", type: Int32?, nillable: true, emit_null: false)]
    property termination_id : Int32?

    # Vitesse du circuit physique
    @[JSON::Field(key: "port_speed", type: Int32?, nillable: true, emit_null: false)]
    property port_speed : Int32?

    # Vitesse ascendante, si elle est différente de la vitesse du port
    @[JSON::Field(key: "upstream_speed", type: Int32?, nillable: true, emit_null: false)]
    property upstream_speed : Int32?

    # ID de l'interconnexion locale
    @[JSON::Field(key: "xconnect_id", type: String?, nillable: true, emit_null: false)]
    property xconnect_id : String?

    # ID du panneau de raccordement et numéro (s) de port
    @[JSON::Field(key: "pp_info", type: String?, nillable: true, emit_null: false)]
    property pp_info : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # Traiter comme si un câble était connecté
    @[JSON::Field(key: "mark_connected", type: Bool?, nillable: true, emit_null: false)]
    property mark_connected : Bool?

    @[JSON::Field(key: "tags", type: Array(NestedTag)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTag)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForTerm_side < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "term_side"
        @allowable_values = ["A", "Z"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display_url : String, @display : String, @circuit : BriefCircuit, @term_side : String, @termination : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)), @cable : BriefCable, @cable_end : String, @link_peers : Array(Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))), @link_peers_type : String, @created : Time, @last_updated : Time, @_occupied : Bool, @termination_type : String? = nil, @termination_id : Int32? = nil, @port_speed : Int32? = nil, @upstream_speed : Int32? = nil, @xconnect_id : String? = nil, @pp_info : String? = nil, @description : String? = nil, @mark_connected : Bool? = nil, @tags : Array(NestedTag)? = nil, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      term_side_validator = EnumAttributeValidatorForTerm_side.new
      if !term_side_validator.valid?(@term_side)
        message = term_side_validator.message
        invalid_properties.push(message)
      end

      if !@port_speed.nil? && @port_speed.try &.> 2147483647
        invalid_properties.push("invalid value for \"port_speed\", must be smaller than or equal to 2147483647.")
      end

      if !@port_speed.nil? && @port_speed.try &.< 0
        invalid_properties.push("invalid value for \"port_speed\", must be greater than or equal to 0.")
      end

      if !@upstream_speed.nil? && @upstream_speed.try &.> 2147483647
        invalid_properties.push("invalid value for \"upstream_speed\", must be smaller than or equal to 2147483647.")
      end

      if !@upstream_speed.nil? && @upstream_speed.try &.< 0
        invalid_properties.push("invalid value for \"upstream_speed\", must be greater than or equal to 0.")
      end

      if !@xconnect_id.nil? && @xconnect_id.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"xconnect_id\", the character length must be smaller than or equal to 50.")
      end

      if !@pp_info.nil? && @pp_info.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"pp_info\", the character length must be smaller than or equal to 100.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      term_side_validator = EnumAttributeValidatorForTerm_side.new
      return false unless term_side_validator.valid?(@term_side)
      return false if !@port_speed.nil? && @port_speed.try &.> 2147483647
      return false if !@port_speed.nil? && @port_speed.try &.< 0
      return false if !@upstream_speed.nil? && @upstream_speed.try &.> 2147483647
      return false if !@upstream_speed.nil? && @upstream_speed.try &.< 0
      return false if !@xconnect_id.nil? && @xconnect_id.try &.to_s.try &.size.try &.> 50
      return false if !@pp_info.nil? && @pp_info.try &.to_s.try &.size.try &.> 100
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] term_side Object to be assigned
    def term_side=(term_side)
      validator = EnumAttributeValidatorForTerm_side.new
      unless validator.valid?(term_side)
        raise ArgumentError.new(validator.message)
      end
      @term_side = term_side
    end

    # Custom attribute writer method with validation
    # @param [Object] port_speed Value to be assigned
    def port_speed=(port_speed)
      if !port_speed.nil? && port_speed > 2147483647
        raise ArgumentError.new("invalid value for \"port_speed\", must be smaller than or equal to 2147483647.")
      end

      if !port_speed.nil? && port_speed < 0
        raise ArgumentError.new("invalid value for \"port_speed\", must be greater than or equal to 0.")
      end

      @port_speed = port_speed
    end

    # Custom attribute writer method with validation
    # @param [Object] upstream_speed Value to be assigned
    def upstream_speed=(upstream_speed)
      if !upstream_speed.nil? && upstream_speed > 2147483647
        raise ArgumentError.new("invalid value for \"upstream_speed\", must be smaller than or equal to 2147483647.")
      end

      if !upstream_speed.nil? && upstream_speed < 0
        raise ArgumentError.new("invalid value for \"upstream_speed\", must be greater than or equal to 0.")
      end

      @upstream_speed = upstream_speed
    end

    # Custom attribute writer method with validation
    # @param [Object] xconnect_id Value to be assigned
    def xconnect_id=(xconnect_id)
      if !xconnect_id.nil? && xconnect_id.to_s.size > 50
        raise ArgumentError.new("invalid value for \"xconnect_id\", the character length must be smaller than or equal to 50.")
      end

      @xconnect_id = xconnect_id
    end

    # Custom attribute writer method with validation
    # @param [Object] pp_info Value to be assigned
    def pp_info=(pp_info)
      if !pp_info.nil? && pp_info.to_s.size > 100
        raise ArgumentError.new("invalid value for \"pp_info\", the character length must be smaller than or equal to 100.")
      end

      @pp_info = pp_info
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display_url == other.display_url &&
        display == other.display &&
        circuit == other.circuit &&
        term_side == other.term_side &&
        termination_type == other.termination_type &&
        termination_id == other.termination_id &&
        termination == other.termination &&
        port_speed == other.port_speed &&
        upstream_speed == other.upstream_speed &&
        xconnect_id == other.xconnect_id &&
        pp_info == other.pp_info &&
        description == other.description &&
        mark_connected == other.mark_connected &&
        cable == other.cable &&
        cable_end == other.cable_end &&
        link_peers == other.link_peers &&
        link_peers_type == other.link_peers_type &&
        tags == other.tags &&
        custom_fields == other.custom_fields &&
        created == other.created &&
        last_updated == other.last_updated &&
        _occupied == other._occupied
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display_url, display, circuit, term_side, termination_type, termination_id, termination, port_speed, upstream_speed, xconnect_id, pp_info, description, mark_connected, cable, cable_end, link_peers, link_peers_type, tags, custom_fields, created, last_updated, _occupied].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["url"] = _to_h(url)
      hash["display_url"] = _to_h(display_url)
      hash["display"] = _to_h(display)
      hash["circuit"] = _to_h(circuit)
      hash["term_side"] = _to_h(term_side)
      hash["termination_type"] = _to_h(termination_type)
      hash["termination_id"] = _to_h(termination_id)
      hash["termination"] = _to_h(termination)
      hash["port_speed"] = _to_h(port_speed)
      hash["upstream_speed"] = _to_h(upstream_speed)
      hash["xconnect_id"] = _to_h(xconnect_id)
      hash["pp_info"] = _to_h(pp_info)
      hash["description"] = _to_h(description)
      hash["mark_connected"] = _to_h(mark_connected)
      hash["cable"] = _to_h(cable)
      hash["cable_end"] = _to_h(cable_end)
      hash["link_peers"] = _to_h(link_peers)
      hash["link_peers_type"] = _to_h(link_peers_type)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash["created"] = _to_h(created)
      hash["last_updated"] = _to_h(last_updated)
      hash["_occupied"] = _to_h(_occupied)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
