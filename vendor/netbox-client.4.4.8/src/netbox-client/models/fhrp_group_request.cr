# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class FHRPGroupRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # * `vrrp2` - VRRPv2 * `vrrp3` - VRRPv3 * `carp` - CARP * `clusterxl` - ClusterXL * `hsrp` - HSRP * `glbp` - GLBP * `other` - Other
    @[JSON::Field(key: "protocol", type: String, nillable: false, emit_null: false)]
    property protocol : String

    @[JSON::Field(key: "group_id", type: Int32, nillable: false, emit_null: false)]
    property group_id : Int32

    # Optional properties
    @[JSON::Field(key: "name", type: String?, nillable: true, emit_null: false)]
    property name : String?

    # * `plaintext` - Texte brut * `md5` - MD5
    @[JSON::Field(key: "auth_type", type: String?, nillable: true, emit_null: false)]
    property auth_type : String?

    @[JSON::Field(key: "auth_key", type: String?, nillable: true, emit_null: false)]
    property auth_key : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForProtocol < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "protocol"
        @allowable_values = ["vrrp2", "vrrp3", "carp", "clusterxl", "hsrp", "glbp", "other"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForAuth_type < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "auth_type"
        @allowable_values = ["plaintext", "md5", ""].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@protocol : String, @group_id : Int32, @name : String? = nil, @auth_type : String? = nil, @auth_key : String? = nil, @description : String? = nil, @comments : String? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@name.nil? && @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      protocol_validator = EnumAttributeValidatorForProtocol.new
      if !protocol_validator.valid?(@protocol)
        message = protocol_validator.message
        invalid_properties.push(message)
      end

      if @group_id.try &.> 32767
        invalid_properties.push("invalid value for \"group_id\", must be smaller than or equal to 32767.")
      end

      if @group_id.try &.< 0
        invalid_properties.push("invalid value for \"group_id\", must be greater than or equal to 0.")
      end

      auth_type_validator = EnumAttributeValidatorForAuth_type.new
      if !auth_type_validator.valid?(@auth_type)
        message = auth_type_validator.message
        invalid_properties.push(message)
      end

      if !@auth_key.nil? && @auth_key.try &.to_s.try &.size.try &.> 255
        invalid_properties.push("invalid value for \"auth_key\", the character length must be smaller than or equal to 255.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@name.nil? && @name.try &.to_s.try &.size.try &.> 100
      protocol_validator = EnumAttributeValidatorForProtocol.new
      return false unless protocol_validator.valid?(@protocol)
      return false if @group_id.try &.> 32767
      return false if @group_id.try &.< 0
      auth_type_validator = EnumAttributeValidatorForAuth_type.new
      return false unless auth_type_validator.valid?(@auth_type)
      return false if !@auth_key.nil? && @auth_key.try &.to_s.try &.size.try &.> 255
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if !name.nil? && name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      @name = name
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] protocol Object to be assigned
    def protocol=(protocol)
      validator = EnumAttributeValidatorForProtocol.new
      unless validator.valid?(protocol)
        raise ArgumentError.new(validator.message)
      end
      @protocol = protocol
    end

    # Custom attribute writer method with validation
    # @param [Object] group_id Value to be assigned
    def group_id=(group_id)
      if group_id > 32767
        raise ArgumentError.new("invalid value for \"group_id\", must be smaller than or equal to 32767.")
      end

      if group_id < 0
        raise ArgumentError.new("invalid value for \"group_id\", must be greater than or equal to 0.")
      end

      @group_id = group_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] auth_type Object to be assigned
    def auth_type=(auth_type)
      validator = EnumAttributeValidatorForAuth_type.new
      unless validator.valid?(auth_type)
        raise ArgumentError.new(validator.message)
      end
      @auth_type = auth_type
    end

    # Custom attribute writer method with validation
    # @param [Object] auth_key Value to be assigned
    def auth_key=(auth_key)
      if !auth_key.nil? && auth_key.to_s.size > 255
        raise ArgumentError.new("invalid value for \"auth_key\", the character length must be smaller than or equal to 255.")
      end

      @auth_key = auth_key
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        name == other.name &&
        protocol == other.protocol &&
        group_id == other.group_id &&
        auth_type == other.auth_type &&
        auth_key == other.auth_key &&
        description == other.description &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [name, protocol, group_id, auth_type, auth_key, description, comments, tags, custom_fields].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["name"] = _to_h(name)
      hash["protocol"] = _to_h(protocol)
      hash["group_id"] = _to_h(group_id)
      hash["auth_type"] = _to_h(auth_type)
      hash["auth_key"] = _to_h(auth_key)
      hash["description"] = _to_h(description)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
