# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class EventRule
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display_url", type: String, nillable: false, emit_null: false)]
    property display_url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    @[JSON::Field(key: "object_types", type: Array(String), nillable: false, emit_null: false)]
    property object_types : Array(String)

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # Les types d'événements qui déclencheront cette règle.
    @[JSON::Field(key: "event_types", type: Array(String), nillable: false, emit_null: false)]
    property event_types : Array(String)

    @[JSON::Field(key: "action_type", type: EventRuleActionType, nillable: false, emit_null: false)]
    property action_type : EventRuleActionType

    @[JSON::Field(key: "action_object_type", type: String, nillable: false, emit_null: false)]
    property action_object_type : String

    @[JSON::Field(key: "action_object", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time), nillable: false, emit_null: false)]
    property action_object : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)

    @[JSON::Field(key: "created", type: Time, nillable: false, emit_null: false)]
    property created : Time

    @[JSON::Field(key: "last_updated", type: Time, nillable: false, emit_null: false)]
    property last_updated : Time

    # Optional properties
    @[JSON::Field(key: "enabled", type: Bool?, nillable: true, emit_null: false)]
    property enabled : Bool?

    # Un ensemble de conditions qui déterminent si l'événement sera généré.
    @[JSON::Field(key: "conditions", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))?, nillable: true, emit_null: false)]
    property conditions : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))?

    @[JSON::Field(key: "action_object_id", type: Int64?, nillable: true, emit_null: false)]
    property action_object_id : Int64?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    @[JSON::Field(key: "tags", type: Array(NestedTag)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTag)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display_url : String, @display : String, @object_types : Array(String), @name : String, @event_types : Array(String), @action_type : EventRuleActionType, @action_object_type : String, @action_object : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time), @created : Time, @last_updated : Time, @enabled : Bool? = nil, @conditions : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))? = nil, @action_object_id : Int64? = nil, @description : String? = nil, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)? = nil, @tags : Array(NestedTag)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 150
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 150.")
      end

      if !@action_object_id.nil? && @action_object_id.try &.> 9223372036854775807
        invalid_properties.push("invalid value for \"action_object_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if !@action_object_id.nil? && @action_object_id.try &.< 0
        invalid_properties.push("invalid value for \"action_object_id\", must be greater than or equal to 0.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 150
      return false if !@action_object_id.nil? && @action_object_id.try &.> 9223372036854775807
      return false if !@action_object_id.nil? && @action_object_id.try &.< 0
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 150
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 150.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] action_object_id Value to be assigned
    def action_object_id=(action_object_id)
      if !action_object_id.nil? && action_object_id > 9223372036854775807
        raise ArgumentError.new("invalid value for \"action_object_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if !action_object_id.nil? && action_object_id < 0
        raise ArgumentError.new("invalid value for \"action_object_id\", must be greater than or equal to 0.")
      end

      @action_object_id = action_object_id
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display_url == other.display_url &&
        display == other.display &&
        object_types == other.object_types &&
        name == other.name &&
        enabled == other.enabled &&
        event_types == other.event_types &&
        conditions == other.conditions &&
        action_type == other.action_type &&
        action_object_type == other.action_object_type &&
        action_object_id == other.action_object_id &&
        action_object == other.action_object &&
        description == other.description &&
        custom_fields == other.custom_fields &&
        tags == other.tags &&
        created == other.created &&
        last_updated == other.last_updated
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display_url, display, object_types, name, enabled, event_types, conditions, action_type, action_object_type, action_object_id, action_object, description, custom_fields, tags, created, last_updated].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["url"] = _to_h(url)
      hash["display_url"] = _to_h(display_url)
      hash["display"] = _to_h(display)
      hash["object_types"] = _to_h(object_types)
      hash["name"] = _to_h(name)
      hash["enabled"] = _to_h(enabled)
      hash["event_types"] = _to_h(event_types)
      hash["conditions"] = _to_h(conditions)
      hash["action_type"] = _to_h(action_type)
      hash["action_object_type"] = _to_h(action_object_type)
      hash["action_object_id"] = _to_h(action_object_id)
      hash["action_object"] = _to_h(action_object)
      hash["description"] = _to_h(description)
      hash["custom_fields"] = _to_h(custom_fields)
      hash["tags"] = _to_h(tags)
      hash["created"] = _to_h(created)
      hash["last_updated"] = _to_h(last_updated)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
