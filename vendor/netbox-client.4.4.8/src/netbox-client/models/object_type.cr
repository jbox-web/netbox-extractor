# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  class ObjectType
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    @[JSON::Field(key: "app_label", type: String, nillable: false, emit_null: false)]
    property app_label : String

    @[JSON::Field(key: "app_name", type: String, nillable: false, emit_null: false)]
    property app_name : String

    @[JSON::Field(key: "model", type: String, nillable: false, emit_null: false)]
    property model : String

    @[JSON::Field(key: "model_name", type: String, nillable: false, emit_null: false)]
    property model_name : String

    @[JSON::Field(key: "model_name_plural", type: String, nillable: false, emit_null: false)]
    property model_name_plural : String

    @[JSON::Field(key: "public", type: Bool, nillable: false, emit_null: false)]
    property public : Bool

    @[JSON::Field(key: "features", type: Array(String), nillable: false, emit_null: false)]
    property features : Array(String)

    @[JSON::Field(key: "is_plugin_model", type: Bool, nillable: false, emit_null: false)]
    property is_plugin_model : Bool

    @[JSON::Field(key: "rest_api_endpoint", type: String, nillable: false, emit_null: false)]
    property rest_api_endpoint : String

    @[JSON::Field(key: "description", type: String, nillable: false, emit_null: false)]
    property description : String

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display : String, @app_label : String, @app_name : String, @model : String, @model_name : String, @model_name_plural : String, @public : Bool, @features : Array(String), @is_plugin_model : Bool, @rest_api_endpoint : String, @description : String)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @app_label.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"app_label\", the character length must be smaller than or equal to 100.")
      end

      if @model.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"model\", the character length must be smaller than or equal to 100.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @app_label.try &.to_s.try &.size.try &.> 100
      return false if @model.try &.to_s.try &.size.try &.> 100
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] app_label Value to be assigned
    def app_label=(app_label)
      if app_label.to_s.size > 100
        raise ArgumentError.new("invalid value for \"app_label\", the character length must be smaller than or equal to 100.")
      end

      @app_label = app_label
    end

    # Custom attribute writer method with validation
    # @param [Object] model Value to be assigned
    def model=(model)
      if model.to_s.size > 100
        raise ArgumentError.new("invalid value for \"model\", the character length must be smaller than or equal to 100.")
      end

      @model = model
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display == other.display &&
        app_label == other.app_label &&
        app_name == other.app_name &&
        model == other.model &&
        model_name == other.model_name &&
        model_name_plural == other.model_name_plural &&
        public == other.public &&
        features == other.features &&
        is_plugin_model == other.is_plugin_model &&
        rest_api_endpoint == other.rest_api_endpoint &&
        description == other.description
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display, app_label, app_name, model, model_name, model_name_plural, public, features, is_plugin_model, rest_api_endpoint, description].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["url"] = _to_h(url)
      hash["display"] = _to_h(display)
      hash["app_label"] = _to_h(app_label)
      hash["app_name"] = _to_h(app_name)
      hash["model"] = _to_h(model)
      hash["model_name"] = _to_h(model_name)
      hash["model_name_plural"] = _to_h(model_name_plural)
      hash["public"] = _to_h(public)
      hash["features"] = _to_h(features)
      hash["is_plugin_model"] = _to_h(is_plugin_model)
      hash["rest_api_endpoint"] = _to_h(rest_api_endpoint)
      hash["description"] = _to_h(description)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
