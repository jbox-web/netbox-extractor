# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Extends the built-in ModelSerializer to enforce calling full_clean() on a copy of the associated instance during validation. (DRF does not do this by default; see https://github.com/encode/django-rest-framework/issues/3144)
  class User
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display_url", type: String, nillable: false, emit_null: false)]
    property display_url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    # Requis. 150 caractères maximum. Uniquement des lettres, nombres et les caractères « @ », « . », « + », « - » et « _ ».
    @[JSON::Field(key: "username", type: String, nillable: false, emit_null: false)]
    property username : String

    # Optional properties
    @[JSON::Field(key: "first_name", type: String?, nillable: true, emit_null: false)]
    property first_name : String?

    @[JSON::Field(key: "last_name", type: String?, nillable: true, emit_null: false)]
    property last_name : String?

    @[JSON::Field(key: "email", type: String?, nillable: true, emit_null: false)]
    property email : String?

    # Précise si l’utilisateur peut se connecter à ce site d'administration.
    @[JSON::Field(key: "is_staff", type: Bool?, nillable: true, emit_null: false)]
    property is_staff : Bool?

    # Précise si l’utilisateur doit être considéré comme actif. Décochez ceci plutôt que de supprimer le compte.
    @[JSON::Field(key: "is_active", type: Bool?, nillable: true, emit_null: false)]
    property is_active : Bool?

    @[JSON::Field(key: "date_joined", type: Time?, nillable: true, emit_null: false)]
    property date_joined : Time?

    @[JSON::Field(key: "last_login", type: Time?, nillable: true, emit_null: false)]
    property last_login : Time?

    @[JSON::Field(key: "groups", type: Array(Group)?, nillable: true, emit_null: false)]
    property groups : Array(Group)?

    @[JSON::Field(key: "permissions", type: Array(ObjectPermission)?, nillable: true, emit_null: false)]
    property permissions : Array(ObjectPermission)?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display_url : String, @display : String, @username : String, @first_name : String? = nil, @last_name : String? = nil, @email : String? = nil, @is_staff : Bool? = nil, @is_active : Bool? = nil, @date_joined : Time? = nil, @last_login : Time? = nil, @groups : Array(Group)? = nil, @permissions : Array(ObjectPermission)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @username.try &.to_s.try &.size.try &.> 150
        invalid_properties.push("invalid value for \"username\", the character length must be smaller than or equal to 150.")
      end

      pattern = /^[\w.@+-]+$/
      if @username.try &.!~ pattern
        invalid_properties.push("invalid value for \"username\", must conform to the pattern #{pattern}.")
      end

      if !@first_name.nil? && @first_name.try &.to_s.try &.size.try &.> 150
        invalid_properties.push("invalid value for \"first_name\", the character length must be smaller than or equal to 150.")
      end

      if !@last_name.nil? && @last_name.try &.to_s.try &.size.try &.> 150
        invalid_properties.push("invalid value for \"last_name\", the character length must be smaller than or equal to 150.")
      end

      if !@email.nil? && @email.try &.to_s.try &.size.try &.> 254
        invalid_properties.push("invalid value for \"email\", the character length must be smaller than or equal to 254.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @username.try &.to_s.try &.size.try &.> 150
      return false if @username.try &.!~ /^[\w.@+-]+$/
      return false if !@first_name.nil? && @first_name.try &.to_s.try &.size.try &.> 150
      return false if !@last_name.nil? && @last_name.try &.to_s.try &.size.try &.> 150
      return false if !@email.nil? && @email.try &.to_s.try &.size.try &.> 254
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] username Value to be assigned
    def username=(username)
      if username.to_s.size > 150
        raise ArgumentError.new("invalid value for \"username\", the character length must be smaller than or equal to 150.")
      end

      pattern = /^[\w.@+-]+$/
      if username !~ pattern
        raise ArgumentError.new("invalid value for \"username\", must conform to the pattern #{pattern}.")
      end

      @username = username
    end

    # Custom attribute writer method with validation
    # @param [Object] first_name Value to be assigned
    def first_name=(first_name)
      if !first_name.nil? && first_name.to_s.size > 150
        raise ArgumentError.new("invalid value for \"first_name\", the character length must be smaller than or equal to 150.")
      end

      @first_name = first_name
    end

    # Custom attribute writer method with validation
    # @param [Object] last_name Value to be assigned
    def last_name=(last_name)
      if !last_name.nil? && last_name.to_s.size > 150
        raise ArgumentError.new("invalid value for \"last_name\", the character length must be smaller than or equal to 150.")
      end

      @last_name = last_name
    end

    # Custom attribute writer method with validation
    # @param [Object] email Value to be assigned
    def email=(email)
      if !email.nil? && email.to_s.size > 254
        raise ArgumentError.new("invalid value for \"email\", the character length must be smaller than or equal to 254.")
      end

      @email = email
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display_url == other.display_url &&
        display == other.display &&
        username == other.username &&
        first_name == other.first_name &&
        last_name == other.last_name &&
        email == other.email &&
        is_staff == other.is_staff &&
        is_active == other.is_active &&
        date_joined == other.date_joined &&
        last_login == other.last_login &&
        groups == other.groups &&
        permissions == other.permissions
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display_url, display, username, first_name, last_name, email, is_staff, is_active, date_joined, last_login, groups, permissions].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["url"] = _to_h(url)
      hash["display_url"] = _to_h(display_url)
      hash["display"] = _to_h(display)
      hash["username"] = _to_h(username)
      hash["first_name"] = _to_h(first_name)
      hash["last_name"] = _to_h(last_name)
      hash["email"] = _to_h(email)
      hash["is_staff"] = _to_h(is_staff)
      hash["is_active"] = _to_h(is_active)
      hash["date_joined"] = _to_h(date_joined)
      hash["last_login"] = _to_h(last_login)
      hash["groups"] = _to_h(groups)
      hash["permissions"] = _to_h(permissions)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
