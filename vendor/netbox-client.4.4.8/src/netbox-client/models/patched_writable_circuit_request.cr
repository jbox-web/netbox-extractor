# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class PatchedWritableCircuitRequest
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # ID de circuit unique
    @[JSON::Field(key: "cid", type: String?, nillable: true, emit_null: false)]
    property cid : String?

    @[JSON::Field(key: "provider", type: BriefCircuitRequestProvider?, nillable: true, emit_null: false)]
    property provider : BriefCircuitRequestProvider?

    @[JSON::Field(key: "provider_account", type: CircuitRequestProviderAccount?, nillable: true, emit_null: false)]
    property provider_account : CircuitRequestProviderAccount?

    @[JSON::Field(key: "type", type: CircuitRequestType?, nillable: true, emit_null: false)]
    property _type : CircuitRequestType?

    # * `planned` - Planifié * `provisioning` - Approvisionnement * `active` - Actif * `offline` - Hors ligne * `deprovisioning` - Déprovisionnement * `decommissioned` - Mis hors service
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    @[JSON::Field(key: "install_date", type: Time?, nillable: true, emit_null: false)]
    property install_date : Time?

    @[JSON::Field(key: "termination_date", type: Time?, nillable: true, emit_null: false)]
    property termination_date : Time?

    # Taux engagé
    @[JSON::Field(key: "commit_rate", type: Int32?, nillable: true, emit_null: false)]
    property commit_rate : Int32?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "distance", type: Float64?, nillable: true, emit_null: false)]
    property distance : Float64?

    # * `km` - Kilomètres * `m` - Mètres * `mi` - Miles * `ft` - Pieds
    @[JSON::Field(key: "distance_unit", type: String?, nillable: true, emit_null: false)]
    property distance_unit : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    @[JSON::Field(key: "assignments", type: Array(BriefCircuitGroupAssignmentSerializerRequest)?, nillable: true, emit_null: false)]
    property assignments : Array(BriefCircuitGroupAssignmentSerializerRequest)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["planned", "provisioning", "active", "offline", "deprovisioning", "decommissioned"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForDistance_unit < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "distance_unit"
        @allowable_values = ["km", "m", "mi", "ft", ""].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@cid : String? = nil, @provider : BriefCircuitRequestProvider? = nil, @provider_account : CircuitRequestProviderAccount? = nil, @_type : CircuitRequestType? = nil, @status : String? = nil, @tenant : ASNRangeRequestTenant? = nil, @install_date : Time? = nil, @termination_date : Time? = nil, @commit_rate : Int32? = nil, @description : String? = nil, @distance : Float64? = nil, @distance_unit : String? = nil, @comments : String? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)? = nil, @assignments : Array(BriefCircuitGroupAssignmentSerializerRequest)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@cid.nil? && @cid.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"cid\", the character length must be smaller than or equal to 100.")
      end

      if !@cid.nil? && @cid.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"cid\", the character length must be greater than or equal to 1.")
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      if !@commit_rate.nil? && @commit_rate.try &.> 2147483647
        invalid_properties.push("invalid value for \"commit_rate\", must be smaller than or equal to 2147483647.")
      end

      if !@commit_rate.nil? && @commit_rate.try &.< 0
        invalid_properties.push("invalid value for \"commit_rate\", must be greater than or equal to 0.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      if !@distance.nil? && @distance.try &.>= 1000000
        invalid_properties.push("invalid value for \"distance\", must be smaller than 1000000.")
      end

      if !@distance.nil? && @distance.try &.<= -1000000
        invalid_properties.push("invalid value for \"distance\", must be greater than -1000000.")
      end

      distance_unit_validator = EnumAttributeValidatorForDistance_unit.new
      if !distance_unit_validator.valid?(@distance_unit)
        message = distance_unit_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@cid.nil? && @cid.try &.to_s.try &.size.try &.> 100
      return false if !@cid.nil? && @cid.try &.to_s.try &.size.try &.< 1
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      return false if !@commit_rate.nil? && @commit_rate.try &.> 2147483647
      return false if !@commit_rate.nil? && @commit_rate.try &.< 0
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      return false if !@distance.nil? && @distance.try &.>= 1000000
      return false if !@distance.nil? && @distance.try &.<= -1000000
      distance_unit_validator = EnumAttributeValidatorForDistance_unit.new
      return false unless distance_unit_validator.valid?(@distance_unit)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] cid Value to be assigned
    def cid=(cid)
      if !cid.nil? && cid.to_s.size > 100
        raise ArgumentError.new("invalid value for \"cid\", the character length must be smaller than or equal to 100.")
      end

      if !cid.nil? && cid.to_s.size < 1
        raise ArgumentError.new("invalid value for \"cid\", the character length must be greater than or equal to 1.")
      end

      @cid = cid
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method with validation
    # @param [Object] commit_rate Value to be assigned
    def commit_rate=(commit_rate)
      if !commit_rate.nil? && commit_rate > 2147483647
        raise ArgumentError.new("invalid value for \"commit_rate\", must be smaller than or equal to 2147483647.")
      end

      if !commit_rate.nil? && commit_rate < 0
        raise ArgumentError.new("invalid value for \"commit_rate\", must be greater than or equal to 0.")
      end

      @commit_rate = commit_rate
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method with validation
    # @param [Object] distance Value to be assigned
    def distance=(distance)
      if !distance.nil? && distance >= 1000000
        raise ArgumentError.new("invalid value for \"distance\", must be smaller than 1000000.")
      end

      if !distance.nil? && distance <= -1000000
        raise ArgumentError.new("invalid value for \"distance\", must be greater than -1000000.")
      end

      @distance = distance
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] distance_unit Object to be assigned
    def distance_unit=(distance_unit)
      validator = EnumAttributeValidatorForDistance_unit.new
      unless validator.valid?(distance_unit)
        raise ArgumentError.new(validator.message)
      end
      @distance_unit = distance_unit
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        cid == other.cid &&
        provider == other.provider &&
        provider_account == other.provider_account &&
        _type == other._type &&
        status == other.status &&
        tenant == other.tenant &&
        install_date == other.install_date &&
        termination_date == other.termination_date &&
        commit_rate == other.commit_rate &&
        description == other.description &&
        distance == other.distance &&
        distance_unit == other.distance_unit &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields &&
        assignments == other.assignments
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [cid, provider, provider_account, _type, status, tenant, install_date, termination_date, commit_rate, description, distance, distance_unit, comments, tags, custom_fields, assignments].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["cid"] = _to_h(cid)
      hash["provider"] = _to_h(provider)
      hash["provider_account"] = _to_h(provider_account)
      hash["type"] = _to_h(_type)
      hash["status"] = _to_h(status)
      hash["tenant"] = _to_h(tenant)
      hash["install_date"] = _to_h(install_date)
      hash["termination_date"] = _to_h(termination_date)
      hash["commit_rate"] = _to_h(commit_rate)
      hash["description"] = _to_h(description)
      hash["distance"] = _to_h(distance)
      hash["distance_unit"] = _to_h(distance_unit)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash["assignments"] = _to_h(assignments)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
