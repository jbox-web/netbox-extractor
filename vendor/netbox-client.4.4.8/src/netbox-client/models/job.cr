# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  class Job
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display_url", type: String, nillable: false, emit_null: false)]
    property display_url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    @[JSON::Field(key: "object_type", type: String, nillable: false, emit_null: false)]
    property object_type : String

    @[JSON::Field(key: "object", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)), nillable: false, emit_null: false)]
    property object : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    @[JSON::Field(key: "status", type: BriefJobStatus, nillable: false, emit_null: false)]
    property status : BriefJobStatus

    @[JSON::Field(key: "created", type: Time, nillable: false, emit_null: false)]
    property created : Time

    @[JSON::Field(key: "user", type: BriefUser, nillable: false, emit_null: false)]
    property user : BriefUser

    @[JSON::Field(key: "error", type: String, nillable: false, emit_null: false)]
    property error : String

    @[JSON::Field(key: "job_id", type: String, nillable: false, emit_null: false)]
    property job_id : String

    # Optional properties
    @[JSON::Field(key: "object_id", type: Int64?, nillable: true, emit_null: false)]
    property object_id : Int64?

    @[JSON::Field(key: "scheduled", type: Time?, nillable: true, emit_null: false)]
    property scheduled : Time?

    # Recurrence interval (in minutes)
    @[JSON::Field(key: "interval", type: Int32?, nillable: true, emit_null: false)]
    property interval : Int32?

    @[JSON::Field(key: "started", type: Time?, nillable: true, emit_null: false)]
    property started : Time?

    @[JSON::Field(key: "completed", type: Time?, nillable: true, emit_null: false)]
    property completed : Time?

    @[JSON::Field(key: "data", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))?, nillable: true, emit_null: false)]
    property data : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))?

    @[JSON::Field(key: "log_entries", type: Array(Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)))?, nillable: true, emit_null: false)]
    property log_entries : Array(Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)))?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display_url : String, @display : String, @object_type : String, @object : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)), @name : String, @status : BriefJobStatus, @created : Time, @user : BriefUser, @error : String, @job_id : String, @object_id : Int64? = nil, @scheduled : Time? = nil, @interval : Int32? = nil, @started : Time? = nil, @completed : Time? = nil, @data : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))? = nil, @log_entries : Array(Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)))? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@object_id.nil? && @object_id.try &.> 9223372036854775807
        invalid_properties.push("invalid value for \"object_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if !@object_id.nil? && @object_id.try &.< 0
        invalid_properties.push("invalid value for \"object_id\", must be greater than or equal to 0.")
      end

      if @name.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 200.")
      end

      if !@interval.nil? && @interval.try &.> 2147483647
        invalid_properties.push("invalid value for \"interval\", must be smaller than or equal to 2147483647.")
      end

      if !@interval.nil? && @interval.try &.< 1
        invalid_properties.push("invalid value for \"interval\", must be greater than or equal to 1.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@object_id.nil? && @object_id.try &.> 9223372036854775807
      return false if !@object_id.nil? && @object_id.try &.< 0
      return false if @name.try &.to_s.try &.size.try &.> 200
      return false if !@interval.nil? && @interval.try &.> 2147483647
      return false if !@interval.nil? && @interval.try &.< 1
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] object_id Value to be assigned
    def object_id=(object_id)
      if !object_id.nil? && object_id > 9223372036854775807
        raise ArgumentError.new("invalid value for \"object_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if !object_id.nil? && object_id < 0
        raise ArgumentError.new("invalid value for \"object_id\", must be greater than or equal to 0.")
      end

      @object_id = object_id
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 200
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 200.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] interval Value to be assigned
    def interval=(interval)
      if !interval.nil? && interval > 2147483647
        raise ArgumentError.new("invalid value for \"interval\", must be smaller than or equal to 2147483647.")
      end

      if !interval.nil? && interval < 1
        raise ArgumentError.new("invalid value for \"interval\", must be greater than or equal to 1.")
      end

      @interval = interval
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display_url == other.display_url &&
        display == other.display &&
        object_type == other.object_type &&
        object_id == other.object_id &&
        object == other.object &&
        name == other.name &&
        status == other.status &&
        created == other.created &&
        scheduled == other.scheduled &&
        interval == other.interval &&
        started == other.started &&
        completed == other.completed &&
        user == other.user &&
        data == other.data &&
        error == other.error &&
        job_id == other.job_id &&
        log_entries == other.log_entries
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display_url, display, object_type, object_id, object, name, status, created, scheduled, interval, started, completed, user, data, error, job_id, log_entries].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["url"] = _to_h(url)
      hash["display_url"] = _to_h(display_url)
      hash["display"] = _to_h(display)
      hash["object_type"] = _to_h(object_type)
      hash["object_id"] = _to_h(object_id)
      hash["object"] = _to_h(object)
      hash["name"] = _to_h(name)
      hash["status"] = _to_h(status)
      hash["created"] = _to_h(created)
      hash["scheduled"] = _to_h(scheduled)
      hash["interval"] = _to_h(interval)
      hash["started"] = _to_h(started)
      hash["completed"] = _to_h(completed)
      hash["user"] = _to_h(user)
      hash["data"] = _to_h(data)
      hash["error"] = _to_h(error)
      hash["job_id"] = _to_h(job_id)
      hash["log_entries"] = _to_h(log_entries)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
