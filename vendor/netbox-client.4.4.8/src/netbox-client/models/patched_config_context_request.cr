# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Extends the built-in ModelSerializer to enforce calling full_clean() on a copy of the associated instance during validation. (DRF does not do this by default; see https://github.com/encode/django-rest-framework/issues/3144)
  class PatchedConfigContextRequest
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "name", type: String?, nillable: true, emit_null: false)]
    property name : String?

    @[JSON::Field(key: "weight", type: Int32?, nillable: true, emit_null: false)]
    property weight : Int32?

    @[JSON::Field(key: "profile", type: ConfigContextRequestProfile?, nillable: true, emit_null: false)]
    property profile : ConfigContextRequestProfile?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "is_active", type: Bool?, nillable: true, emit_null: false)]
    property is_active : Bool?

    @[JSON::Field(key: "regions", type: Array(Int32)?, nillable: true, emit_null: false)]
    property regions : Array(Int32)?

    @[JSON::Field(key: "site_groups", type: Array(Int32)?, nillable: true, emit_null: false)]
    property site_groups : Array(Int32)?

    @[JSON::Field(key: "sites", type: Array(Int32)?, nillable: true, emit_null: false)]
    property sites : Array(Int32)?

    @[JSON::Field(key: "locations", type: Array(Int32)?, nillable: true, emit_null: false)]
    property locations : Array(Int32)?

    @[JSON::Field(key: "device_types", type: Array(Int32)?, nillable: true, emit_null: false)]
    property device_types : Array(Int32)?

    @[JSON::Field(key: "roles", type: Array(Int32)?, nillable: true, emit_null: false)]
    property roles : Array(Int32)?

    @[JSON::Field(key: "platforms", type: Array(Int32)?, nillable: true, emit_null: false)]
    property platforms : Array(Int32)?

    @[JSON::Field(key: "cluster_types", type: Array(Int32)?, nillable: true, emit_null: false)]
    property cluster_types : Array(Int32)?

    @[JSON::Field(key: "cluster_groups", type: Array(Int32)?, nillable: true, emit_null: false)]
    property cluster_groups : Array(Int32)?

    @[JSON::Field(key: "clusters", type: Array(Int32)?, nillable: true, emit_null: false)]
    property clusters : Array(Int32)?

    @[JSON::Field(key: "tenant_groups", type: Array(Int32)?, nillable: true, emit_null: false)]
    property tenant_groups : Array(Int32)?

    @[JSON::Field(key: "tenants", type: Array(Int32)?, nillable: true, emit_null: false)]
    property tenants : Array(Int32)?

    @[JSON::Field(key: "tags", type: Array(String)?, nillable: true, emit_null: false)]
    property tags : Array(String)?

    @[JSON::Field(key: "data_source", type: ConfigContextProfileRequestDataSource?, nillable: true, emit_null: false)]
    property data_source : ConfigContextProfileRequestDataSource?

    @[JSON::Field(key: "data", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))?, nillable: true, emit_null: false)]
    property data : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@name : String? = nil, @weight : Int32? = nil, @profile : ConfigContextRequestProfile? = nil, @description : String? = nil, @is_active : Bool? = nil, @regions : Array(Int32)? = nil, @site_groups : Array(Int32)? = nil, @sites : Array(Int32)? = nil, @locations : Array(Int32)? = nil, @device_types : Array(Int32)? = nil, @roles : Array(Int32)? = nil, @platforms : Array(Int32)? = nil, @cluster_types : Array(Int32)? = nil, @cluster_groups : Array(Int32)? = nil, @clusters : Array(Int32)? = nil, @tenant_groups : Array(Int32)? = nil, @tenants : Array(Int32)? = nil, @tags : Array(String)? = nil, @data_source : ConfigContextProfileRequestDataSource? = nil, @data : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time | Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time))? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@name.nil? && @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !@name.nil? && @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if !@weight.nil? && @weight.try &.> 32767
        invalid_properties.push("invalid value for \"weight\", must be smaller than or equal to 32767.")
      end

      if !@weight.nil? && @weight.try &.< 0
        invalid_properties.push("invalid value for \"weight\", must be greater than or equal to 0.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@name.nil? && @name.try &.to_s.try &.size.try &.> 100
      return false if !@name.nil? && @name.try &.to_s.try &.size.try &.< 1
      return false if !@weight.nil? && @weight.try &.> 32767
      return false if !@weight.nil? && @weight.try &.< 0
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if !name.nil? && name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !name.nil? && name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] weight Value to be assigned
    def weight=(weight)
      if !weight.nil? && weight > 32767
        raise ArgumentError.new("invalid value for \"weight\", must be smaller than or equal to 32767.")
      end

      if !weight.nil? && weight < 0
        raise ArgumentError.new("invalid value for \"weight\", must be greater than or equal to 0.")
      end

      @weight = weight
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        name == other.name &&
        weight == other.weight &&
        profile == other.profile &&
        description == other.description &&
        is_active == other.is_active &&
        regions == other.regions &&
        site_groups == other.site_groups &&
        sites == other.sites &&
        locations == other.locations &&
        device_types == other.device_types &&
        roles == other.roles &&
        platforms == other.platforms &&
        cluster_types == other.cluster_types &&
        cluster_groups == other.cluster_groups &&
        clusters == other.clusters &&
        tenant_groups == other.tenant_groups &&
        tenants == other.tenants &&
        tags == other.tags &&
        data_source == other.data_source &&
        data == other.data
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [name, weight, profile, description, is_active, regions, site_groups, sites, locations, device_types, roles, platforms, cluster_types, cluster_groups, clusters, tenant_groups, tenants, tags, data_source, data].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["name"] = _to_h(name)
      hash["weight"] = _to_h(weight)
      hash["profile"] = _to_h(profile)
      hash["description"] = _to_h(description)
      hash["is_active"] = _to_h(is_active)
      hash["regions"] = _to_h(regions)
      hash["site_groups"] = _to_h(site_groups)
      hash["sites"] = _to_h(sites)
      hash["locations"] = _to_h(locations)
      hash["device_types"] = _to_h(device_types)
      hash["roles"] = _to_h(roles)
      hash["platforms"] = _to_h(platforms)
      hash["cluster_types"] = _to_h(cluster_types)
      hash["cluster_groups"] = _to_h(cluster_groups)
      hash["clusters"] = _to_h(clusters)
      hash["tenant_groups"] = _to_h(tenant_groups)
      hash["tenants"] = _to_h(tenants)
      hash["tags"] = _to_h(tags)
      hash["data_source"] = _to_h(data_source)
      hash["data"] = _to_h(data)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
