# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class RackTypeRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "manufacturer", type: BriefDeviceTypeRequestManufacturer, nillable: false, emit_null: false)]
    property manufacturer : BriefDeviceTypeRequestManufacturer

    @[JSON::Field(key: "model", type: String, nillable: false, emit_null: false)]
    property model : String

    @[JSON::Field(key: "slug", type: String, nillable: false, emit_null: false)]
    property slug : String

    # Optional properties
    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # * `2-post-frame` - Châssis à 2 montants * `4-post-frame` - Châssis à 4 montants * `4-post-cabinet` - Armoire à 4 montants * `wall-frame` - Châssis mural * `wall-frame-vertical` - Châssis mural (vertical) * `wall-cabinet` - Armoire murale * `wall-cabinet-vertical` - Armoire murale (verticale)
    @[JSON::Field(key: "form_factor", type: String?, nillable: true, emit_null: false)]
    property form_factor : String?

    # * `10` - 10 inches * `19` - 19 inches * `21` - 21 inches * `23` - 23 inches
    @[JSON::Field(key: "width", type: Int32?, nillable: true, emit_null: false)]
    property width : Int32?

    # Hauteur en U de la baie
    @[JSON::Field(key: "u_height", type: Int32?, nillable: true, emit_null: false)]
    property u_height : Int32?

    # Unité de départ pour baie
    @[JSON::Field(key: "starting_unit", type: Int32?, nillable: true, emit_null: false)]
    property starting_unit : Int32?

    # Les unités sont numérotées de haut en bas
    @[JSON::Field(key: "desc_units", type: Bool?, nillable: true, emit_null: false)]
    property desc_units : Bool?

    # Dimension extérieure de la baie (largeur)
    @[JSON::Field(key: "outer_width", type: Int32?, nillable: true, emit_null: false)]
    property outer_width : Int32?

    # Dimension extérieure du rack (hauteur)
    @[JSON::Field(key: "outer_height", type: Int32?, nillable: true, emit_null: false)]
    property outer_height : Int32?

    # Dimension extérieure de la baie (profondeur)
    @[JSON::Field(key: "outer_depth", type: Int32?, nillable: true, emit_null: false)]
    property outer_depth : Int32?

    # * `mm` - Millimètres * `in` - Pouces
    @[JSON::Field(key: "outer_unit", type: String?, nillable: true, emit_null: false)]
    property outer_unit : String?

    @[JSON::Field(key: "weight", type: Float64?, nillable: true, emit_null: false)]
    property weight : Float64?

    # Capacité de charge maximale de la baie
    @[JSON::Field(key: "max_weight", type: Int32?, nillable: true, emit_null: false)]
    property max_weight : Int32?

    # * `kg` - Kilogrammes * `g` - Grammes * `lb` - Livres * `oz` - Onces
    @[JSON::Field(key: "weight_unit", type: String?, nillable: true, emit_null: false)]
    property weight_unit : String?

    # Profondeur maximale d'un appareil monté, en millimètres. Pour les supports à quatre montants, il s'agit de la distance entre les rails avant et arrière.
    @[JSON::Field(key: "mounting_depth", type: Int32?, nillable: true, emit_null: false)]
    property mounting_depth : Int32?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForForm_factor < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "form_factor"
        @allowable_values = ["2-post-frame", "4-post-frame", "4-post-cabinet", "wall-frame", "wall-frame-vertical", "wall-cabinet", "wall-cabinet-vertical", ""].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForWidth < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "width"
        @allowable_values = ["10", "19", "21", "23"].map { |value| to(Int32, value) }
      end
    end

    class EnumAttributeValidatorForOuter_unit < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "outer_unit"
        @allowable_values = ["mm", "in", ""].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForWeight_unit < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "weight_unit"
        @allowable_values = ["kg", "g", "lb", "oz", ""].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@manufacturer : BriefDeviceTypeRequestManufacturer, @model : String, @slug : String, @description : String? = nil, @form_factor : String? = nil, @width : Int32? = nil, @u_height : Int32? = nil, @starting_unit : Int32? = nil, @desc_units : Bool? = nil, @outer_width : Int32? = nil, @outer_height : Int32? = nil, @outer_depth : Int32? = nil, @outer_unit : String? = nil, @weight : Float64? = nil, @max_weight : Int32? = nil, @weight_unit : String? = nil, @mounting_depth : Int32? = nil, @comments : String? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @model.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"model\", the character length must be smaller than or equal to 100.")
      end

      if @model.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"model\", the character length must be greater than or equal to 1.")
      end

      if @slug.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"slug\", the character length must be smaller than or equal to 100.")
      end

      if @slug.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"slug\", the character length must be greater than or equal to 1.")
      end

      pattern = /^[-a-zA-Z0-9_]+$/
      if @slug.try &.!~ pattern
        invalid_properties.push("invalid value for \"slug\", must conform to the pattern #{pattern}.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      form_factor_validator = EnumAttributeValidatorForForm_factor.new
      if !form_factor_validator.valid?(@form_factor)
        message = form_factor_validator.message
        invalid_properties.push(message)
      end

      width_validator = EnumAttributeValidatorForWidth.new
      if !width_validator.valid?(@width)
        message = width_validator.message
        invalid_properties.push(message)
      end

      if !@u_height.nil? && @u_height.try &.> 100
        invalid_properties.push("invalid value for \"u_height\", must be smaller than or equal to 100.")
      end

      if !@u_height.nil? && @u_height.try &.< 1
        invalid_properties.push("invalid value for \"u_height\", must be greater than or equal to 1.")
      end

      if !@starting_unit.nil? && @starting_unit.try &.> 32767
        invalid_properties.push("invalid value for \"starting_unit\", must be smaller than or equal to 32767.")
      end

      if !@starting_unit.nil? && @starting_unit.try &.< 1
        invalid_properties.push("invalid value for \"starting_unit\", must be greater than or equal to 1.")
      end

      if !@outer_width.nil? && @outer_width.try &.> 32767
        invalid_properties.push("invalid value for \"outer_width\", must be smaller than or equal to 32767.")
      end

      if !@outer_width.nil? && @outer_width.try &.< 0
        invalid_properties.push("invalid value for \"outer_width\", must be greater than or equal to 0.")
      end

      if !@outer_height.nil? && @outer_height.try &.> 32767
        invalid_properties.push("invalid value for \"outer_height\", must be smaller than or equal to 32767.")
      end

      if !@outer_height.nil? && @outer_height.try &.< 0
        invalid_properties.push("invalid value for \"outer_height\", must be greater than or equal to 0.")
      end

      if !@outer_depth.nil? && @outer_depth.try &.> 32767
        invalid_properties.push("invalid value for \"outer_depth\", must be smaller than or equal to 32767.")
      end

      if !@outer_depth.nil? && @outer_depth.try &.< 0
        invalid_properties.push("invalid value for \"outer_depth\", must be greater than or equal to 0.")
      end

      outer_unit_validator = EnumAttributeValidatorForOuter_unit.new
      if !outer_unit_validator.valid?(@outer_unit)
        message = outer_unit_validator.message
        invalid_properties.push(message)
      end

      if !@weight.nil? && @weight.try &.>= 1000000
        invalid_properties.push("invalid value for \"weight\", must be smaller than 1000000.")
      end

      if !@weight.nil? && @weight.try &.<= -1000000
        invalid_properties.push("invalid value for \"weight\", must be greater than -1000000.")
      end

      if !@max_weight.nil? && @max_weight.try &.> 2147483647
        invalid_properties.push("invalid value for \"max_weight\", must be smaller than or equal to 2147483647.")
      end

      if !@max_weight.nil? && @max_weight.try &.< 0
        invalid_properties.push("invalid value for \"max_weight\", must be greater than or equal to 0.")
      end

      weight_unit_validator = EnumAttributeValidatorForWeight_unit.new
      if !weight_unit_validator.valid?(@weight_unit)
        message = weight_unit_validator.message
        invalid_properties.push(message)
      end

      if !@mounting_depth.nil? && @mounting_depth.try &.> 32767
        invalid_properties.push("invalid value for \"mounting_depth\", must be smaller than or equal to 32767.")
      end

      if !@mounting_depth.nil? && @mounting_depth.try &.< 0
        invalid_properties.push("invalid value for \"mounting_depth\", must be greater than or equal to 0.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @model.try &.to_s.try &.size.try &.> 100
      return false if @model.try &.to_s.try &.size.try &.< 1
      return false if @slug.try &.to_s.try &.size.try &.> 100
      return false if @slug.try &.to_s.try &.size.try &.< 1
      return false if @slug.try &.!~ /^[-a-zA-Z0-9_]+$/
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      form_factor_validator = EnumAttributeValidatorForForm_factor.new
      return false unless form_factor_validator.valid?(@form_factor)
      width_validator = EnumAttributeValidatorForWidth.new
      return false unless width_validator.valid?(@width)
      return false if !@u_height.nil? && @u_height.try &.> 100
      return false if !@u_height.nil? && @u_height.try &.< 1
      return false if !@starting_unit.nil? && @starting_unit.try &.> 32767
      return false if !@starting_unit.nil? && @starting_unit.try &.< 1
      return false if !@outer_width.nil? && @outer_width.try &.> 32767
      return false if !@outer_width.nil? && @outer_width.try &.< 0
      return false if !@outer_height.nil? && @outer_height.try &.> 32767
      return false if !@outer_height.nil? && @outer_height.try &.< 0
      return false if !@outer_depth.nil? && @outer_depth.try &.> 32767
      return false if !@outer_depth.nil? && @outer_depth.try &.< 0
      outer_unit_validator = EnumAttributeValidatorForOuter_unit.new
      return false unless outer_unit_validator.valid?(@outer_unit)
      return false if !@weight.nil? && @weight.try &.>= 1000000
      return false if !@weight.nil? && @weight.try &.<= -1000000
      return false if !@max_weight.nil? && @max_weight.try &.> 2147483647
      return false if !@max_weight.nil? && @max_weight.try &.< 0
      weight_unit_validator = EnumAttributeValidatorForWeight_unit.new
      return false unless weight_unit_validator.valid?(@weight_unit)
      return false if !@mounting_depth.nil? && @mounting_depth.try &.> 32767
      return false if !@mounting_depth.nil? && @mounting_depth.try &.< 0
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] model Value to be assigned
    def model=(model)
      if model.to_s.size > 100
        raise ArgumentError.new("invalid value for \"model\", the character length must be smaller than or equal to 100.")
      end

      if model.to_s.size < 1
        raise ArgumentError.new("invalid value for \"model\", the character length must be greater than or equal to 1.")
      end

      @model = model
    end

    # Custom attribute writer method with validation
    # @param [Object] slug Value to be assigned
    def slug=(slug)
      if slug.to_s.size > 100
        raise ArgumentError.new("invalid value for \"slug\", the character length must be smaller than or equal to 100.")
      end

      if slug.to_s.size < 1
        raise ArgumentError.new("invalid value for \"slug\", the character length must be greater than or equal to 1.")
      end

      pattern = /^[-a-zA-Z0-9_]+$/
      if slug !~ pattern
        raise ArgumentError.new("invalid value for \"slug\", must conform to the pattern #{pattern}.")
      end

      @slug = slug
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] form_factor Object to be assigned
    def form_factor=(form_factor)
      validator = EnumAttributeValidatorForForm_factor.new
      unless validator.valid?(form_factor)
        raise ArgumentError.new(validator.message)
      end
      @form_factor = form_factor
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] width Object to be assigned
    def width=(width)
      validator = EnumAttributeValidatorForWidth.new
      unless validator.valid?(width)
        raise ArgumentError.new(validator.message)
      end
      @width = width
    end

    # Custom attribute writer method with validation
    # @param [Object] u_height Value to be assigned
    def u_height=(u_height)
      if !u_height.nil? && u_height > 100
        raise ArgumentError.new("invalid value for \"u_height\", must be smaller than or equal to 100.")
      end

      if !u_height.nil? && u_height < 1
        raise ArgumentError.new("invalid value for \"u_height\", must be greater than or equal to 1.")
      end

      @u_height = u_height
    end

    # Custom attribute writer method with validation
    # @param [Object] starting_unit Value to be assigned
    def starting_unit=(starting_unit)
      if !starting_unit.nil? && starting_unit > 32767
        raise ArgumentError.new("invalid value for \"starting_unit\", must be smaller than or equal to 32767.")
      end

      if !starting_unit.nil? && starting_unit < 1
        raise ArgumentError.new("invalid value for \"starting_unit\", must be greater than or equal to 1.")
      end

      @starting_unit = starting_unit
    end

    # Custom attribute writer method with validation
    # @param [Object] outer_width Value to be assigned
    def outer_width=(outer_width)
      if !outer_width.nil? && outer_width > 32767
        raise ArgumentError.new("invalid value for \"outer_width\", must be smaller than or equal to 32767.")
      end

      if !outer_width.nil? && outer_width < 0
        raise ArgumentError.new("invalid value for \"outer_width\", must be greater than or equal to 0.")
      end

      @outer_width = outer_width
    end

    # Custom attribute writer method with validation
    # @param [Object] outer_height Value to be assigned
    def outer_height=(outer_height)
      if !outer_height.nil? && outer_height > 32767
        raise ArgumentError.new("invalid value for \"outer_height\", must be smaller than or equal to 32767.")
      end

      if !outer_height.nil? && outer_height < 0
        raise ArgumentError.new("invalid value for \"outer_height\", must be greater than or equal to 0.")
      end

      @outer_height = outer_height
    end

    # Custom attribute writer method with validation
    # @param [Object] outer_depth Value to be assigned
    def outer_depth=(outer_depth)
      if !outer_depth.nil? && outer_depth > 32767
        raise ArgumentError.new("invalid value for \"outer_depth\", must be smaller than or equal to 32767.")
      end

      if !outer_depth.nil? && outer_depth < 0
        raise ArgumentError.new("invalid value for \"outer_depth\", must be greater than or equal to 0.")
      end

      @outer_depth = outer_depth
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] outer_unit Object to be assigned
    def outer_unit=(outer_unit)
      validator = EnumAttributeValidatorForOuter_unit.new
      unless validator.valid?(outer_unit)
        raise ArgumentError.new(validator.message)
      end
      @outer_unit = outer_unit
    end

    # Custom attribute writer method with validation
    # @param [Object] weight Value to be assigned
    def weight=(weight)
      if !weight.nil? && weight >= 1000000
        raise ArgumentError.new("invalid value for \"weight\", must be smaller than 1000000.")
      end

      if !weight.nil? && weight <= -1000000
        raise ArgumentError.new("invalid value for \"weight\", must be greater than -1000000.")
      end

      @weight = weight
    end

    # Custom attribute writer method with validation
    # @param [Object] max_weight Value to be assigned
    def max_weight=(max_weight)
      if !max_weight.nil? && max_weight > 2147483647
        raise ArgumentError.new("invalid value for \"max_weight\", must be smaller than or equal to 2147483647.")
      end

      if !max_weight.nil? && max_weight < 0
        raise ArgumentError.new("invalid value for \"max_weight\", must be greater than or equal to 0.")
      end

      @max_weight = max_weight
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] weight_unit Object to be assigned
    def weight_unit=(weight_unit)
      validator = EnumAttributeValidatorForWeight_unit.new
      unless validator.valid?(weight_unit)
        raise ArgumentError.new(validator.message)
      end
      @weight_unit = weight_unit
    end

    # Custom attribute writer method with validation
    # @param [Object] mounting_depth Value to be assigned
    def mounting_depth=(mounting_depth)
      if !mounting_depth.nil? && mounting_depth > 32767
        raise ArgumentError.new("invalid value for \"mounting_depth\", must be smaller than or equal to 32767.")
      end

      if !mounting_depth.nil? && mounting_depth < 0
        raise ArgumentError.new("invalid value for \"mounting_depth\", must be greater than or equal to 0.")
      end

      @mounting_depth = mounting_depth
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        manufacturer == other.manufacturer &&
        model == other.model &&
        slug == other.slug &&
        description == other.description &&
        form_factor == other.form_factor &&
        width == other.width &&
        u_height == other.u_height &&
        starting_unit == other.starting_unit &&
        desc_units == other.desc_units &&
        outer_width == other.outer_width &&
        outer_height == other.outer_height &&
        outer_depth == other.outer_depth &&
        outer_unit == other.outer_unit &&
        weight == other.weight &&
        max_weight == other.max_weight &&
        weight_unit == other.weight_unit &&
        mounting_depth == other.mounting_depth &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [manufacturer, model, slug, description, form_factor, width, u_height, starting_unit, desc_units, outer_width, outer_height, outer_depth, outer_unit, weight, max_weight, weight_unit, mounting_depth, comments, tags, custom_fields].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["manufacturer"] = _to_h(manufacturer)
      hash["model"] = _to_h(model)
      hash["slug"] = _to_h(slug)
      hash["description"] = _to_h(description)
      hash["form_factor"] = _to_h(form_factor)
      hash["width"] = _to_h(width)
      hash["u_height"] = _to_h(u_height)
      hash["starting_unit"] = _to_h(starting_unit)
      hash["desc_units"] = _to_h(desc_units)
      hash["outer_width"] = _to_h(outer_width)
      hash["outer_height"] = _to_h(outer_height)
      hash["outer_depth"] = _to_h(outer_depth)
      hash["outer_unit"] = _to_h(outer_unit)
      hash["weight"] = _to_h(weight)
      hash["max_weight"] = _to_h(max_weight)
      hash["weight_unit"] = _to_h(weight_unit)
      hash["mounting_depth"] = _to_h(mounting_depth)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
