# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Extends the built-in ModelSerializer to enforce calling full_clean() on a copy of the associated instance during validation. (DRF does not do this by default; see https://github.com/encode/django-rest-framework/issues/3144)
  class CustomLinkRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "object_types", type: Array(String), nillable: false, emit_null: false)]
    property object_types : Array(String)

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # Code modèle Jinja2 pour le texte du lien
    @[JSON::Field(key: "link_text", type: String, nillable: false, emit_null: false)]
    property link_text : String

    # Code modèle Jinja2 pour l'URL du lien
    @[JSON::Field(key: "link_url", type: String, nillable: false, emit_null: false)]
    property link_url : String

    # Optional properties
    @[JSON::Field(key: "enabled", type: Bool?, nillable: true, emit_null: false)]
    property enabled : Bool?

    @[JSON::Field(key: "weight", type: Int32?, nillable: true, emit_null: false)]
    property weight : Int32?

    # Les liens avec le même groupe apparaîtront dans un menu déroulant
    @[JSON::Field(key: "group_name", type: String?, nillable: true, emit_null: false)]
    property group_name : String?

    # La classe du premier lien d'un groupe sera utilisée pour le bouton déroulant  * `default` - Par défaut * `blue` - Bleu * `indigo` - Indigo * `purple` - Violet * `pink` - Rose * `red` - Rouge * `orange` - Orange * `yellow` - Jaune * `green` - Vert * `teal` - Sarcelle * `cyan` - Cyan * `gray` - Gris * `black` - Noir * `white` - Blanc * `ghost-dark` - Lien
    @[JSON::Field(key: "button_class", type: String?, nillable: true, emit_null: false)]
    property button_class : String?

    # Forcer l'ouverture du lien dans une nouvelle fenêtre
    @[JSON::Field(key: "new_window", type: Bool?, nillable: true, emit_null: false)]
    property new_window : Bool?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForButton_class < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "button_class"
        @allowable_values = ["default", "blue", "indigo", "purple", "pink", "red", "orange", "yellow", "green", "teal", "cyan", "gray", "black", "white", "ghost-dark"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@object_types : Array(String), @name : String, @link_text : String, @link_url : String, @enabled : Bool? = nil, @weight : Int32? = nil, @group_name : String? = nil, @button_class : String? = nil, @new_window : Bool? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if @link_text.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"link_text\", the character length must be greater than or equal to 1.")
      end

      if @link_url.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"link_url\", the character length must be greater than or equal to 1.")
      end

      if !@weight.nil? && @weight.try &.> 32767
        invalid_properties.push("invalid value for \"weight\", must be smaller than or equal to 32767.")
      end

      if !@weight.nil? && @weight.try &.< 0
        invalid_properties.push("invalid value for \"weight\", must be greater than or equal to 0.")
      end

      if !@group_name.nil? && @group_name.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"group_name\", the character length must be smaller than or equal to 50.")
      end

      button_class_validator = EnumAttributeValidatorForButton_class.new
      if !button_class_validator.valid?(@button_class)
        message = button_class_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 100
      return false if @name.try &.to_s.try &.size.try &.< 1
      return false if @link_text.try &.to_s.try &.size.try &.< 1
      return false if @link_url.try &.to_s.try &.size.try &.< 1
      return false if !@weight.nil? && @weight.try &.> 32767
      return false if !@weight.nil? && @weight.try &.< 0
      return false if !@group_name.nil? && @group_name.try &.to_s.try &.size.try &.> 50
      button_class_validator = EnumAttributeValidatorForButton_class.new
      return false unless button_class_validator.valid?(@button_class)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] link_text Value to be assigned
    def link_text=(link_text)
      if link_text.to_s.size < 1
        raise ArgumentError.new("invalid value for \"link_text\", the character length must be greater than or equal to 1.")
      end

      @link_text = link_text
    end

    # Custom attribute writer method with validation
    # @param [Object] link_url Value to be assigned
    def link_url=(link_url)
      if link_url.to_s.size < 1
        raise ArgumentError.new("invalid value for \"link_url\", the character length must be greater than or equal to 1.")
      end

      @link_url = link_url
    end

    # Custom attribute writer method with validation
    # @param [Object] weight Value to be assigned
    def weight=(weight)
      if !weight.nil? && weight > 32767
        raise ArgumentError.new("invalid value for \"weight\", must be smaller than or equal to 32767.")
      end

      if !weight.nil? && weight < 0
        raise ArgumentError.new("invalid value for \"weight\", must be greater than or equal to 0.")
      end

      @weight = weight
    end

    # Custom attribute writer method with validation
    # @param [Object] group_name Value to be assigned
    def group_name=(group_name)
      if !group_name.nil? && group_name.to_s.size > 50
        raise ArgumentError.new("invalid value for \"group_name\", the character length must be smaller than or equal to 50.")
      end

      @group_name = group_name
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] button_class Object to be assigned
    def button_class=(button_class)
      validator = EnumAttributeValidatorForButton_class.new
      unless validator.valid?(button_class)
        raise ArgumentError.new(validator.message)
      end
      @button_class = button_class
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        object_types == other.object_types &&
        name == other.name &&
        enabled == other.enabled &&
        link_text == other.link_text &&
        link_url == other.link_url &&
        weight == other.weight &&
        group_name == other.group_name &&
        button_class == other.button_class &&
        new_window == other.new_window
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [object_types, name, enabled, link_text, link_url, weight, group_name, button_class, new_window].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["object_types"] = _to_h(object_types)
      hash["name"] = _to_h(name)
      hash["enabled"] = _to_h(enabled)
      hash["link_text"] = _to_h(link_text)
      hash["link_url"] = _to_h(link_url)
      hash["weight"] = _to_h(weight)
      hash["group_name"] = _to_h(group_name)
      hash["button_class"] = _to_h(button_class)
      hash["new_window"] = _to_h(new_window)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
