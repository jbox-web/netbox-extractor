# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.8 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class VMInterface
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display_url", type: String, nillable: false, emit_null: false)]
    property display_url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    @[JSON::Field(key: "virtual_machine", type: BriefVirtualMachine, nillable: false, emit_null: false)]
    property virtual_machine : BriefVirtualMachine

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    @[JSON::Field(key: "mac_address", type: String, nillable: false, emit_null: false)]
    property mac_address : String

    @[JSON::Field(key: "mac_addresses", type: Array(BriefMACAddress), nillable: false, emit_null: false)]
    property mac_addresses : Array(BriefMACAddress)

    @[JSON::Field(key: "l2vpn_termination", type: BriefL2VPNTermination, nillable: false, emit_null: false)]
    property l2vpn_termination : BriefL2VPNTermination

    @[JSON::Field(key: "created", type: Time, nillable: false, emit_null: false)]
    property created : Time

    @[JSON::Field(key: "last_updated", type: Time, nillable: false, emit_null: false)]
    property last_updated : Time

    @[JSON::Field(key: "count_ipaddresses", type: Int32, nillable: false, emit_null: false)]
    property count_ipaddresses : Int32

    @[JSON::Field(key: "count_fhrp_groups", type: Int32, nillable: false, emit_null: false)]
    property count_fhrp_groups : Int32

    # Optional properties
    @[JSON::Field(key: "enabled", type: Bool?, nillable: true, emit_null: false)]
    property enabled : Bool?

    @[JSON::Field(key: "parent", type: NestedVMInterface?, nillable: true, emit_null: false)]
    property parent : NestedVMInterface?

    @[JSON::Field(key: "bridge", type: NestedVMInterface?, nillable: true, emit_null: false)]
    property bridge : NestedVMInterface?

    @[JSON::Field(key: "mtu", type: Int32?, nillable: true, emit_null: false)]
    property mtu : Int32?

    @[JSON::Field(key: "primary_mac_address", type: BriefMACAddress?, nillable: true, emit_null: false)]
    property primary_mac_address : BriefMACAddress?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "mode", type: InterfaceMode?, nillable: true, emit_null: false)]
    property mode : InterfaceMode?

    @[JSON::Field(key: "untagged_vlan", type: BriefVLAN?, nillable: true, emit_null: false)]
    property untagged_vlan : BriefVLAN?

    @[JSON::Field(key: "tagged_vlans", type: Array(VLAN)?, nillable: true, emit_null: false)]
    property tagged_vlans : Array(VLAN)?

    @[JSON::Field(key: "qinq_svlan", type: BriefVLAN?, nillable: true, emit_null: false)]
    property qinq_svlan : BriefVLAN?

    @[JSON::Field(key: "vlan_translation_policy", type: BriefVLANTranslationPolicy?, nillable: true, emit_null: false)]
    property vlan_translation_policy : BriefVLANTranslationPolicy?

    @[JSON::Field(key: "vrf", type: BriefVRF?, nillable: true, emit_null: false)]
    property vrf : BriefVRF?

    @[JSON::Field(key: "tags", type: Array(NestedTag)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTag)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display_url : String, @display : String, @virtual_machine : BriefVirtualMachine, @name : String, @mac_address : String, @mac_addresses : Array(BriefMACAddress), @l2vpn_termination : BriefL2VPNTermination, @created : Time, @last_updated : Time, @count_ipaddresses : Int32, @count_fhrp_groups : Int32, @enabled : Bool? = nil, @parent : NestedVMInterface? = nil, @bridge : NestedVMInterface? = nil, @mtu : Int32? = nil, @primary_mac_address : BriefMACAddress? = nil, @description : String? = nil, @mode : InterfaceMode? = nil, @untagged_vlan : BriefVLAN? = nil, @tagged_vlans : Array(VLAN)? = nil, @qinq_svlan : BriefVLAN? = nil, @vlan_translation_policy : BriefVLANTranslationPolicy? = nil, @vrf : BriefVRF? = nil, @tags : Array(NestedTag)? = nil, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 64
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if !@mtu.nil? && @mtu.try &.> 65536
        invalid_properties.push("invalid value for \"mtu\", must be smaller than or equal to 65536.")
      end

      if !@mtu.nil? && @mtu.try &.< 1
        invalid_properties.push("invalid value for \"mtu\", must be greater than or equal to 1.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 64
      return false if !@mtu.nil? && @mtu.try &.> 65536
      return false if !@mtu.nil? && @mtu.try &.< 1
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 64
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] mtu Value to be assigned
    def mtu=(mtu)
      if !mtu.nil? && mtu > 65536
        raise ArgumentError.new("invalid value for \"mtu\", must be smaller than or equal to 65536.")
      end

      if !mtu.nil? && mtu < 1
        raise ArgumentError.new("invalid value for \"mtu\", must be greater than or equal to 1.")
      end

      @mtu = mtu
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display_url == other.display_url &&
        display == other.display &&
        virtual_machine == other.virtual_machine &&
        name == other.name &&
        enabled == other.enabled &&
        parent == other.parent &&
        bridge == other.bridge &&
        mtu == other.mtu &&
        mac_address == other.mac_address &&
        primary_mac_address == other.primary_mac_address &&
        mac_addresses == other.mac_addresses &&
        description == other.description &&
        mode == other.mode &&
        untagged_vlan == other.untagged_vlan &&
        tagged_vlans == other.tagged_vlans &&
        qinq_svlan == other.qinq_svlan &&
        vlan_translation_policy == other.vlan_translation_policy &&
        vrf == other.vrf &&
        l2vpn_termination == other.l2vpn_termination &&
        tags == other.tags &&
        custom_fields == other.custom_fields &&
        created == other.created &&
        last_updated == other.last_updated &&
        count_ipaddresses == other.count_ipaddresses &&
        count_fhrp_groups == other.count_fhrp_groups
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display_url, display, virtual_machine, name, enabled, parent, bridge, mtu, mac_address, primary_mac_address, mac_addresses, description, mode, untagged_vlan, tagged_vlans, qinq_svlan, vlan_translation_policy, vrf, l2vpn_termination, tags, custom_fields, created, last_updated, count_ipaddresses, count_fhrp_groups].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["url"] = _to_h(url)
      hash["display_url"] = _to_h(display_url)
      hash["display"] = _to_h(display)
      hash["virtual_machine"] = _to_h(virtual_machine)
      hash["name"] = _to_h(name)
      hash["enabled"] = _to_h(enabled)
      hash["parent"] = _to_h(parent)
      hash["bridge"] = _to_h(bridge)
      hash["mtu"] = _to_h(mtu)
      hash["mac_address"] = _to_h(mac_address)
      hash["primary_mac_address"] = _to_h(primary_mac_address)
      hash["mac_addresses"] = _to_h(mac_addresses)
      hash["description"] = _to_h(description)
      hash["mode"] = _to_h(mode)
      hash["untagged_vlan"] = _to_h(untagged_vlan)
      hash["tagged_vlans"] = _to_h(tagged_vlans)
      hash["qinq_svlan"] = _to_h(qinq_svlan)
      hash["vlan_translation_policy"] = _to_h(vlan_translation_policy)
      hash["vrf"] = _to_h(vrf)
      hash["l2vpn_termination"] = _to_h(l2vpn_termination)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash["created"] = _to_h(created)
      hash["last_updated"] = _to_h(last_updated)
      hash["count_ipaddresses"] = _to_h(count_ipaddresses)
      hash["count_fhrp_groups"] = _to_h(count_fhrp_groups)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
