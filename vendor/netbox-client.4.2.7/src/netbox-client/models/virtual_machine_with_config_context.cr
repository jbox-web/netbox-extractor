# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.2.7 (4.2)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class VirtualMachineWithConfigContext
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display_url", type: String, nillable: false, emit_null: false)]
    property display_url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    @[JSON::Field(key: "primary_ip", type: BriefIPAddress?, nillable: true, emit_null: false)]
    property primary_ip : BriefIPAddress?

    @[JSON::Field(key: "config_context", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property config_context : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    @[JSON::Field(key: "created", type: Time, nillable: false, emit_null: false)]
    property created : Time

    @[JSON::Field(key: "last_updated", type: Time, nillable: false, emit_null: false)]
    property last_updated : Time

    @[JSON::Field(key: "interface_count", type: Int32, nillable: false, emit_null: false)]
    property interface_count : Int32

    @[JSON::Field(key: "virtual_disk_count", type: Int32, nillable: false, emit_null: false)]
    property virtual_disk_count : Int32

    # Optional properties
    @[JSON::Field(key: "status", type: VirtualMachineWithConfigContextStatus?, nillable: true, emit_null: false)]
    property status : VirtualMachineWithConfigContextStatus?

    @[JSON::Field(key: "site", type: BriefSite?, nillable: true, emit_null: false)]
    property site : BriefSite?

    @[JSON::Field(key: "cluster", type: BriefCluster?, nillable: true, emit_null: false)]
    property cluster : BriefCluster?

    @[JSON::Field(key: "device", type: BriefDevice?, nillable: true, emit_null: false)]
    property device : BriefDevice?

    @[JSON::Field(key: "serial", type: String?, nillable: true, emit_null: false)]
    property serial : String?

    @[JSON::Field(key: "role", type: BriefDeviceRole?, nillable: true, emit_null: false)]
    property role : BriefDeviceRole?

    @[JSON::Field(key: "tenant", type: BriefTenant?, nillable: true, emit_null: false)]
    property tenant : BriefTenant?

    @[JSON::Field(key: "platform", type: BriefPlatform?, nillable: true, emit_null: false)]
    property platform : BriefPlatform?

    @[JSON::Field(key: "primary_ip4", type: BriefIPAddress?, nillable: true, emit_null: false)]
    property primary_ip4 : BriefIPAddress?

    @[JSON::Field(key: "primary_ip6", type: BriefIPAddress?, nillable: true, emit_null: false)]
    property primary_ip6 : BriefIPAddress?

    @[JSON::Field(key: "vcpus", type: Float64?, nillable: true, emit_null: false)]
    property vcpus : Float64?

    @[JSON::Field(key: "memory", type: Int32?, nillable: true, emit_null: false)]
    property memory : Int32?

    @[JSON::Field(key: "disk", type: Int32?, nillable: true, emit_null: false)]
    property disk : Int32?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "config_template", type: BriefConfigTemplate?, nillable: true, emit_null: false)]
    property config_template : BriefConfigTemplate?

    # Les données du contexte de configuration local ont priorité sur les contextes source dans le contexte de configuration final rendu
    @[JSON::Field(key: "local_context_data", type: Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time?, nillable: true, emit_null: false)]
    property local_context_data : Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time?

    @[JSON::Field(key: "tags", type: Array(NestedTag)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTag)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display_url : String, @display : String, @name : String, @primary_ip : BriefIPAddress, @config_context : Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time, @created : Time, @last_updated : Time, @interface_count : Int32, @virtual_disk_count : Int32, @status : VirtualMachineWithConfigContextStatus?, @site : BriefSite?, @cluster : BriefCluster?, @device : BriefDevice?, @serial : String?, @role : BriefDeviceRole?, @tenant : BriefTenant?, @platform : BriefPlatform?, @primary_ip4 : BriefIPAddress?, @primary_ip6 : BriefIPAddress?, @vcpus : Float64?, @memory : Int32?, @disk : Int32?, @description : String?, @comments : String?, @config_template : BriefConfigTemplate?, @local_context_data : Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time?, @tags : Array(NestedTag)?, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.to_s.size > 64
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if !@serial.nil? && @serial.to_s.size > 50
        invalid_properties.push("invalid value for \"serial\", the character length must be smaller than or equal to 50.")
      end

      if !@vcpus.nil? && @vcpus >= 10000
        invalid_properties.push("invalid value for \"vcpus\", must be smaller than 10000.")
      end

      if !@vcpus.nil? && @vcpus < 0.01
        invalid_properties.push("invalid value for \"vcpus\", must be greater than or equal to 0.01.")
      end

      if !@memory.nil? && @memory > 2147483647
        invalid_properties.push("invalid value for \"memory\", must be smaller than or equal to 2147483647.")
      end

      if !@memory.nil? && @memory < 0
        invalid_properties.push("invalid value for \"memory\", must be greater than or equal to 0.")
      end

      if !@disk.nil? && @disk > 2147483647
        invalid_properties.push("invalid value for \"disk\", must be smaller than or equal to 2147483647.")
      end

      if !@disk.nil? && @disk < 0
        invalid_properties.push("invalid value for \"disk\", must be greater than or equal to 0.")
      end

      if !@description.nil? && @description.to_s.size > 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.to_s.size > 64
      return false if !@serial.nil? && @serial.to_s.size > 50
      return false if !@vcpus.nil? && @vcpus >= 10000
      return false if !@vcpus.nil? && @vcpus < 0.01
      return false if !@memory.nil? && @memory > 2147483647
      return false if !@memory.nil? && @memory < 0
      return false if !@disk.nil? && @disk > 2147483647
      return false if !@disk.nil? && @disk < 0
      return false if !@description.nil? && @description.to_s.size > 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 64
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] serial Value to be assigned
    def serial=(serial)
      if !serial.nil? && serial.to_s.size > 50
        raise ArgumentError.new("invalid value for \"serial\", the character length must be smaller than or equal to 50.")
      end

      @serial = serial
    end

    # Custom attribute writer method with validation
    # @param [Object] vcpus Value to be assigned
    def vcpus=(vcpus)
      if !vcpus.nil? && vcpus >= 10000
        raise ArgumentError.new("invalid value for \"vcpus\", must be smaller than 10000.")
      end

      if !vcpus.nil? && vcpus < 0.01
        raise ArgumentError.new("invalid value for \"vcpus\", must be greater than or equal to 0.01.")
      end

      @vcpus = vcpus
    end

    # Custom attribute writer method with validation
    # @param [Object] memory Value to be assigned
    def memory=(memory)
      if !memory.nil? && memory > 2147483647
        raise ArgumentError.new("invalid value for \"memory\", must be smaller than or equal to 2147483647.")
      end

      if !memory.nil? && memory < 0
        raise ArgumentError.new("invalid value for \"memory\", must be greater than or equal to 0.")
      end

      @memory = memory
    end

    # Custom attribute writer method with validation
    # @param [Object] disk Value to be assigned
    def disk=(disk)
      if !disk.nil? && disk > 2147483647
        raise ArgumentError.new("invalid value for \"disk\", must be smaller than or equal to 2147483647.")
      end

      if !disk.nil? && disk < 0
        raise ArgumentError.new("invalid value for \"disk\", must be greater than or equal to 0.")
      end

      @disk = disk
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display_url == other.display_url &&
        display == other.display &&
        name == other.name &&
        status == other.status &&
        site == other.site &&
        cluster == other.cluster &&
        device == other.device &&
        serial == other.serial &&
        role == other.role &&
        tenant == other.tenant &&
        platform == other.platform &&
        primary_ip == other.primary_ip &&
        primary_ip4 == other.primary_ip4 &&
        primary_ip6 == other.primary_ip6 &&
        vcpus == other.vcpus &&
        memory == other.memory &&
        disk == other.disk &&
        description == other.description &&
        comments == other.comments &&
        config_template == other.config_template &&
        local_context_data == other.local_context_data &&
        tags == other.tags &&
        custom_fields == other.custom_fields &&
        config_context == other.config_context &&
        created == other.created &&
        last_updated == other.last_updated &&
        interface_count == other.interface_count &&
        virtual_disk_count == other.virtual_disk_count
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display_url, display, name, status, site, cluster, device, serial, role, tenant, platform, primary_ip, primary_ip4, primary_ip6, vcpus, memory, disk, description, comments, config_template, local_context_data, tags, custom_fields, config_context, created, last_updated, interface_count, virtual_disk_count].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
