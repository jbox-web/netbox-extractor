# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.2.7 (4.2)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class WirelessLinkRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "interface_a", type: PatchedWritableVirtualCircuitTerminationRequestInterface, nillable: false, emit_null: false)]
    property interface_a : PatchedWritableVirtualCircuitTerminationRequestInterface

    @[JSON::Field(key: "interface_b", type: PatchedWritableVirtualCircuitTerminationRequestInterface, nillable: false, emit_null: false)]
    property interface_b : PatchedWritableVirtualCircuitTerminationRequestInterface

    # Optional properties
    @[JSON::Field(key: "ssid", type: String?, nillable: true, emit_null: false)]
    property ssid : String?

    # * `connected` - Connecté * `planned` - Planifié * `decommissioning` - Démantèlement
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    # * `open` - Ouvert * `wep` - WEP * `wpa-personal` - WPA Personnel (PSK) * `wpa-enterprise` - WPA Entreprise
    @[JSON::Field(key: "auth_type", type: String?, nillable: true, emit_null: false)]
    property auth_type : String?

    # * `auto` - Automatique * `tkip` - TKIP * `aes` - AES
    @[JSON::Field(key: "auth_cipher", type: String?, nillable: true, emit_null: false)]
    property auth_cipher : String?

    @[JSON::Field(key: "auth_psk", type: String?, nillable: true, emit_null: false)]
    property auth_psk : String?

    @[JSON::Field(key: "distance", type: Float64?, nillable: true, emit_null: false)]
    property distance : Float64?

    # * `km` - Kilomètres * `m` - Mètres * `mi` - Miles * `ft` - Pieds
    @[JSON::Field(key: "distance_unit", type: String?, nillable: true, emit_null: false)]
    property distance_unit : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@interface_a : PatchedWritableVirtualCircuitTerminationRequestInterface, @interface_b : PatchedWritableVirtualCircuitTerminationRequestInterface, @ssid : String?, @status : String?, @tenant : ASNRangeRequestTenant?, @auth_type : String?, @auth_cipher : String?, @auth_psk : String?, @distance : Float64?, @distance_unit : String?, @description : String?, @comments : String?, @tags : Array(NestedTagRequest)?, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@ssid.nil? && @ssid.to_s.size > 32
        invalid_properties.push("invalid value for \"ssid\", the character length must be smaller than or equal to 32.")
      end

      if !@auth_psk.nil? && @auth_psk.to_s.size > 64
        invalid_properties.push("invalid value for \"auth_psk\", the character length must be smaller than or equal to 64.")
      end

      if !@distance.nil? && @distance >= 1000000
        invalid_properties.push("invalid value for \"distance\", must be smaller than 1000000.")
      end

      if !@distance.nil? && @distance <= -1000000
        invalid_properties.push("invalid value for \"distance\", must be greater than -1000000.")
      end

      if !@description.nil? && @description.to_s.size > 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@ssid.nil? && @ssid.to_s.size > 32
      status_validator = EnumAttributeValidator.new("String", ["connected", "planned", "decommissioning"])
      return false unless status_validator.valid?(@status)
      auth_type_validator = EnumAttributeValidator.new("String", ["open", "wep", "wpa-personal", "wpa-enterprise", ""])
      return false unless auth_type_validator.valid?(@auth_type)
      auth_cipher_validator = EnumAttributeValidator.new("String", ["auto", "tkip", "aes", ""])
      return false unless auth_cipher_validator.valid?(@auth_cipher)
      return false if !@auth_psk.nil? && @auth_psk.to_s.size > 64
      return false if !@distance.nil? && @distance >= 1000000
      return false if !@distance.nil? && @distance <= -1000000
      distance_unit_validator = EnumAttributeValidator.new("String", ["km", "m", "mi", "ft", ""])
      return false unless distance_unit_validator.valid?(@distance_unit)
      return false if !@description.nil? && @description.to_s.size > 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] ssid Value to be assigned
    def ssid=(ssid)
      if !ssid.nil? && ssid.to_s.size > 32
        raise ArgumentError.new("invalid value for \"ssid\", the character length must be smaller than or equal to 32.")
      end

      @ssid = ssid
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidator.new("String", ["connected", "planned", "decommissioning"])
      unless validator.valid?(status)
        raise ArgumentError.new("invalid value for \"status\", must be one of #{validator.allowable_values}.")
      end
      @status = status
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] auth_type Object to be assigned
    def auth_type=(auth_type)
      validator = EnumAttributeValidator.new("String", ["open", "wep", "wpa-personal", "wpa-enterprise", ""])
      unless validator.valid?(auth_type)
        raise ArgumentError.new("invalid value for \"auth_type\", must be one of #{validator.allowable_values}.")
      end
      @auth_type = auth_type
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] auth_cipher Object to be assigned
    def auth_cipher=(auth_cipher)
      validator = EnumAttributeValidator.new("String", ["auto", "tkip", "aes", ""])
      unless validator.valid?(auth_cipher)
        raise ArgumentError.new("invalid value for \"auth_cipher\", must be one of #{validator.allowable_values}.")
      end
      @auth_cipher = auth_cipher
    end

    # Custom attribute writer method with validation
    # @param [Object] auth_psk Value to be assigned
    def auth_psk=(auth_psk)
      if !auth_psk.nil? && auth_psk.to_s.size > 64
        raise ArgumentError.new("invalid value for \"auth_psk\", the character length must be smaller than or equal to 64.")
      end

      @auth_psk = auth_psk
    end

    # Custom attribute writer method with validation
    # @param [Object] distance Value to be assigned
    def distance=(distance)
      if !distance.nil? && distance >= 1000000
        raise ArgumentError.new("invalid value for \"distance\", must be smaller than 1000000.")
      end

      if !distance.nil? && distance <= -1000000
        raise ArgumentError.new("invalid value for \"distance\", must be greater than -1000000.")
      end

      @distance = distance
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] distance_unit Object to be assigned
    def distance_unit=(distance_unit)
      validator = EnumAttributeValidator.new("String", ["km", "m", "mi", "ft", ""])
      unless validator.valid?(distance_unit)
        raise ArgumentError.new("invalid value for \"distance_unit\", must be one of #{validator.allowable_values}.")
      end
      @distance_unit = distance_unit
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        interface_a == other.interface_a &&
        interface_b == other.interface_b &&
        ssid == other.ssid &&
        status == other.status &&
        tenant == other.tenant &&
        auth_type == other.auth_type &&
        auth_cipher == other.auth_cipher &&
        auth_psk == other.auth_psk &&
        distance == other.distance &&
        distance_unit == other.distance_unit &&
        description == other.description &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [interface_a, interface_b, ssid, status, tenant, auth_type, auth_cipher, auth_psk, distance, distance_unit, description, comments, tags, custom_fields].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
