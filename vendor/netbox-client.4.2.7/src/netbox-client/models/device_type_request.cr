# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.2.7 (4.2)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class DeviceTypeRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "manufacturer", type: BriefDeviceTypeRequestManufacturer, nillable: false, emit_null: false)]
    property manufacturer : BriefDeviceTypeRequestManufacturer

    @[JSON::Field(key: "model", type: String, nillable: false, emit_null: false)]
    property model : String

    @[JSON::Field(key: "slug", type: String, nillable: false, emit_null: false)]
    property slug : String

    # Optional properties
    @[JSON::Field(key: "default_platform", type: DeviceTypeRequestDefaultPlatform?, nillable: true, emit_null: false)]
    property default_platform : DeviceTypeRequestDefaultPlatform?

    # Numéro de pièce discret (facultatif)
    @[JSON::Field(key: "part_number", type: String?, nillable: true, emit_null: false)]
    property part_number : String?

    @[JSON::Field(key: "u_height", type: Float64?, default: 1.0, nillable: true, emit_null: false)]
    property u_height : Float64?

    # Les appareils de ce type sont exclus du calcul de l'utilisation des baies.
    @[JSON::Field(key: "exclude_from_utilization", type: Bool?, nillable: true, emit_null: false)]
    property exclude_from_utilization : Bool?

    # L'appareil consomme à la fois les faces avant et arrière de la baie.
    @[JSON::Field(key: "is_full_depth", type: Bool?, nillable: true, emit_null: false)]
    property is_full_depth : Bool?

    # * `parent` - Parent * `child` - Enfant
    @[JSON::Field(key: "subdevice_role", type: String?, nillable: true, emit_null: false)]
    property subdevice_role : String?

    # * `front-to-rear` - De l'avant vers l'arrière * `rear-to-front` - De l'arrière vers l'avant * `left-to-right` - De gauche à droite * `right-to-left` - De droite à gauche * `side-to-rear` - D'un côté à l'arrière * `rear-to-side` - De l'arrière vers le côté * `bottom-to-top` - De bas en haut * `top-to-bottom` - De haut en bas * `passive` - Passif * `mixed` - Mixte
    @[JSON::Field(key: "airflow", type: String?, nillable: true, emit_null: false)]
    property airflow : String?

    @[JSON::Field(key: "weight", type: Float64?, nillable: true, emit_null: false)]
    property weight : Float64?

    # * `kg` - Kilogrammes * `g` - Grammes * `lb` - Livres * `oz` - Onces
    @[JSON::Field(key: "weight_unit", type: String?, nillable: true, emit_null: false)]
    property weight_unit : String?

    @[JSON::Field(key: "front_image", type: ::File?, nillable: true, emit_null: false)]
    property front_image : ::File?

    @[JSON::Field(key: "rear_image", type: ::File?, nillable: true, emit_null: false)]
    property rear_image : ::File?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@manufacturer : BriefDeviceTypeRequestManufacturer, @model : String, @slug : String, @default_platform : DeviceTypeRequestDefaultPlatform?, @part_number : String?, @u_height : Float64?, @exclude_from_utilization : Bool?, @is_full_depth : Bool?, @subdevice_role : String?, @airflow : String?, @weight : Float64?, @weight_unit : String?, @front_image : ::File?, @rear_image : ::File?, @description : String?, @comments : String?, @tags : Array(NestedTagRequest)?, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @model.to_s.size > 100
        invalid_properties.push("invalid value for \"model\", the character length must be smaller than or equal to 100.")
      end

      if @model.to_s.size < 1
        invalid_properties.push("invalid value for \"model\", the character length must be greater than or equal to 1.")
      end

      if @slug.to_s.size > 100
        invalid_properties.push("invalid value for \"slug\", the character length must be smaller than or equal to 100.")
      end

      if @slug.to_s.size < 1
        invalid_properties.push("invalid value for \"slug\", the character length must be greater than or equal to 1.")
      end

      pattern = Regexp.new(/^[-a-zA-Z0-9_]+$/)
      if @slug !~ pattern
        invalid_properties.push("invalid value for \"slug\", must conform to the pattern #{pattern}.")
      end

      if !@part_number.nil? && @part_number.to_s.size > 50
        invalid_properties.push("invalid value for \"part_number\", the character length must be smaller than or equal to 50.")
      end

      if !@u_height.nil? && @u_height >= 1000
        invalid_properties.push("invalid value for \"u_height\", must be smaller than 1000.")
      end

      if !@u_height.nil? && @u_height < 0
        invalid_properties.push("invalid value for \"u_height\", must be greater than or equal to 0.")
      end

      if !@weight.nil? && @weight >= 1000000
        invalid_properties.push("invalid value for \"weight\", must be smaller than 1000000.")
      end

      if !@weight.nil? && @weight <= -1000000
        invalid_properties.push("invalid value for \"weight\", must be greater than -1000000.")
      end

      if !@description.nil? && @description.to_s.size > 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @model.to_s.size > 100
      return false if @model.to_s.size < 1
      return false if @slug.to_s.size > 100
      return false if @slug.to_s.size < 1
      return false if @slug !~ Regexp.new(/^[-a-zA-Z0-9_]+$/)
      return false if !@part_number.nil? && @part_number.to_s.size > 50
      return false if !@u_height.nil? && @u_height >= 1000
      return false if !@u_height.nil? && @u_height < 0
      subdevice_role_validator = EnumAttributeValidator.new("String", ["parent", "child", ""])
      return false unless subdevice_role_validator.valid?(@subdevice_role)
      airflow_validator = EnumAttributeValidator.new("String", ["front-to-rear", "rear-to-front", "left-to-right", "right-to-left", "side-to-rear", "rear-to-side", "bottom-to-top", "top-to-bottom", "passive", "mixed", ""])
      return false unless airflow_validator.valid?(@airflow)
      return false if !@weight.nil? && @weight >= 1000000
      return false if !@weight.nil? && @weight <= -1000000
      weight_unit_validator = EnumAttributeValidator.new("String", ["kg", "g", "lb", "oz", ""])
      return false unless weight_unit_validator.valid?(@weight_unit)
      return false if !@description.nil? && @description.to_s.size > 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] model Value to be assigned
    def model=(model)
      if model.to_s.size > 100
        raise ArgumentError.new("invalid value for \"model\", the character length must be smaller than or equal to 100.")
      end

      if model.to_s.size < 1
        raise ArgumentError.new("invalid value for \"model\", the character length must be greater than or equal to 1.")
      end

      @model = model
    end

    # Custom attribute writer method with validation
    # @param [Object] slug Value to be assigned
    def slug=(slug)
      if slug.to_s.size > 100
        raise ArgumentError.new("invalid value for \"slug\", the character length must be smaller than or equal to 100.")
      end

      if slug.to_s.size < 1
        raise ArgumentError.new("invalid value for \"slug\", the character length must be greater than or equal to 1.")
      end

      pattern = Regexp.new(/^[-a-zA-Z0-9_]+$/)
      if slug !~ pattern
        raise ArgumentError.new("invalid value for \"slug\", must conform to the pattern #{pattern}.")
      end

      @slug = slug
    end

    # Custom attribute writer method with validation
    # @param [Object] part_number Value to be assigned
    def part_number=(part_number)
      if !part_number.nil? && part_number.to_s.size > 50
        raise ArgumentError.new("invalid value for \"part_number\", the character length must be smaller than or equal to 50.")
      end

      @part_number = part_number
    end

    # Custom attribute writer method with validation
    # @param [Object] u_height Value to be assigned
    def u_height=(u_height)
      if !u_height.nil? && u_height >= 1000
        raise ArgumentError.new("invalid value for \"u_height\", must be smaller than 1000.")
      end

      if !u_height.nil? && u_height < 0
        raise ArgumentError.new("invalid value for \"u_height\", must be greater than or equal to 0.")
      end

      @u_height = u_height
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] subdevice_role Object to be assigned
    def subdevice_role=(subdevice_role)
      validator = EnumAttributeValidator.new("String", ["parent", "child", ""])
      unless validator.valid?(subdevice_role)
        raise ArgumentError.new("invalid value for \"subdevice_role\", must be one of #{validator.allowable_values}.")
      end
      @subdevice_role = subdevice_role
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] airflow Object to be assigned
    def airflow=(airflow)
      validator = EnumAttributeValidator.new("String", ["front-to-rear", "rear-to-front", "left-to-right", "right-to-left", "side-to-rear", "rear-to-side", "bottom-to-top", "top-to-bottom", "passive", "mixed", ""])
      unless validator.valid?(airflow)
        raise ArgumentError.new("invalid value for \"airflow\", must be one of #{validator.allowable_values}.")
      end
      @airflow = airflow
    end

    # Custom attribute writer method with validation
    # @param [Object] weight Value to be assigned
    def weight=(weight)
      if !weight.nil? && weight >= 1000000
        raise ArgumentError.new("invalid value for \"weight\", must be smaller than 1000000.")
      end

      if !weight.nil? && weight <= -1000000
        raise ArgumentError.new("invalid value for \"weight\", must be greater than -1000000.")
      end

      @weight = weight
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] weight_unit Object to be assigned
    def weight_unit=(weight_unit)
      validator = EnumAttributeValidator.new("String", ["kg", "g", "lb", "oz", ""])
      unless validator.valid?(weight_unit)
        raise ArgumentError.new("invalid value for \"weight_unit\", must be one of #{validator.allowable_values}.")
      end
      @weight_unit = weight_unit
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        manufacturer == other.manufacturer &&
        default_platform == other.default_platform &&
        model == other.model &&
        slug == other.slug &&
        part_number == other.part_number &&
        u_height == other.u_height &&
        exclude_from_utilization == other.exclude_from_utilization &&
        is_full_depth == other.is_full_depth &&
        subdevice_role == other.subdevice_role &&
        airflow == other.airflow &&
        weight == other.weight &&
        weight_unit == other.weight_unit &&
        front_image == other.front_image &&
        rear_image == other.rear_image &&
        description == other.description &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [manufacturer, default_platform, model, slug, part_number, u_height, exclude_from_utilization, is_full_depth, subdevice_role, airflow, weight, weight_unit, front_image, rear_image, description, comments, tags, custom_fields].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
