# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.2.7 (4.2)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class RackRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    @[JSON::Field(key: "site", type: DeviceWithConfigContextRequestSite, nillable: false, emit_null: false)]
    property site : DeviceWithConfigContextRequestSite

    # Optional properties
    @[JSON::Field(key: "facility_id", type: String?, nillable: true, emit_null: false)]
    property facility_id : String?

    @[JSON::Field(key: "location", type: DeviceWithConfigContextRequestLocation?, nillable: true, emit_null: false)]
    property location : DeviceWithConfigContextRequestLocation?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    # * `reserved` - Réservé * `available` - Disponible * `planned` - Planifié * `active` - Actif * `deprecated` - Obsolète
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    @[JSON::Field(key: "role", type: PatchedWritableRackRequestRole?, nillable: true, emit_null: false)]
    property role : PatchedWritableRackRequestRole?

    @[JSON::Field(key: "serial", type: String?, nillable: true, emit_null: false)]
    property serial : String?

    # Une étiquette unique utilisée pour identifier cette baie
    @[JSON::Field(key: "asset_tag", type: String?, nillable: true, emit_null: false)]
    property asset_tag : String?

    @[JSON::Field(key: "rack_type", type: PatchedWritableRackRequestRackType?, nillable: true, emit_null: false)]
    property rack_type : PatchedWritableRackRequestRackType?

    # * `2-post-frame` - Châssis à 2 montants * `4-post-frame` - Châssis à 4 montants * `4-post-cabinet` - Armoire à 4 montants * `wall-frame` - Châssis mural * `wall-frame-vertical` - Châssis mural (vertical) * `wall-cabinet` - Armoire murale * `wall-cabinet-vertical` - Armoire murale (verticale)
    @[JSON::Field(key: "form_factor", type: String?, nillable: true, emit_null: false)]
    property form_factor : String?

    # * `10` - 10 inches * `19` - 19 inches * `21` - 21 inches * `23` - 23 inches
    @[JSON::Field(key: "width", type: Int32?, nillable: true, emit_null: false)]
    property width : Int32?

    # Hauteur en U de la baie
    @[JSON::Field(key: "u_height", type: Int32?, nillable: true, emit_null: false)]
    property u_height : Int32?

    # Unité de départ pour baie
    @[JSON::Field(key: "starting_unit", type: Int32?, nillable: true, emit_null: false)]
    property starting_unit : Int32?

    @[JSON::Field(key: "weight", type: Float64?, nillable: true, emit_null: false)]
    property weight : Float64?

    # Capacité de charge maximale de la baie
    @[JSON::Field(key: "max_weight", type: Int32?, nillable: true, emit_null: false)]
    property max_weight : Int32?

    # * `kg` - Kilogrammes * `g` - Grammes * `lb` - Livres * `oz` - Onces
    @[JSON::Field(key: "weight_unit", type: String?, nillable: true, emit_null: false)]
    property weight_unit : String?

    # Les unités sont numérotées de haut en bas
    @[JSON::Field(key: "desc_units", type: Bool?, nillable: true, emit_null: false)]
    property desc_units : Bool?

    # Dimension extérieure de la baie (largeur)
    @[JSON::Field(key: "outer_width", type: Int32?, nillable: true, emit_null: false)]
    property outer_width : Int32?

    # Dimension extérieure de la baie (profondeur)
    @[JSON::Field(key: "outer_depth", type: Int32?, nillable: true, emit_null: false)]
    property outer_depth : Int32?

    # * `mm` - Millimètres * `in` - Pouces
    @[JSON::Field(key: "outer_unit", type: String?, nillable: true, emit_null: false)]
    property outer_unit : String?

    # Profondeur maximale d'un appareil monté, en millimètres. Pour les supports à quatre montants, il s'agit de la distance entre les rails avant et arrière.
    @[JSON::Field(key: "mounting_depth", type: Int32?, nillable: true, emit_null: false)]
    property mounting_depth : Int32?

    # * `front-to-rear` - De l'avant vers l'arrière * `rear-to-front` - De l'arrière vers l'avant
    @[JSON::Field(key: "airflow", type: String?, nillable: true, emit_null: false)]
    property airflow : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@name : String, @site : DeviceWithConfigContextRequestSite, @facility_id : String?, @location : DeviceWithConfigContextRequestLocation?, @tenant : ASNRangeRequestTenant?, @status : String?, @role : PatchedWritableRackRequestRole?, @serial : String?, @asset_tag : String?, @rack_type : PatchedWritableRackRequestRackType?, @form_factor : String?, @width : Int32?, @u_height : Int32?, @starting_unit : Int32?, @weight : Float64?, @max_weight : Int32?, @weight_unit : String?, @desc_units : Bool?, @outer_width : Int32?, @outer_depth : Int32?, @outer_unit : String?, @mounting_depth : Int32?, @airflow : String?, @description : String?, @comments : String?, @tags : Array(NestedTagRequest)?, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.to_s.size > 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if @name.to_s.size < 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if !@facility_id.nil? && @facility_id.to_s.size > 50
        invalid_properties.push("invalid value for \"facility_id\", the character length must be smaller than or equal to 50.")
      end

      if !@serial.nil? && @serial.to_s.size > 50
        invalid_properties.push("invalid value for \"serial\", the character length must be smaller than or equal to 50.")
      end

      if !@asset_tag.nil? && @asset_tag.to_s.size > 50
        invalid_properties.push("invalid value for \"asset_tag\", the character length must be smaller than or equal to 50.")
      end

      if !@u_height.nil? && @u_height > 100
        invalid_properties.push("invalid value for \"u_height\", must be smaller than or equal to 100.")
      end

      if !@u_height.nil? && @u_height < 1
        invalid_properties.push("invalid value for \"u_height\", must be greater than or equal to 1.")
      end

      if !@starting_unit.nil? && @starting_unit > 32767
        invalid_properties.push("invalid value for \"starting_unit\", must be smaller than or equal to 32767.")
      end

      if !@starting_unit.nil? && @starting_unit < 1
        invalid_properties.push("invalid value for \"starting_unit\", must be greater than or equal to 1.")
      end

      if !@weight.nil? && @weight >= 1000000
        invalid_properties.push("invalid value for \"weight\", must be smaller than 1000000.")
      end

      if !@weight.nil? && @weight <= -1000000
        invalid_properties.push("invalid value for \"weight\", must be greater than -1000000.")
      end

      if !@max_weight.nil? && @max_weight > 2147483647
        invalid_properties.push("invalid value for \"max_weight\", must be smaller than or equal to 2147483647.")
      end

      if !@max_weight.nil? && @max_weight < 0
        invalid_properties.push("invalid value for \"max_weight\", must be greater than or equal to 0.")
      end

      if !@outer_width.nil? && @outer_width > 32767
        invalid_properties.push("invalid value for \"outer_width\", must be smaller than or equal to 32767.")
      end

      if !@outer_width.nil? && @outer_width < 0
        invalid_properties.push("invalid value for \"outer_width\", must be greater than or equal to 0.")
      end

      if !@outer_depth.nil? && @outer_depth > 32767
        invalid_properties.push("invalid value for \"outer_depth\", must be smaller than or equal to 32767.")
      end

      if !@outer_depth.nil? && @outer_depth < 0
        invalid_properties.push("invalid value for \"outer_depth\", must be greater than or equal to 0.")
      end

      if !@mounting_depth.nil? && @mounting_depth > 32767
        invalid_properties.push("invalid value for \"mounting_depth\", must be smaller than or equal to 32767.")
      end

      if !@mounting_depth.nil? && @mounting_depth < 0
        invalid_properties.push("invalid value for \"mounting_depth\", must be greater than or equal to 0.")
      end

      if !@description.nil? && @description.to_s.size > 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.to_s.size > 100
      return false if @name.to_s.size < 1
      return false if !@facility_id.nil? && @facility_id.to_s.size > 50
      status_validator = EnumAttributeValidator.new("String", ["reserved", "available", "planned", "active", "deprecated"])
      return false unless status_validator.valid?(@status)
      return false if !@serial.nil? && @serial.to_s.size > 50
      return false if !@asset_tag.nil? && @asset_tag.to_s.size > 50
      form_factor_validator = EnumAttributeValidator.new("String", ["2-post-frame", "4-post-frame", "4-post-cabinet", "wall-frame", "wall-frame-vertical", "wall-cabinet", "wall-cabinet-vertical", ""])
      return false unless form_factor_validator.valid?(@form_factor)
      width_validator = EnumAttributeValidator.new("Int32", ["10", "19", "21", "23"])
      return false unless width_validator.valid?(@width)
      return false if !@u_height.nil? && @u_height > 100
      return false if !@u_height.nil? && @u_height < 1
      return false if !@starting_unit.nil? && @starting_unit > 32767
      return false if !@starting_unit.nil? && @starting_unit < 1
      return false if !@weight.nil? && @weight >= 1000000
      return false if !@weight.nil? && @weight <= -1000000
      return false if !@max_weight.nil? && @max_weight > 2147483647
      return false if !@max_weight.nil? && @max_weight < 0
      weight_unit_validator = EnumAttributeValidator.new("String", ["kg", "g", "lb", "oz", ""])
      return false unless weight_unit_validator.valid?(@weight_unit)
      return false if !@outer_width.nil? && @outer_width > 32767
      return false if !@outer_width.nil? && @outer_width < 0
      return false if !@outer_depth.nil? && @outer_depth > 32767
      return false if !@outer_depth.nil? && @outer_depth < 0
      outer_unit_validator = EnumAttributeValidator.new("String", ["mm", "in", ""])
      return false unless outer_unit_validator.valid?(@outer_unit)
      return false if !@mounting_depth.nil? && @mounting_depth > 32767
      return false if !@mounting_depth.nil? && @mounting_depth < 0
      airflow_validator = EnumAttributeValidator.new("String", ["front-to-rear", "rear-to-front", ""])
      return false unless airflow_validator.valid?(@airflow)
      return false if !@description.nil? && @description.to_s.size > 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] facility_id Value to be assigned
    def facility_id=(facility_id)
      if !facility_id.nil? && facility_id.to_s.size > 50
        raise ArgumentError.new("invalid value for \"facility_id\", the character length must be smaller than or equal to 50.")
      end

      @facility_id = facility_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidator.new("String", ["reserved", "available", "planned", "active", "deprecated"])
      unless validator.valid?(status)
        raise ArgumentError.new("invalid value for \"status\", must be one of #{validator.allowable_values}.")
      end
      @status = status
    end

    # Custom attribute writer method with validation
    # @param [Object] serial Value to be assigned
    def serial=(serial)
      if !serial.nil? && serial.to_s.size > 50
        raise ArgumentError.new("invalid value for \"serial\", the character length must be smaller than or equal to 50.")
      end

      @serial = serial
    end

    # Custom attribute writer method with validation
    # @param [Object] asset_tag Value to be assigned
    def asset_tag=(asset_tag)
      if !asset_tag.nil? && asset_tag.to_s.size > 50
        raise ArgumentError.new("invalid value for \"asset_tag\", the character length must be smaller than or equal to 50.")
      end

      @asset_tag = asset_tag
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] form_factor Object to be assigned
    def form_factor=(form_factor)
      validator = EnumAttributeValidator.new("String", ["2-post-frame", "4-post-frame", "4-post-cabinet", "wall-frame", "wall-frame-vertical", "wall-cabinet", "wall-cabinet-vertical", ""])
      unless validator.valid?(form_factor)
        raise ArgumentError.new("invalid value for \"form_factor\", must be one of #{validator.allowable_values}.")
      end
      @form_factor = form_factor
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] width Object to be assigned
    def width=(width)
      validator = EnumAttributeValidator.new("Int32", ["10", "19", "21", "23"])
      unless validator.valid?(width)
        raise ArgumentError.new("invalid value for \"width\", must be one of #{validator.allowable_values}.")
      end
      @width = width
    end

    # Custom attribute writer method with validation
    # @param [Object] u_height Value to be assigned
    def u_height=(u_height)
      if !u_height.nil? && u_height > 100
        raise ArgumentError.new("invalid value for \"u_height\", must be smaller than or equal to 100.")
      end

      if !u_height.nil? && u_height < 1
        raise ArgumentError.new("invalid value for \"u_height\", must be greater than or equal to 1.")
      end

      @u_height = u_height
    end

    # Custom attribute writer method with validation
    # @param [Object] starting_unit Value to be assigned
    def starting_unit=(starting_unit)
      if !starting_unit.nil? && starting_unit > 32767
        raise ArgumentError.new("invalid value for \"starting_unit\", must be smaller than or equal to 32767.")
      end

      if !starting_unit.nil? && starting_unit < 1
        raise ArgumentError.new("invalid value for \"starting_unit\", must be greater than or equal to 1.")
      end

      @starting_unit = starting_unit
    end

    # Custom attribute writer method with validation
    # @param [Object] weight Value to be assigned
    def weight=(weight)
      if !weight.nil? && weight >= 1000000
        raise ArgumentError.new("invalid value for \"weight\", must be smaller than 1000000.")
      end

      if !weight.nil? && weight <= -1000000
        raise ArgumentError.new("invalid value for \"weight\", must be greater than -1000000.")
      end

      @weight = weight
    end

    # Custom attribute writer method with validation
    # @param [Object] max_weight Value to be assigned
    def max_weight=(max_weight)
      if !max_weight.nil? && max_weight > 2147483647
        raise ArgumentError.new("invalid value for \"max_weight\", must be smaller than or equal to 2147483647.")
      end

      if !max_weight.nil? && max_weight < 0
        raise ArgumentError.new("invalid value for \"max_weight\", must be greater than or equal to 0.")
      end

      @max_weight = max_weight
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] weight_unit Object to be assigned
    def weight_unit=(weight_unit)
      validator = EnumAttributeValidator.new("String", ["kg", "g", "lb", "oz", ""])
      unless validator.valid?(weight_unit)
        raise ArgumentError.new("invalid value for \"weight_unit\", must be one of #{validator.allowable_values}.")
      end
      @weight_unit = weight_unit
    end

    # Custom attribute writer method with validation
    # @param [Object] outer_width Value to be assigned
    def outer_width=(outer_width)
      if !outer_width.nil? && outer_width > 32767
        raise ArgumentError.new("invalid value for \"outer_width\", must be smaller than or equal to 32767.")
      end

      if !outer_width.nil? && outer_width < 0
        raise ArgumentError.new("invalid value for \"outer_width\", must be greater than or equal to 0.")
      end

      @outer_width = outer_width
    end

    # Custom attribute writer method with validation
    # @param [Object] outer_depth Value to be assigned
    def outer_depth=(outer_depth)
      if !outer_depth.nil? && outer_depth > 32767
        raise ArgumentError.new("invalid value for \"outer_depth\", must be smaller than or equal to 32767.")
      end

      if !outer_depth.nil? && outer_depth < 0
        raise ArgumentError.new("invalid value for \"outer_depth\", must be greater than or equal to 0.")
      end

      @outer_depth = outer_depth
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] outer_unit Object to be assigned
    def outer_unit=(outer_unit)
      validator = EnumAttributeValidator.new("String", ["mm", "in", ""])
      unless validator.valid?(outer_unit)
        raise ArgumentError.new("invalid value for \"outer_unit\", must be one of #{validator.allowable_values}.")
      end
      @outer_unit = outer_unit
    end

    # Custom attribute writer method with validation
    # @param [Object] mounting_depth Value to be assigned
    def mounting_depth=(mounting_depth)
      if !mounting_depth.nil? && mounting_depth > 32767
        raise ArgumentError.new("invalid value for \"mounting_depth\", must be smaller than or equal to 32767.")
      end

      if !mounting_depth.nil? && mounting_depth < 0
        raise ArgumentError.new("invalid value for \"mounting_depth\", must be greater than or equal to 0.")
      end

      @mounting_depth = mounting_depth
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] airflow Object to be assigned
    def airflow=(airflow)
      validator = EnumAttributeValidator.new("String", ["front-to-rear", "rear-to-front", ""])
      unless validator.valid?(airflow)
        raise ArgumentError.new("invalid value for \"airflow\", must be one of #{validator.allowable_values}.")
      end
      @airflow = airflow
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        name == other.name &&
        facility_id == other.facility_id &&
        site == other.site &&
        location == other.location &&
        tenant == other.tenant &&
        status == other.status &&
        role == other.role &&
        serial == other.serial &&
        asset_tag == other.asset_tag &&
        rack_type == other.rack_type &&
        form_factor == other.form_factor &&
        width == other.width &&
        u_height == other.u_height &&
        starting_unit == other.starting_unit &&
        weight == other.weight &&
        max_weight == other.max_weight &&
        weight_unit == other.weight_unit &&
        desc_units == other.desc_units &&
        outer_width == other.outer_width &&
        outer_depth == other.outer_depth &&
        outer_unit == other.outer_unit &&
        mounting_depth == other.mounting_depth &&
        airflow == other.airflow &&
        description == other.description &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [name, facility_id, site, location, tenant, status, role, serial, asset_tag, rack_type, form_factor, width, u_height, starting_unit, weight, max_weight, weight_unit, desc_units, outer_width, outer_depth, outer_unit, mounting_depth, airflow, description, comments, tags, custom_fields].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
