# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.2.7 (4.2)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class PatchedWritableDeviceWithConfigContextRequest
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "name", type: String?, nillable: true, emit_null: false)]
    property name : String?

    @[JSON::Field(key: "device_type", type: DeviceBayTemplateRequestDeviceType?, nillable: true, emit_null: false)]
    property device_type : DeviceBayTemplateRequestDeviceType?

    @[JSON::Field(key: "role", type: DeviceWithConfigContextRequestRole?, nillable: true, emit_null: false)]
    property role : DeviceWithConfigContextRequestRole?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    @[JSON::Field(key: "platform", type: DeviceTypeRequestDefaultPlatform?, nillable: true, emit_null: false)]
    property platform : DeviceTypeRequestDefaultPlatform?

    # Numéro de série du châssis attribué par le fabricant
    @[JSON::Field(key: "serial", type: String?, nillable: true, emit_null: false)]
    property serial : String?

    # Un tag unique utilisé pour identifier cet appareil
    @[JSON::Field(key: "asset_tag", type: String?, nillable: true, emit_null: false)]
    property asset_tag : String?

    @[JSON::Field(key: "site", type: DeviceWithConfigContextRequestSite?, nillable: true, emit_null: false)]
    property site : DeviceWithConfigContextRequestSite?

    @[JSON::Field(key: "location", type: DeviceWithConfigContextRequestLocation?, nillable: true, emit_null: false)]
    property location : DeviceWithConfigContextRequestLocation?

    @[JSON::Field(key: "rack", type: DeviceWithConfigContextRequestRack?, nillable: true, emit_null: false)]
    property rack : DeviceWithConfigContextRequestRack?

    @[JSON::Field(key: "position", type: Float64?, nillable: true, emit_null: false)]
    property position : Float64?

    # * `front` - Avant * `rear` - Arrière
    @[JSON::Field(key: "face", type: String?, nillable: true, emit_null: false)]
    property face : String?

    # Coordonnées GPS au format décimal (xx.yyyyyy)
    @[JSON::Field(key: "latitude", type: Float64?, nillable: true, emit_null: false)]
    property latitude : Float64?

    # Coordonnées GPS au format décimal (xx.yyyyyy)
    @[JSON::Field(key: "longitude", type: Float64?, nillable: true, emit_null: false)]
    property longitude : Float64?

    # * `offline` - Hors ligne * `active` - Actif * `planned` - Planifié * `staged` - Mis en scène * `failed` - Échoué * `inventory` - Inventaire * `decommissioning` - Démantèlement
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    # * `front-to-rear` - De l'avant vers l'arrière * `rear-to-front` - De l'arrière vers l'avant * `left-to-right` - De gauche à droite * `right-to-left` - De droite à gauche * `side-to-rear` - D'un côté à l'arrière * `rear-to-side` - De l'arrière vers le côté * `bottom-to-top` - De bas en haut * `top-to-bottom` - De haut en bas * `passive` - Passif * `mixed` - Mixte
    @[JSON::Field(key: "airflow", type: String?, nillable: true, emit_null: false)]
    property airflow : String?

    @[JSON::Field(key: "primary_ip4", type: DeviceWithConfigContextRequestPrimaryIp4?, nillable: true, emit_null: false)]
    property primary_ip4 : DeviceWithConfigContextRequestPrimaryIp4?

    @[JSON::Field(key: "primary_ip6", type: DeviceWithConfigContextRequestPrimaryIp4?, nillable: true, emit_null: false)]
    property primary_ip6 : DeviceWithConfigContextRequestPrimaryIp4?

    @[JSON::Field(key: "oob_ip", type: DeviceWithConfigContextRequestPrimaryIp4?, nillable: true, emit_null: false)]
    property oob_ip : DeviceWithConfigContextRequestPrimaryIp4?

    @[JSON::Field(key: "cluster", type: DeviceWithConfigContextRequestCluster?, nillable: true, emit_null: false)]
    property cluster : DeviceWithConfigContextRequestCluster?

    @[JSON::Field(key: "virtual_chassis", type: DeviceWithConfigContextRequestVirtualChassis?, nillable: true, emit_null: false)]
    property virtual_chassis : DeviceWithConfigContextRequestVirtualChassis?

    @[JSON::Field(key: "vc_position", type: Int32?, nillable: true, emit_null: false)]
    property vc_position : Int32?

    # Priorité d'élection principale du châssis virtuel
    @[JSON::Field(key: "vc_priority", type: Int32?, nillable: true, emit_null: false)]
    property vc_priority : Int32?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "config_template", type: DeviceRoleRequestConfigTemplate?, nillable: true, emit_null: false)]
    property config_template : DeviceRoleRequestConfigTemplate?

    # Les données du contexte de configuration local ont priorité sur les contextes source dans le contexte de configuration final rendu
    @[JSON::Field(key: "local_context_data", type: Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time?, nillable: true, emit_null: false)]
    property local_context_data : Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@name : String?, @device_type : DeviceBayTemplateRequestDeviceType?, @role : DeviceWithConfigContextRequestRole?, @tenant : ASNRangeRequestTenant?, @platform : DeviceTypeRequestDefaultPlatform?, @serial : String?, @asset_tag : String?, @site : DeviceWithConfigContextRequestSite?, @location : DeviceWithConfigContextRequestLocation?, @rack : DeviceWithConfigContextRequestRack?, @position : Float64?, @face : String?, @latitude : Float64?, @longitude : Float64?, @status : String?, @airflow : String?, @primary_ip4 : DeviceWithConfigContextRequestPrimaryIp4?, @primary_ip6 : DeviceWithConfigContextRequestPrimaryIp4?, @oob_ip : DeviceWithConfigContextRequestPrimaryIp4?, @cluster : DeviceWithConfigContextRequestCluster?, @virtual_chassis : DeviceWithConfigContextRequestVirtualChassis?, @vc_position : Int32?, @vc_priority : Int32?, @description : String?, @comments : String?, @config_template : DeviceRoleRequestConfigTemplate?, @local_context_data : Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time?, @tags : Array(NestedTagRequest)?, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@name.nil? && @name.to_s.size > 64
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if !@serial.nil? && @serial.to_s.size > 50
        invalid_properties.push("invalid value for \"serial\", the character length must be smaller than or equal to 50.")
      end

      if !@asset_tag.nil? && @asset_tag.to_s.size > 50
        invalid_properties.push("invalid value for \"asset_tag\", the character length must be smaller than or equal to 50.")
      end

      if !@position.nil? && @position >= 1000
        invalid_properties.push("invalid value for \"position\", must be smaller than 1000.")
      end

      if !@position.nil? && @position < 0.5
        invalid_properties.push("invalid value for \"position\", must be greater than or equal to 0.5.")
      end

      if !@latitude.nil? && @latitude >= 100
        invalid_properties.push("invalid value for \"latitude\", must be smaller than 100.")
      end

      if !@latitude.nil? && @latitude <= -100
        invalid_properties.push("invalid value for \"latitude\", must be greater than -100.")
      end

      if !@longitude.nil? && @longitude >= 1000
        invalid_properties.push("invalid value for \"longitude\", must be smaller than 1000.")
      end

      if !@longitude.nil? && @longitude <= -1000
        invalid_properties.push("invalid value for \"longitude\", must be greater than -1000.")
      end

      if !@vc_position.nil? && @vc_position > 255
        invalid_properties.push("invalid value for \"vc_position\", must be smaller than or equal to 255.")
      end

      if !@vc_position.nil? && @vc_position < 0
        invalid_properties.push("invalid value for \"vc_position\", must be greater than or equal to 0.")
      end

      if !@vc_priority.nil? && @vc_priority > 255
        invalid_properties.push("invalid value for \"vc_priority\", must be smaller than or equal to 255.")
      end

      if !@vc_priority.nil? && @vc_priority < 0
        invalid_properties.push("invalid value for \"vc_priority\", must be greater than or equal to 0.")
      end

      if !@description.nil? && @description.to_s.size > 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@name.nil? && @name.to_s.size > 64
      return false if !@serial.nil? && @serial.to_s.size > 50
      return false if !@asset_tag.nil? && @asset_tag.to_s.size > 50
      return false if !@position.nil? && @position >= 1000
      return false if !@position.nil? && @position < 0.5
      face_validator = EnumAttributeValidator.new("String", ["front", "rear", ""])
      return false unless face_validator.valid?(@face)
      return false if !@latitude.nil? && @latitude >= 100
      return false if !@latitude.nil? && @latitude <= -100
      return false if !@longitude.nil? && @longitude >= 1000
      return false if !@longitude.nil? && @longitude <= -1000
      status_validator = EnumAttributeValidator.new("String", ["offline", "active", "planned", "staged", "failed", "inventory", "decommissioning"])
      return false unless status_validator.valid?(@status)
      airflow_validator = EnumAttributeValidator.new("String", ["front-to-rear", "rear-to-front", "left-to-right", "right-to-left", "side-to-rear", "rear-to-side", "bottom-to-top", "top-to-bottom", "passive", "mixed", ""])
      return false unless airflow_validator.valid?(@airflow)
      return false if !@vc_position.nil? && @vc_position > 255
      return false if !@vc_position.nil? && @vc_position < 0
      return false if !@vc_priority.nil? && @vc_priority > 255
      return false if !@vc_priority.nil? && @vc_priority < 0
      return false if !@description.nil? && @description.to_s.size > 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if !name.nil? && name.to_s.size > 64
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] serial Value to be assigned
    def serial=(serial)
      if !serial.nil? && serial.to_s.size > 50
        raise ArgumentError.new("invalid value for \"serial\", the character length must be smaller than or equal to 50.")
      end

      @serial = serial
    end

    # Custom attribute writer method with validation
    # @param [Object] asset_tag Value to be assigned
    def asset_tag=(asset_tag)
      if !asset_tag.nil? && asset_tag.to_s.size > 50
        raise ArgumentError.new("invalid value for \"asset_tag\", the character length must be smaller than or equal to 50.")
      end

      @asset_tag = asset_tag
    end

    # Custom attribute writer method with validation
    # @param [Object] position Value to be assigned
    def position=(position)
      if !position.nil? && position >= 1000
        raise ArgumentError.new("invalid value for \"position\", must be smaller than 1000.")
      end

      if !position.nil? && position < 0.5
        raise ArgumentError.new("invalid value for \"position\", must be greater than or equal to 0.5.")
      end

      @position = position
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] face Object to be assigned
    def face=(face)
      validator = EnumAttributeValidator.new("String", ["front", "rear", ""])
      unless validator.valid?(face)
        raise ArgumentError.new("invalid value for \"face\", must be one of #{validator.allowable_values}.")
      end
      @face = face
    end

    # Custom attribute writer method with validation
    # @param [Object] latitude Value to be assigned
    def latitude=(latitude)
      if !latitude.nil? && latitude >= 100
        raise ArgumentError.new("invalid value for \"latitude\", must be smaller than 100.")
      end

      if !latitude.nil? && latitude <= -100
        raise ArgumentError.new("invalid value for \"latitude\", must be greater than -100.")
      end

      @latitude = latitude
    end

    # Custom attribute writer method with validation
    # @param [Object] longitude Value to be assigned
    def longitude=(longitude)
      if !longitude.nil? && longitude >= 1000
        raise ArgumentError.new("invalid value for \"longitude\", must be smaller than 1000.")
      end

      if !longitude.nil? && longitude <= -1000
        raise ArgumentError.new("invalid value for \"longitude\", must be greater than -1000.")
      end

      @longitude = longitude
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidator.new("String", ["offline", "active", "planned", "staged", "failed", "inventory", "decommissioning"])
      unless validator.valid?(status)
        raise ArgumentError.new("invalid value for \"status\", must be one of #{validator.allowable_values}.")
      end
      @status = status
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] airflow Object to be assigned
    def airflow=(airflow)
      validator = EnumAttributeValidator.new("String", ["front-to-rear", "rear-to-front", "left-to-right", "right-to-left", "side-to-rear", "rear-to-side", "bottom-to-top", "top-to-bottom", "passive", "mixed", ""])
      unless validator.valid?(airflow)
        raise ArgumentError.new("invalid value for \"airflow\", must be one of #{validator.allowable_values}.")
      end
      @airflow = airflow
    end

    # Custom attribute writer method with validation
    # @param [Object] vc_position Value to be assigned
    def vc_position=(vc_position)
      if !vc_position.nil? && vc_position > 255
        raise ArgumentError.new("invalid value for \"vc_position\", must be smaller than or equal to 255.")
      end

      if !vc_position.nil? && vc_position < 0
        raise ArgumentError.new("invalid value for \"vc_position\", must be greater than or equal to 0.")
      end

      @vc_position = vc_position
    end

    # Custom attribute writer method with validation
    # @param [Object] vc_priority Value to be assigned
    def vc_priority=(vc_priority)
      if !vc_priority.nil? && vc_priority > 255
        raise ArgumentError.new("invalid value for \"vc_priority\", must be smaller than or equal to 255.")
      end

      if !vc_priority.nil? && vc_priority < 0
        raise ArgumentError.new("invalid value for \"vc_priority\", must be greater than or equal to 0.")
      end

      @vc_priority = vc_priority
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        name == other.name &&
        device_type == other.device_type &&
        role == other.role &&
        tenant == other.tenant &&
        platform == other.platform &&
        serial == other.serial &&
        asset_tag == other.asset_tag &&
        site == other.site &&
        location == other.location &&
        rack == other.rack &&
        position == other.position &&
        face == other.face &&
        latitude == other.latitude &&
        longitude == other.longitude &&
        status == other.status &&
        airflow == other.airflow &&
        primary_ip4 == other.primary_ip4 &&
        primary_ip6 == other.primary_ip6 &&
        oob_ip == other.oob_ip &&
        cluster == other.cluster &&
        virtual_chassis == other.virtual_chassis &&
        vc_position == other.vc_position &&
        vc_priority == other.vc_priority &&
        description == other.description &&
        comments == other.comments &&
        config_template == other.config_template &&
        local_context_data == other.local_context_data &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [name, device_type, role, tenant, platform, serial, asset_tag, site, location, rack, position, face, latitude, longitude, status, airflow, primary_ip4, primary_ip6, oob_ip, cluster, virtual_chassis, vc_position, vc_priority, description, comments, config_template, local_context_data, tags, custom_fields].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
