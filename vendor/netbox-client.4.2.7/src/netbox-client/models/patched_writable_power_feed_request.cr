# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.2.7 (4.2)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class PatchedWritablePowerFeedRequest
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "power_panel", type: PatchedWritablePowerFeedRequestPowerPanel?, nillable: true, emit_null: false)]
    property power_panel : PatchedWritablePowerFeedRequestPowerPanel?

    @[JSON::Field(key: "rack", type: DeviceWithConfigContextRequestRack?, nillable: true, emit_null: false)]
    property rack : DeviceWithConfigContextRequestRack?

    @[JSON::Field(key: "name", type: String?, nillable: true, emit_null: false)]
    property name : String?

    # * `offline` - Hors ligne * `active` - Actif * `planned` - Planifié * `failed` - Échoué
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    # * `primary` - Primaire * `redundant` - Redondant
    @[JSON::Field(key: "type", type: String?, nillable: true, emit_null: false)]
    property _type : String?

    # * `ac` - AC * `dc` - DC
    @[JSON::Field(key: "supply", type: String?, nillable: true, emit_null: false)]
    property supply : String?

    # * `single-phase` - Monophasé * `three-phase` - Triphasé
    @[JSON::Field(key: "phase", type: String?, nillable: true, emit_null: false)]
    property phase : String?

    @[JSON::Field(key: "voltage", type: Int32?, nillable: true, emit_null: false)]
    property voltage : Int32?

    @[JSON::Field(key: "amperage", type: Int32?, nillable: true, emit_null: false)]
    property amperage : Int32?

    # Tirage maximum autorisé (pourcentage)
    @[JSON::Field(key: "max_utilization", type: Int32?, nillable: true, emit_null: false)]
    property max_utilization : Int32?

    # Traitez comme si un câble était connecté
    @[JSON::Field(key: "mark_connected", type: Bool?, nillable: true, emit_null: false)]
    property mark_connected : Bool?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@power_panel : PatchedWritablePowerFeedRequestPowerPanel?, @rack : DeviceWithConfigContextRequestRack?, @name : String?, @status : String?, @_type : String?, @supply : String?, @phase : String?, @voltage : Int32?, @amperage : Int32?, @max_utilization : Int32?, @mark_connected : Bool?, @description : String?, @tenant : ASNRangeRequestTenant?, @comments : String?, @tags : Array(NestedTagRequest)?, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@name.nil? && @name.to_s.size > 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !@name.nil? && @name.to_s.size < 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if !@voltage.nil? && @voltage > 32767
        invalid_properties.push("invalid value for \"voltage\", must be smaller than or equal to 32767.")
      end

      if !@voltage.nil? && @voltage < -32768
        invalid_properties.push("invalid value for \"voltage\", must be greater than or equal to -32768.")
      end

      if !@amperage.nil? && @amperage > 32767
        invalid_properties.push("invalid value for \"amperage\", must be smaller than or equal to 32767.")
      end

      if !@amperage.nil? && @amperage < 1
        invalid_properties.push("invalid value for \"amperage\", must be greater than or equal to 1.")
      end

      if !@max_utilization.nil? && @max_utilization > 100
        invalid_properties.push("invalid value for \"max_utilization\", must be smaller than or equal to 100.")
      end

      if !@max_utilization.nil? && @max_utilization < 1
        invalid_properties.push("invalid value for \"max_utilization\", must be greater than or equal to 1.")
      end

      if !@description.nil? && @description.to_s.size > 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@name.nil? && @name.to_s.size > 100
      return false if !@name.nil? && @name.to_s.size < 1
      status_validator = EnumAttributeValidator.new("String", ["offline", "active", "planned", "failed"])
      return false unless status_validator.valid?(@status)
      _type_validator = EnumAttributeValidator.new("String", ["primary", "redundant"])
      return false unless _type_validator.valid?(@_type)
      supply_validator = EnumAttributeValidator.new("String", ["ac", "dc"])
      return false unless supply_validator.valid?(@supply)
      phase_validator = EnumAttributeValidator.new("String", ["single-phase", "three-phase"])
      return false unless phase_validator.valid?(@phase)
      return false if !@voltage.nil? && @voltage > 32767
      return false if !@voltage.nil? && @voltage < -32768
      return false if !@amperage.nil? && @amperage > 32767
      return false if !@amperage.nil? && @amperage < 1
      return false if !@max_utilization.nil? && @max_utilization > 100
      return false if !@max_utilization.nil? && @max_utilization < 1
      return false if !@description.nil? && @description.to_s.size > 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if !name.nil? && name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !name.nil? && name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidator.new("String", ["offline", "active", "planned", "failed"])
      unless validator.valid?(status)
        raise ArgumentError.new("invalid value for \"status\", must be one of #{validator.allowable_values}.")
      end
      @status = status
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] _type Object to be assigned
    def _type=(_type)
      validator = EnumAttributeValidator.new("String", ["primary", "redundant"])
      unless validator.valid?(_type)
        raise ArgumentError.new("invalid value for \"_type\", must be one of #{validator.allowable_values}.")
      end
      @_type = _type
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] supply Object to be assigned
    def supply=(supply)
      validator = EnumAttributeValidator.new("String", ["ac", "dc"])
      unless validator.valid?(supply)
        raise ArgumentError.new("invalid value for \"supply\", must be one of #{validator.allowable_values}.")
      end
      @supply = supply
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] phase Object to be assigned
    def phase=(phase)
      validator = EnumAttributeValidator.new("String", ["single-phase", "three-phase"])
      unless validator.valid?(phase)
        raise ArgumentError.new("invalid value for \"phase\", must be one of #{validator.allowable_values}.")
      end
      @phase = phase
    end

    # Custom attribute writer method with validation
    # @param [Object] voltage Value to be assigned
    def voltage=(voltage)
      if !voltage.nil? && voltage > 32767
        raise ArgumentError.new("invalid value for \"voltage\", must be smaller than or equal to 32767.")
      end

      if !voltage.nil? && voltage < -32768
        raise ArgumentError.new("invalid value for \"voltage\", must be greater than or equal to -32768.")
      end

      @voltage = voltage
    end

    # Custom attribute writer method with validation
    # @param [Object] amperage Value to be assigned
    def amperage=(amperage)
      if !amperage.nil? && amperage > 32767
        raise ArgumentError.new("invalid value for \"amperage\", must be smaller than or equal to 32767.")
      end

      if !amperage.nil? && amperage < 1
        raise ArgumentError.new("invalid value for \"amperage\", must be greater than or equal to 1.")
      end

      @amperage = amperage
    end

    # Custom attribute writer method with validation
    # @param [Object] max_utilization Value to be assigned
    def max_utilization=(max_utilization)
      if !max_utilization.nil? && max_utilization > 100
        raise ArgumentError.new("invalid value for \"max_utilization\", must be smaller than or equal to 100.")
      end

      if !max_utilization.nil? && max_utilization < 1
        raise ArgumentError.new("invalid value for \"max_utilization\", must be greater than or equal to 1.")
      end

      @max_utilization = max_utilization
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        power_panel == other.power_panel &&
        rack == other.rack &&
        name == other.name &&
        status == other.status &&
        _type == other._type &&
        supply == other.supply &&
        phase == other.phase &&
        voltage == other.voltage &&
        amperage == other.amperage &&
        max_utilization == other.max_utilization &&
        mark_connected == other.mark_connected &&
        description == other.description &&
        tenant == other.tenant &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [power_panel, rack, name, status, _type, supply, phase, voltage, amperage, max_utilization, mark_connected, description, tenant, comments, tags, custom_fields].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
