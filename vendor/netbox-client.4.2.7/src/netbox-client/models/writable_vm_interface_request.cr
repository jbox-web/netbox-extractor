# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.2.7 (4.2)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.18.0-SNAPSHOT
#

require "big"
require "json"
require "yaml"
require "time"

module NetboxClient
  # Adds support for custom fields and tags.
  class WritableVMInterfaceRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "virtual_machine", type: PatchedVirtualDiskRequestVirtualMachine, nillable: false, emit_null: false)]
    property virtual_machine : PatchedVirtualDiskRequestVirtualMachine

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # Optional properties
    @[JSON::Field(key: "enabled", type: Bool?, nillable: true, emit_null: false)]
    property enabled : Bool?

    @[JSON::Field(key: "parent", type: Int32?, nillable: true, emit_null: false)]
    property parent : Int32?

    @[JSON::Field(key: "bridge", type: Int32?, nillable: true, emit_null: false)]
    property bridge : Int32?

    @[JSON::Field(key: "mtu", type: Int32?, nillable: true, emit_null: false)]
    property mtu : Int32?

    @[JSON::Field(key: "primary_mac_address", type: InterfaceRequestPrimaryMacAddress?, nillable: true, emit_null: false)]
    property primary_mac_address : InterfaceRequestPrimaryMacAddress?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # Stratégie de marquage IEEE 802.1Q  * `access` - Accès * `tagged` - Tagué * `tagged-all` - Tagué (Tous) * `q-in-q` - Qin-Q (802.1ad)
    @[JSON::Field(key: "mode", type: String?, nillable: true, emit_null: false)]
    property mode : String?

    @[JSON::Field(key: "untagged_vlan", type: InterfaceRequestUntaggedVlan?, nillable: true, emit_null: false)]
    property untagged_vlan : InterfaceRequestUntaggedVlan?

    @[JSON::Field(key: "tagged_vlans", type: Array(Int32)?, nillable: true, emit_null: false)]
    property tagged_vlans : Array(Int32)?

    @[JSON::Field(key: "qinq_svlan", type: InterfaceRequestUntaggedVlan?, nillable: true, emit_null: false)]
    property qinq_svlan : InterfaceRequestUntaggedVlan?

    @[JSON::Field(key: "vlan_translation_policy", type: InterfaceRequestVlanTranslationPolicy?, nillable: true, emit_null: false)]
    property vlan_translation_policy : InterfaceRequestVlanTranslationPolicy?

    @[JSON::Field(key: "vrf", type: IPAddressRequestVrf?, nillable: true, emit_null: false)]
    property vrf : IPAddressRequestVrf?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@virtual_machine : PatchedVirtualDiskRequestVirtualMachine, @name : String, @enabled : Bool?, @parent : Int32?, @bridge : Int32?, @mtu : Int32?, @primary_mac_address : InterfaceRequestPrimaryMacAddress?, @description : String?, @mode : String?, @untagged_vlan : InterfaceRequestUntaggedVlan?, @tagged_vlans : Array(Int32)?, @qinq_svlan : InterfaceRequestUntaggedVlan?, @vlan_translation_policy : InterfaceRequestVlanTranslationPolicy?, @vrf : IPAddressRequestVrf?, @tags : Array(NestedTagRequest)?, @custom_fields : Hash(String, Nil | String | Bool | Int32 | Int64 | Float32 | Float64 | Time)?)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.to_s.size > 64
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if @name.to_s.size < 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if !@mtu.nil? && @mtu > 65536
        invalid_properties.push("invalid value for \"mtu\", must be smaller than or equal to 65536.")
      end

      if !@mtu.nil? && @mtu < 1
        invalid_properties.push("invalid value for \"mtu\", must be greater than or equal to 1.")
      end

      if !@description.nil? && @description.to_s.size > 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.to_s.size > 64
      return false if @name.to_s.size < 1
      return false if !@mtu.nil? && @mtu > 65536
      return false if !@mtu.nil? && @mtu < 1
      return false if !@description.nil? && @description.to_s.size > 200
      mode_validator = EnumAttributeValidator.new("String", ["access", "tagged", "tagged-all", "q-in-q", ""])
      return false unless mode_validator.valid?(@mode)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 64
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] mtu Value to be assigned
    def mtu=(mtu)
      if !mtu.nil? && mtu > 65536
        raise ArgumentError.new("invalid value for \"mtu\", must be smaller than or equal to 65536.")
      end

      if !mtu.nil? && mtu < 1
        raise ArgumentError.new("invalid value for \"mtu\", must be greater than or equal to 1.")
      end

      @mtu = mtu
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] mode Object to be assigned
    def mode=(mode)
      validator = EnumAttributeValidator.new("String", ["access", "tagged", "tagged-all", "q-in-q", ""])
      unless validator.valid?(mode)
        raise ArgumentError.new("invalid value for \"mode\", must be one of #{validator.allowable_values}.")
      end
      @mode = mode
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        virtual_machine == other.virtual_machine &&
        name == other.name &&
        enabled == other.enabled &&
        parent == other.parent &&
        bridge == other.bridge &&
        mtu == other.mtu &&
        primary_mac_address == other.primary_mac_address &&
        description == other.description &&
        mode == other.mode &&
        untagged_vlan == other.untagged_vlan &&
        tagged_vlans == other.tagged_vlans &&
        qinq_svlan == other.qinq_svlan &&
        vlan_translation_policy == other.vlan_translation_policy &&
        vrf == other.vrf &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [virtual_machine, name, enabled, parent, bridge, mtu, primary_mac_address, description, mode, untagged_vlan, tagged_vlans, qinq_svlan, vlan_translation_policy, vrf, tags, custom_fields].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = NetboxClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
