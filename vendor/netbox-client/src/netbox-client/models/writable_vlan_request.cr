# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class WritableVLANRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Numeric VLAN ID (1-4094)
    @[JSON::Field(key: "vid", type: Int32, nillable: false, emit_null: false)]
    property vid : Int32

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # Optional properties
    @[JSON::Field(key: "site", type: PatchedWritableVLANRequestSite?, nillable: true, emit_null: false)]
    property site : PatchedWritableVLANRequestSite?

    @[JSON::Field(key: "group", type: PatchedWritableVLANRequestGroup?, nillable: true, emit_null: false)]
    property group : PatchedWritableVLANRequestGroup?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    # Operational status of this VLAN  * `active` - Active * `reserved` - Reserved * `deprecated` - Deprecated
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    @[JSON::Field(key: "role", type: IPRangeRequestRole?, nillable: true, emit_null: false)]
    property role : IPRangeRequestRole?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # Customer/service VLAN designation (for Q-in-Q/IEEE 802.1ad)  * `svlan` - Service * `cvlan` - Customer
    @[JSON::Field(key: "qinq_role", type: String?, nillable: true, emit_null: false)]
    property qinq_role : String?

    @[JSON::Field(key: "qinq_svlan", type: Int32?, nillable: true, emit_null: false)]
    property qinq_svlan : Int32?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["active", "reserved", "deprecated"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForQinqRole < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "qinq_role"
        @allowable_values = ["svlan", "cvlan", ""].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@vid : Int32, @name : String, @site : PatchedWritableVLANRequestSite? = nil, @group : PatchedWritableVLANRequestGroup? = nil, @tenant : ASNRangeRequestTenant? = nil, @status : String? = nil, @role : IPRangeRequestRole? = nil, @description : String? = nil, @qinq_role : String? = nil, @qinq_svlan : Int32? = nil, @comments : String? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @vid.try &.> 4094
        invalid_properties.push("invalid value for \"vid\", must be smaller than or equal to 4094.")
      end

      if @vid.try &.< 1
        invalid_properties.push("invalid value for \"vid\", must be greater than or equal to 1.")
      end

      if @name.try &.to_s.try &.size.try &.> 64
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      qinq_role_validator = EnumAttributeValidatorForQinqRole.new
      if !qinq_role_validator.valid?(@qinq_role)
        message = qinq_role_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @vid.try &.> 4094
      return false if @vid.try &.< 1
      return false if @name.try &.to_s.try &.size.try &.> 64
      return false if @name.try &.to_s.try &.size.try &.< 1
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      qinq_role_validator = EnumAttributeValidatorForQinqRole.new
      return false unless qinq_role_validator.valid?(@qinq_role)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] vid Value to be assigned
    def vid=(vid)
      if vid > 4094
        raise ArgumentError.new("invalid value for \"vid\", must be smaller than or equal to 4094.")
      end

      if vid < 1
        raise ArgumentError.new("invalid value for \"vid\", must be greater than or equal to 1.")
      end

      @vid = vid
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 64
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] qinq_role Object to be assigned
    def qinq_role=(qinq_role)
      validator = EnumAttributeValidatorForQinqRole.new
      unless validator.valid?(qinq_role)
        raise ArgumentError.new(validator.message)
      end
      @qinq_role = qinq_role
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        site == other.site &&
        group == other.group &&
        vid == other.vid &&
        name == other.name &&
        tenant == other.tenant &&
        status == other.status &&
        role == other.role &&
        description == other.description &&
        qinq_role == other.qinq_role &&
        qinq_svlan == other.qinq_svlan &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [site, group, vid, name, tenant, status, role, description, qinq_role, qinq_svlan, comments, tags, custom_fields].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["site"] = _to_h(site)
      hash["group"] = _to_h(group)
      hash["vid"] = _to_h(vid)
      hash["name"] = _to_h(name)
      hash["tenant"] = _to_h(tenant)
      hash["status"] = _to_h(status)
      hash["role"] = _to_h(role)
      hash["description"] = _to_h(description)
      hash["qinq_role"] = _to_h(qinq_role)
      hash["qinq_svlan"] = _to_h(qinq_svlan)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
