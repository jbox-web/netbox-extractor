# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class WritableCableRequest
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    # * `cat3` - CAT3 * `cat5` - CAT5 * `cat5e` - CAT5e * `cat6` - CAT6 * `cat6a` - CAT6a * `cat7` - CAT7 * `cat7a` - CAT7a * `cat8` - CAT8 * `mrj21-trunk` - MRJ21 Trunk * `dac-active` - Direct Attach Copper (Active) * `dac-passive` - Direct Attach Copper (Passive) * `coaxial` - Coaxial * `rg-6` - RG-6 * `rg-8` - RG-8 * `rg-11` - RG-11 * `rg-59` - RG-59 * `rg-62` - RG-62 * `rg-213` - RG-213 * `lmr-100` - LMR-100 * `lmr-200` - LMR-200 * `lmr-400` - LMR-400 * `mmf` - Multimode Fiber * `mmf-om1` - Multimode Fiber (OM1) * `mmf-om2` - Multimode Fiber (OM2) * `mmf-om3` - Multimode Fiber (OM3) * `mmf-om4` - Multimode Fiber (OM4) * `mmf-om5` - Multimode Fiber (OM5) * `smf` - Single-mode Fiber * `smf-os1` - Single-mode Fiber (OS1) * `smf-os2` - Single-mode Fiber (OS2) * `aoc` - Active Optical Cabling (AOC) * `power` - Power * `usb` - USB
    @[JSON::Field(key: "type", type: String?, nillable: true, emit_null: false)]
    property _type : String?

    @[JSON::Field(key: "a_terminations", type: Array(GenericObjectRequest)?, nillable: true, emit_null: false)]
    property a_terminations : Array(GenericObjectRequest)?

    @[JSON::Field(key: "b_terminations", type: Array(GenericObjectRequest)?, nillable: true, emit_null: false)]
    property b_terminations : Array(GenericObjectRequest)?

    # * `connected` - Connected * `planned` - Planned * `decommissioning` - Decommissioning
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    @[JSON::Field(key: "label", type: String?, nillable: true, emit_null: false)]
    property label : String?

    @[JSON::Field(key: "color", type: String?, nillable: true, emit_null: false)]
    property color : String?

    @[JSON::Field(key: "length", type: Float64?, nillable: true, emit_null: false)]
    property length : Float64?

    # * `km` - Kilometers * `m` - Meters * `cm` - Centimeters * `mi` - Miles * `ft` - Feet * `in` - Inches
    @[JSON::Field(key: "length_unit", type: String?, nillable: true, emit_null: false)]
    property length_unit : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForType < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "_type"
        @allowable_values = ["cat3", "cat5", "cat5e", "cat6", "cat6a", "cat7", "cat7a", "cat8", "mrj21-trunk", "dac-active", "dac-passive", "coaxial", "rg-6", "rg-8", "rg-11", "rg-59", "rg-62", "rg-213", "lmr-100", "lmr-200", "lmr-400", "mmf", "mmf-om1", "mmf-om2", "mmf-om3", "mmf-om4", "mmf-om5", "smf", "smf-os1", "smf-os2", "aoc", "power", "usb", ""].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["connected", "planned", "decommissioning"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForLengthUnit < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "length_unit"
        @allowable_values = ["km", "m", "cm", "mi", "ft", "in", ""].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@_type : String? = nil, @a_terminations : Array(GenericObjectRequest)? = nil, @b_terminations : Array(GenericObjectRequest)? = nil, @status : String? = nil, @tenant : ASNRangeRequestTenant? = nil, @label : String? = nil, @color : String? = nil, @length : Float64? = nil, @length_unit : String? = nil, @description : String? = nil, @comments : String? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      _type_validator = EnumAttributeValidatorForType.new
      if !_type_validator.valid?(@_type)
        message = _type_validator.message
        invalid_properties.push(message)
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      if !@label.nil? && @label.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"label\", the character length must be smaller than or equal to 100.")
      end

      if !@color.nil? && @color.try &.to_s.try &.size.try &.> 6
        invalid_properties.push("invalid value for \"color\", the character length must be smaller than or equal to 6.")
      end

      pattern = /^[0-9a-f]{6}$/
      if !@color.nil? && @color.try &.!~ pattern
        invalid_properties.push("invalid value for \"color\", must conform to the pattern #{pattern}.")
      end

      if !@length.nil? && @length.try &.>= 1000000
        invalid_properties.push("invalid value for \"length\", must be smaller than 1000000.")
      end

      if !@length.nil? && @length.try &.<= -1000000
        invalid_properties.push("invalid value for \"length\", must be greater than -1000000.")
      end

      length_unit_validator = EnumAttributeValidatorForLengthUnit.new
      if !length_unit_validator.valid?(@length_unit)
        message = length_unit_validator.message
        invalid_properties.push(message)
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      _type_validator = EnumAttributeValidatorForType.new
      return false unless _type_validator.valid?(@_type)
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      return false if !@label.nil? && @label.try &.to_s.try &.size.try &.> 100
      return false if !@color.nil? && @color.try &.to_s.try &.size.try &.> 6
      return false if !@color.nil? && @color.try &.!~ /^[0-9a-f]{6}$/
      return false if !@length.nil? && @length.try &.>= 1000000
      return false if !@length.nil? && @length.try &.<= -1000000
      length_unit_validator = EnumAttributeValidatorForLengthUnit.new
      return false unless length_unit_validator.valid?(@length_unit)
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] _type Object to be assigned
    def _type=(_type)
      validator = EnumAttributeValidatorForType.new
      unless validator.valid?(_type)
        raise ArgumentError.new(validator.message)
      end
      @_type = _type
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method with validation
    # @param [Object] label Value to be assigned
    def label=(label)
      if !label.nil? && label.to_s.size > 100
        raise ArgumentError.new("invalid value for \"label\", the character length must be smaller than or equal to 100.")
      end

      @label = label
    end

    # Custom attribute writer method with validation
    # @param [Object] color Value to be assigned
    def color=(color)
      if !color.nil? && color.to_s.size > 6
        raise ArgumentError.new("invalid value for \"color\", the character length must be smaller than or equal to 6.")
      end

      pattern = /^[0-9a-f]{6}$/
      if !color.nil? && color !~ pattern
        raise ArgumentError.new("invalid value for \"color\", must conform to the pattern #{pattern}.")
      end

      @color = color
    end

    # Custom attribute writer method with validation
    # @param [Object] length Value to be assigned
    def length=(length)
      if !length.nil? && length >= 1000000
        raise ArgumentError.new("invalid value for \"length\", must be smaller than 1000000.")
      end

      if !length.nil? && length <= -1000000
        raise ArgumentError.new("invalid value for \"length\", must be greater than -1000000.")
      end

      @length = length
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] length_unit Object to be assigned
    def length_unit=(length_unit)
      validator = EnumAttributeValidatorForLengthUnit.new
      unless validator.valid?(length_unit)
        raise ArgumentError.new(validator.message)
      end
      @length_unit = length_unit
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        _type == other._type &&
        a_terminations == other.a_terminations &&
        b_terminations == other.b_terminations &&
        status == other.status &&
        tenant == other.tenant &&
        label == other.label &&
        color == other.color &&
        length == other.length &&
        length_unit == other.length_unit &&
        description == other.description &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [_type, a_terminations, b_terminations, status, tenant, label, color, length, length_unit, description, comments, tags, custom_fields].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["type"] = _to_h(_type)
      hash["a_terminations"] = _to_h(a_terminations)
      hash["b_terminations"] = _to_h(b_terminations)
      hash["status"] = _to_h(status)
      hash["tenant"] = _to_h(tenant)
      hash["label"] = _to_h(label)
      hash["color"] = _to_h(color)
      hash["length"] = _to_h(length)
      hash["length_unit"] = _to_h(length_unit)
      hash["description"] = _to_h(description)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
