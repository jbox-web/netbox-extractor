# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Extends the built-in ModelSerializer to enforce calling full_clean() on a copy of the associated instance during validation. (DRF does not do this by default; see https://github.com/encode/django-rest-framework/issues/3144)
  class WritableFrontPortTemplateRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # {module} is accepted as a substitution for the module bay position when attached to a module type.
    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # * `8p8c` - 8P8C * `8p6c` - 8P6C * `8p4c` - 8P4C * `8p2c` - 8P2C * `6p6c` - 6P6C * `6p4c` - 6P4C * `6p2c` - 6P2C * `4p4c` - 4P4C * `4p2c` - 4P2C * `gg45` - GG45 * `tera-4p` - TERA 4P * `tera-2p` - TERA 2P * `tera-1p` - TERA 1P * `110-punch` - 110 Punch * `bnc` - BNC * `f` - F Connector * `n` - N Connector * `mrj21` - MRJ21 * `fc` - FC * `fc-pc` - FC/PC * `fc-upc` - FC/UPC * `fc-apc` - FC/APC * `lc` - LC * `lc-pc` - LC/PC * `lc-upc` - LC/UPC * `lc-apc` - LC/APC * `lsh` - LSH * `lsh-pc` - LSH/PC * `lsh-upc` - LSH/UPC * `lsh-apc` - LSH/APC * `lx5` - LX.5 * `lx5-pc` - LX.5/PC * `lx5-upc` - LX.5/UPC * `lx5-apc` - LX.5/APC * `mpo` - MPO * `mtrj` - MTRJ * `sc` - SC * `sc-pc` - SC/PC * `sc-upc` - SC/UPC * `sc-apc` - SC/APC * `st` - ST * `cs` - CS * `sn` - SN * `sma-905` - SMA 905 * `sma-906` - SMA 906 * `urm-p2` - URM-P2 * `urm-p4` - URM-P4 * `urm-p8` - URM-P8 * `splice` - Splice * `usb-a` - USB Type A * `usb-b` - USB Type B * `usb-c` - USB Type C * `usb-mini-a` - USB Mini A * `usb-mini-b` - USB Mini B * `usb-micro-a` - USB Micro A * `usb-micro-b` - USB Micro B * `usb-micro-ab` - USB Micro AB * `other` - Other
    @[JSON::Field(key: "type", type: String, nillable: false, emit_null: false)]
    property _type : String

    @[JSON::Field(key: "rear_port", type: FrontPortTemplateRequestRearPort, nillable: false, emit_null: false)]
    property rear_port : FrontPortTemplateRequestRearPort

    # Optional properties
    @[JSON::Field(key: "device_type", type: ConsolePortTemplateRequestDeviceType?, nillable: true, emit_null: false)]
    property device_type : ConsolePortTemplateRequestDeviceType?

    @[JSON::Field(key: "module_type", type: ConsolePortTemplateRequestModuleType?, nillable: true, emit_null: false)]
    property module_type : ConsolePortTemplateRequestModuleType?

    # Physical label
    @[JSON::Field(key: "label", type: String?, nillable: true, emit_null: false)]
    property label : String?

    @[JSON::Field(key: "color", type: String?, nillable: true, emit_null: false)]
    property color : String?

    @[JSON::Field(key: "rear_port_position", type: Int32?, nillable: true, emit_null: false)]
    property rear_port_position : Int32?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForType < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "_type"
        @allowable_values = ["8p8c", "8p6c", "8p4c", "8p2c", "6p6c", "6p4c", "6p2c", "4p4c", "4p2c", "gg45", "tera-4p", "tera-2p", "tera-1p", "110-punch", "bnc", "f", "n", "mrj21", "fc", "fc-pc", "fc-upc", "fc-apc", "lc", "lc-pc", "lc-upc", "lc-apc", "lsh", "lsh-pc", "lsh-upc", "lsh-apc", "lx5", "lx5-pc", "lx5-upc", "lx5-apc", "mpo", "mtrj", "sc", "sc-pc", "sc-upc", "sc-apc", "st", "cs", "sn", "sma-905", "sma-906", "urm-p2", "urm-p4", "urm-p8", "splice", "usb-a", "usb-b", "usb-c", "usb-mini-a", "usb-mini-b", "usb-micro-a", "usb-micro-b", "usb-micro-ab", "other"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@name : String, @_type : String, @rear_port : FrontPortTemplateRequestRearPort, @device_type : ConsolePortTemplateRequestDeviceType? = nil, @module_type : ConsolePortTemplateRequestModuleType? = nil, @label : String? = nil, @color : String? = nil, @rear_port_position : Int32? = nil, @description : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 64
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if !@label.nil? && @label.try &.to_s.try &.size.try &.> 64
        invalid_properties.push("invalid value for \"label\", the character length must be smaller than or equal to 64.")
      end

      _type_validator = EnumAttributeValidatorForType.new
      if !_type_validator.valid?(@_type)
        message = _type_validator.message
        invalid_properties.push(message)
      end

      if !@color.nil? && @color.try &.to_s.try &.size.try &.> 6
        invalid_properties.push("invalid value for \"color\", the character length must be smaller than or equal to 6.")
      end

      pattern = /^[0-9a-f]{6}$/
      if !@color.nil? && @color.try &.!~ pattern
        invalid_properties.push("invalid value for \"color\", must conform to the pattern #{pattern}.")
      end

      if !@rear_port_position.nil? && @rear_port_position.try &.> 1024
        invalid_properties.push("invalid value for \"rear_port_position\", must be smaller than or equal to 1024.")
      end

      if !@rear_port_position.nil? && @rear_port_position.try &.< 1
        invalid_properties.push("invalid value for \"rear_port_position\", must be greater than or equal to 1.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 64
      return false if @name.try &.to_s.try &.size.try &.< 1
      return false if !@label.nil? && @label.try &.to_s.try &.size.try &.> 64
      _type_validator = EnumAttributeValidatorForType.new
      return false unless _type_validator.valid?(@_type)
      return false if !@color.nil? && @color.try &.to_s.try &.size.try &.> 6
      return false if !@color.nil? && @color.try &.!~ /^[0-9a-f]{6}$/
      return false if !@rear_port_position.nil? && @rear_port_position.try &.> 1024
      return false if !@rear_port_position.nil? && @rear_port_position.try &.< 1
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 64
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] label Value to be assigned
    def label=(label)
      if !label.nil? && label.to_s.size > 64
        raise ArgumentError.new("invalid value for \"label\", the character length must be smaller than or equal to 64.")
      end

      @label = label
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] _type Object to be assigned
    def _type=(_type)
      validator = EnumAttributeValidatorForType.new
      unless validator.valid?(_type)
        raise ArgumentError.new(validator.message)
      end
      @_type = _type
    end

    # Custom attribute writer method with validation
    # @param [Object] color Value to be assigned
    def color=(color)
      if !color.nil? && color.to_s.size > 6
        raise ArgumentError.new("invalid value for \"color\", the character length must be smaller than or equal to 6.")
      end

      pattern = /^[0-9a-f]{6}$/
      if !color.nil? && color !~ pattern
        raise ArgumentError.new("invalid value for \"color\", must conform to the pattern #{pattern}.")
      end

      @color = color
    end

    # Custom attribute writer method with validation
    # @param [Object] rear_port_position Value to be assigned
    def rear_port_position=(rear_port_position)
      if !rear_port_position.nil? && rear_port_position > 1024
        raise ArgumentError.new("invalid value for \"rear_port_position\", must be smaller than or equal to 1024.")
      end

      if !rear_port_position.nil? && rear_port_position < 1
        raise ArgumentError.new("invalid value for \"rear_port_position\", must be greater than or equal to 1.")
      end

      @rear_port_position = rear_port_position
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        device_type == other.device_type &&
        module_type == other.module_type &&
        name == other.name &&
        label == other.label &&
        _type == other._type &&
        color == other.color &&
        rear_port == other.rear_port &&
        rear_port_position == other.rear_port_position &&
        description == other.description
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [device_type, module_type, name, label, _type, color, rear_port, rear_port_position, description].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["device_type"] = _to_h(device_type)
      hash["module_type"] = _to_h(module_type)
      hash["name"] = _to_h(name)
      hash["label"] = _to_h(label)
      hash["type"] = _to_h(_type)
      hash["color"] = _to_h(color)
      hash["rear_port"] = _to_h(rear_port)
      hash["rear_port_position"] = _to_h(rear_port_position)
      hash["description"] = _to_h(description)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
