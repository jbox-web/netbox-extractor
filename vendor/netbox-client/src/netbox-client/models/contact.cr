# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class Contact
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display_url", type: String, nillable: false, emit_null: false)]
    property display_url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    @[JSON::Field(key: "created", type: Time, nillable: false, emit_null: false)]
    property created : Time

    @[JSON::Field(key: "last_updated", type: Time, nillable: false, emit_null: false)]
    property last_updated : Time

    # Optional properties
    @[JSON::Field(key: "groups", type: Array(ContactGroup)?, nillable: true, emit_null: false)]
    property groups : Array(ContactGroup)?

    @[JSON::Field(key: "title", type: String?, nillable: true, emit_null: false)]
    property title : String?

    @[JSON::Field(key: "phone", type: String?, nillable: true, emit_null: false)]
    property phone : String?

    @[JSON::Field(key: "email", type: String?, nillable: true, emit_null: false)]
    property email : String?

    @[JSON::Field(key: "address", type: String?, nillable: true, emit_null: false)]
    property address : String?

    @[JSON::Field(key: "link", type: String?, nillable: true, emit_null: false)]
    property link : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTag)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTag)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display_url : String, @display : String, @name : String, @created : Time, @last_updated : Time, @groups : Array(ContactGroup)? = nil, @title : String? = nil, @phone : String? = nil, @email : String? = nil, @address : String? = nil, @link : String? = nil, @description : String? = nil, @comments : String? = nil, @tags : Array(NestedTag)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !@title.nil? && @title.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"title\", the character length must be smaller than or equal to 100.")
      end

      if !@phone.nil? && @phone.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"phone\", the character length must be smaller than or equal to 50.")
      end

      if !@email.nil? && @email.try &.to_s.try &.size.try &.> 254
        invalid_properties.push("invalid value for \"email\", the character length must be smaller than or equal to 254.")
      end

      if !@address.nil? && @address.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"address\", the character length must be smaller than or equal to 200.")
      end

      if !@link.nil? && @link.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"link\", the character length must be smaller than or equal to 200.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 100
      return false if !@title.nil? && @title.try &.to_s.try &.size.try &.> 100
      return false if !@phone.nil? && @phone.try &.to_s.try &.size.try &.> 50
      return false if !@email.nil? && @email.try &.to_s.try &.size.try &.> 254
      return false if !@address.nil? && @address.try &.to_s.try &.size.try &.> 200
      return false if !@link.nil? && @link.try &.to_s.try &.size.try &.> 200
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] title Value to be assigned
    def title=(title)
      if !title.nil? && title.to_s.size > 100
        raise ArgumentError.new("invalid value for \"title\", the character length must be smaller than or equal to 100.")
      end

      @title = title
    end

    # Custom attribute writer method with validation
    # @param [Object] phone Value to be assigned
    def phone=(phone)
      if !phone.nil? && phone.to_s.size > 50
        raise ArgumentError.new("invalid value for \"phone\", the character length must be smaller than or equal to 50.")
      end

      @phone = phone
    end

    # Custom attribute writer method with validation
    # @param [Object] email Value to be assigned
    def email=(email)
      if !email.nil? && email.to_s.size > 254
        raise ArgumentError.new("invalid value for \"email\", the character length must be smaller than or equal to 254.")
      end

      @email = email
    end

    # Custom attribute writer method with validation
    # @param [Object] address Value to be assigned
    def address=(address)
      if !address.nil? && address.to_s.size > 200
        raise ArgumentError.new("invalid value for \"address\", the character length must be smaller than or equal to 200.")
      end

      @address = address
    end

    # Custom attribute writer method with validation
    # @param [Object] link Value to be assigned
    def link=(link)
      if !link.nil? && link.to_s.size > 200
        raise ArgumentError.new("invalid value for \"link\", the character length must be smaller than or equal to 200.")
      end

      @link = link
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display_url == other.display_url &&
        display == other.display &&
        groups == other.groups &&
        name == other.name &&
        title == other.title &&
        phone == other.phone &&
        email == other.email &&
        address == other.address &&
        link == other.link &&
        description == other.description &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields &&
        created == other.created &&
        last_updated == other.last_updated
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display_url, display, groups, name, title, phone, email, address, link, description, comments, tags, custom_fields, created, last_updated].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["url"] = _to_h(url)
      hash["display_url"] = _to_h(display_url)
      hash["display"] = _to_h(display)
      hash["groups"] = _to_h(groups)
      hash["name"] = _to_h(name)
      hash["title"] = _to_h(title)
      hash["phone"] = _to_h(phone)
      hash["email"] = _to_h(email)
      hash["address"] = _to_h(address)
      hash["link"] = _to_h(link)
      hash["description"] = _to_h(description)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash["created"] = _to_h(created)
      hash["last_updated"] = _to_h(last_updated)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
