# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Extends the built-in ModelSerializer to enforce calling full_clean() on a copy of the associated instance during validation. (DRF does not do this by default; see https://github.com/encode/django-rest-framework/issues/3144)
  class TokenRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "user", type: BookmarkRequestUser, nillable: false, emit_null: false)]
    property user : BookmarkRequestUser

    # Optional properties
    @[JSON::Field(key: "expires", type: Time?, nillable: true, emit_null: false)]
    property expires : Time?

    @[JSON::Field(key: "last_used", type: Time?, nillable: true, emit_null: false)]
    property last_used : Time?

    @[JSON::Field(key: "key", type: String?, nillable: true, emit_null: false)]
    property key : String?

    # Permit create/update/delete operations using this key
    @[JSON::Field(key: "write_enabled", type: Bool?, nillable: true, emit_null: false)]
    property write_enabled : Bool?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@user : BookmarkRequestUser, @expires : Time? = nil, @last_used : Time? = nil, @key : String? = nil, @write_enabled : Bool? = nil, @description : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@key.nil? && @key.try &.to_s.try &.size.try &.> 40
        invalid_properties.push("invalid value for \"key\", the character length must be smaller than or equal to 40.")
      end

      if !@key.nil? && @key.try &.to_s.try &.size.try &.< 40
        invalid_properties.push("invalid value for \"key\", the character length must be greater than or equal to 40.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@key.nil? && @key.try &.to_s.try &.size.try &.> 40
      return false if !@key.nil? && @key.try &.to_s.try &.size.try &.< 40
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] key Value to be assigned
    def key=(key)
      if !key.nil? && key.to_s.size > 40
        raise ArgumentError.new("invalid value for \"key\", the character length must be smaller than or equal to 40.")
      end

      if !key.nil? && key.to_s.size < 40
        raise ArgumentError.new("invalid value for \"key\", the character length must be greater than or equal to 40.")
      end

      @key = key
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        user == other.user &&
        expires == other.expires &&
        last_used == other.last_used &&
        key == other.key &&
        write_enabled == other.write_enabled &&
        description == other.description
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [user, expires, last_used, key, write_enabled, description].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["user"] = _to_h(user)
      hash["expires"] = _to_h(expires)
      hash["last_used"] = _to_h(last_used)
      hash["key"] = _to_h(key)
      hash["write_enabled"] = _to_h(write_enabled)
      hash["description"] = _to_h(description)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
