# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class PatchedWritableIPRangeRequest
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "start_address", type: String?, nillable: true, emit_null: false)]
    property start_address : String?

    @[JSON::Field(key: "end_address", type: String?, nillable: true, emit_null: false)]
    property end_address : String?

    @[JSON::Field(key: "vrf", type: IPAddressRequestVrf?, nillable: true, emit_null: false)]
    property vrf : IPAddressRequestVrf?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    # Operational status of this range  * `active` - Active * `reserved` - Reserved * `deprecated` - Deprecated
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    @[JSON::Field(key: "role", type: IPRangeRequestRole?, nillable: true, emit_null: false)]
    property role : IPRangeRequestRole?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    # Prevent the creation of IP addresses within this range
    @[JSON::Field(key: "mark_populated", type: Bool?, nillable: true, emit_null: false)]
    property mark_populated : Bool?

    # Report space as fully utilized
    @[JSON::Field(key: "mark_utilized", type: Bool?, nillable: true, emit_null: false)]
    property mark_utilized : Bool?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["active", "reserved", "deprecated"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@start_address : String? = nil, @end_address : String? = nil, @vrf : IPAddressRequestVrf? = nil, @tenant : ASNRangeRequestTenant? = nil, @status : String? = nil, @role : IPRangeRequestRole? = nil, @description : String? = nil, @comments : String? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil, @mark_populated : Bool? = nil, @mark_utilized : Bool? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@start_address.nil? && @start_address.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"start_address\", the character length must be greater than or equal to 1.")
      end

      if !@end_address.nil? && @end_address.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"end_address\", the character length must be greater than or equal to 1.")
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@start_address.nil? && @start_address.try &.to_s.try &.size.try &.< 1
      return false if !@end_address.nil? && @end_address.try &.to_s.try &.size.try &.< 1
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] start_address Value to be assigned
    def start_address=(start_address)
      if !start_address.nil? && start_address.to_s.size < 1
        raise ArgumentError.new("invalid value for \"start_address\", the character length must be greater than or equal to 1.")
      end

      @start_address = start_address
    end

    # Custom attribute writer method with validation
    # @param [Object] end_address Value to be assigned
    def end_address=(end_address)
      if !end_address.nil? && end_address.to_s.size < 1
        raise ArgumentError.new("invalid value for \"end_address\", the character length must be greater than or equal to 1.")
      end

      @end_address = end_address
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        start_address == other.start_address &&
        end_address == other.end_address &&
        vrf == other.vrf &&
        tenant == other.tenant &&
        status == other.status &&
        role == other.role &&
        description == other.description &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields &&
        mark_populated == other.mark_populated &&
        mark_utilized == other.mark_utilized
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [start_address, end_address, vrf, tenant, status, role, description, comments, tags, custom_fields, mark_populated, mark_utilized].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["start_address"] = _to_h(start_address)
      hash["end_address"] = _to_h(end_address)
      hash["vrf"] = _to_h(vrf)
      hash["tenant"] = _to_h(tenant)
      hash["status"] = _to_h(status)
      hash["role"] = _to_h(role)
      hash["description"] = _to_h(description)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash["mark_populated"] = _to_h(mark_populated)
      hash["mark_utilized"] = _to_h(mark_utilized)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
