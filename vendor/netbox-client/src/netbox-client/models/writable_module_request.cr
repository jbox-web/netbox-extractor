# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class WritableModuleRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "device", type: BriefInterfaceRequestDevice, nillable: false, emit_null: false)]
    property device : BriefInterfaceRequestDevice

    @[JSON::Field(key: "module_bay", type: Int32, nillable: false, emit_null: false)]
    property module_bay : Int32

    @[JSON::Field(key: "module_type", type: ModuleRequestModuleType, nillable: false, emit_null: false)]
    property module_type : ModuleRequestModuleType

    # Optional properties
    # * `offline` - Offline * `active` - Active * `planned` - Planned * `staged` - Staged * `failed` - Failed * `decommissioning` - Decommissioning
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    @[JSON::Field(key: "serial", type: String?, nillable: true, emit_null: false)]
    property serial : String?

    # A unique tag used to identify this device
    @[JSON::Field(key: "asset_tag", type: String?, nillable: true, emit_null: false)]
    property asset_tag : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["offline", "active", "planned", "staged", "failed", "decommissioning"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@device : BriefInterfaceRequestDevice, @module_bay : Int32, @module_type : ModuleRequestModuleType, @status : String? = nil, @serial : String? = nil, @asset_tag : String? = nil, @description : String? = nil, @comments : String? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      if !@serial.nil? && @serial.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"serial\", the character length must be smaller than or equal to 50.")
      end

      if !@asset_tag.nil? && @asset_tag.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"asset_tag\", the character length must be smaller than or equal to 50.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      return false if !@serial.nil? && @serial.try &.to_s.try &.size.try &.> 50
      return false if !@asset_tag.nil? && @asset_tag.try &.to_s.try &.size.try &.> 50
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method with validation
    # @param [Object] serial Value to be assigned
    def serial=(serial)
      if !serial.nil? && serial.to_s.size > 50
        raise ArgumentError.new("invalid value for \"serial\", the character length must be smaller than or equal to 50.")
      end

      @serial = serial
    end

    # Custom attribute writer method with validation
    # @param [Object] asset_tag Value to be assigned
    def asset_tag=(asset_tag)
      if !asset_tag.nil? && asset_tag.to_s.size > 50
        raise ArgumentError.new("invalid value for \"asset_tag\", the character length must be smaller than or equal to 50.")
      end

      @asset_tag = asset_tag
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        device == other.device &&
        module_bay == other.module_bay &&
        module_type == other.module_type &&
        status == other.status &&
        serial == other.serial &&
        asset_tag == other.asset_tag &&
        description == other.description &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [device, module_bay, module_type, status, serial, asset_tag, description, comments, tags, custom_fields].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["device"] = _to_h(device)
      hash["module_bay"] = _to_h(module_bay)
      hash["module_type"] = _to_h(module_type)
      hash["status"] = _to_h(status)
      hash["serial"] = _to_h(serial)
      hash["asset_tag"] = _to_h(asset_tag)
      hash["description"] = _to_h(description)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
