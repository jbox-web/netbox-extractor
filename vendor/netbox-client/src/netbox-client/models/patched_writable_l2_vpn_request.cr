# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class PatchedWritableL2VPNRequest
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "identifier", type: Int64?, nillable: true, emit_null: false)]
    property identifier : Int64?

    @[JSON::Field(key: "name", type: String?, nillable: true, emit_null: false)]
    property name : String?

    @[JSON::Field(key: "slug", type: String?, nillable: true, emit_null: false)]
    property slug : String?

    # * `vpws` - VPWS * `vpls` - VPLS * `vxlan` - VXLAN * `vxlan-evpn` - VXLAN-EVPN * `mpls-evpn` - MPLS EVPN * `pbb-evpn` - PBB EVPN * `evpn-vpws` - EVPN VPWS * `epl` - EPL * `evpl` - EVPL * `ep-lan` - Ethernet Private LAN * `evp-lan` - Ethernet Virtual Private LAN * `ep-tree` - Ethernet Private Tree * `evp-tree` - Ethernet Virtual Private Tree * `spb` - SPB
    @[JSON::Field(key: "type", type: String?, nillable: true, emit_null: false)]
    property _type : String?

    # * `active` - Active * `planned` - Planned * `decommissioning` - Decommissioning
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    @[JSON::Field(key: "import_targets", type: Array(Int32)?, nillable: true, emit_null: false)]
    property import_targets : Array(Int32)?

    @[JSON::Field(key: "export_targets", type: Array(Int32)?, nillable: true, emit_null: false)]
    property export_targets : Array(Int32)?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForType < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "_type"
        @allowable_values = ["vpws", "vpls", "vxlan", "vxlan-evpn", "mpls-evpn", "pbb-evpn", "evpn-vpws", "epl", "evpl", "ep-lan", "evp-lan", "ep-tree", "evp-tree", "spb"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["active", "planned", "decommissioning"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@identifier : Int64? = nil, @name : String? = nil, @slug : String? = nil, @_type : String? = nil, @status : String? = nil, @import_targets : Array(Int32)? = nil, @export_targets : Array(Int32)? = nil, @description : String? = nil, @comments : String? = nil, @tenant : ASNRangeRequestTenant? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@identifier.nil? && @identifier.try &.> 9223372036854775807
        invalid_properties.push("invalid value for \"identifier\", must be smaller than or equal to 9223372036854775807.")
      end

      if !@identifier.nil? && @identifier.try &.< -9223372036854775808
        invalid_properties.push("invalid value for \"identifier\", must be greater than or equal to -9223372036854775808.")
      end

      if !@name.nil? && @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !@name.nil? && @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if !@slug.nil? && @slug.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"slug\", the character length must be smaller than or equal to 100.")
      end

      if !@slug.nil? && @slug.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"slug\", the character length must be greater than or equal to 1.")
      end

      pattern = /^[-a-zA-Z0-9_]+$/
      if !@slug.nil? && @slug.try &.!~ pattern
        invalid_properties.push("invalid value for \"slug\", must conform to the pattern #{pattern}.")
      end

      _type_validator = EnumAttributeValidatorForType.new
      if !_type_validator.valid?(@_type)
        message = _type_validator.message
        invalid_properties.push(message)
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@identifier.nil? && @identifier.try &.> 9223372036854775807
      return false if !@identifier.nil? && @identifier.try &.< -9223372036854775808
      return false if !@name.nil? && @name.try &.to_s.try &.size.try &.> 100
      return false if !@name.nil? && @name.try &.to_s.try &.size.try &.< 1
      return false if !@slug.nil? && @slug.try &.to_s.try &.size.try &.> 100
      return false if !@slug.nil? && @slug.try &.to_s.try &.size.try &.< 1
      return false if !@slug.nil? && @slug.try &.!~ /^[-a-zA-Z0-9_]+$/
      _type_validator = EnumAttributeValidatorForType.new
      return false unless _type_validator.valid?(@_type)
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] identifier Value to be assigned
    def identifier=(identifier)
      if !identifier.nil? && identifier > 9223372036854775807
        raise ArgumentError.new("invalid value for \"identifier\", must be smaller than or equal to 9223372036854775807.")
      end

      if !identifier.nil? && identifier < -9223372036854775808
        raise ArgumentError.new("invalid value for \"identifier\", must be greater than or equal to -9223372036854775808.")
      end

      @identifier = identifier
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if !name.nil? && name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !name.nil? && name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] slug Value to be assigned
    def slug=(slug)
      if !slug.nil? && slug.to_s.size > 100
        raise ArgumentError.new("invalid value for \"slug\", the character length must be smaller than or equal to 100.")
      end

      if !slug.nil? && slug.to_s.size < 1
        raise ArgumentError.new("invalid value for \"slug\", the character length must be greater than or equal to 1.")
      end

      pattern = /^[-a-zA-Z0-9_]+$/
      if !slug.nil? && slug !~ pattern
        raise ArgumentError.new("invalid value for \"slug\", must conform to the pattern #{pattern}.")
      end

      @slug = slug
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] _type Object to be assigned
    def _type=(_type)
      validator = EnumAttributeValidatorForType.new
      unless validator.valid?(_type)
        raise ArgumentError.new(validator.message)
      end
      @_type = _type
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        identifier == other.identifier &&
        name == other.name &&
        slug == other.slug &&
        _type == other._type &&
        status == other.status &&
        import_targets == other.import_targets &&
        export_targets == other.export_targets &&
        description == other.description &&
        comments == other.comments &&
        tenant == other.tenant &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [identifier, name, slug, _type, status, import_targets, export_targets, description, comments, tenant, tags, custom_fields].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["identifier"] = _to_h(identifier)
      hash["name"] = _to_h(name)
      hash["slug"] = _to_h(slug)
      hash["type"] = _to_h(_type)
      hash["status"] = _to_h(status)
      hash["import_targets"] = _to_h(import_targets)
      hash["export_targets"] = _to_h(export_targets)
      hash["description"] = _to_h(description)
      hash["comments"] = _to_h(comments)
      hash["tenant"] = _to_h(tenant)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
