# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class WritableWirelessLANRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "ssid", type: String, nillable: false, emit_null: false)]
    property ssid : String

    # Optional properties
    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "group", type: PatchedWritableWirelessLANRequestGroup?, nillable: true, emit_null: false)]
    property group : PatchedWritableWirelessLANRequestGroup?

    # * `active` - Active * `reserved` - Reserved * `disabled` - Disabled * `deprecated` - Deprecated
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    @[JSON::Field(key: "vlan", type: InterfaceRequestUntaggedVlan?, nillable: true, emit_null: false)]
    property vlan : InterfaceRequestUntaggedVlan?

    @[JSON::Field(key: "scope_type", type: String?, nillable: true, emit_null: false)]
    property scope_type : String?

    @[JSON::Field(key: "scope_id", type: Int32?, nillable: true, emit_null: false)]
    property scope_id : Int32?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    # * `open` - Open * `wep` - WEP * `wpa-personal` - WPA Personal (PSK) * `wpa-enterprise` - WPA Enterprise
    @[JSON::Field(key: "auth_type", type: String?, nillable: true, emit_null: false)]
    property auth_type : String?

    # * `auto` - Auto * `tkip` - TKIP * `aes` - AES
    @[JSON::Field(key: "auth_cipher", type: String?, nillable: true, emit_null: false)]
    property auth_cipher : String?

    @[JSON::Field(key: "auth_psk", type: String?, nillable: true, emit_null: false)]
    property auth_psk : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["active", "reserved", "disabled", "deprecated"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForAuthType < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "auth_type"
        @allowable_values = ["open", "wep", "wpa-personal", "wpa-enterprise", ""].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForAuthCipher < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "auth_cipher"
        @allowable_values = ["auto", "tkip", "aes", ""].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@ssid : String, @description : String? = nil, @group : PatchedWritableWirelessLANRequestGroup? = nil, @status : String? = nil, @vlan : InterfaceRequestUntaggedVlan? = nil, @scope_type : String? = nil, @scope_id : Int32? = nil, @tenant : ASNRangeRequestTenant? = nil, @auth_type : String? = nil, @auth_cipher : String? = nil, @auth_psk : String? = nil, @comments : String? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @ssid.try &.to_s.try &.size.try &.> 32
        invalid_properties.push("invalid value for \"ssid\", the character length must be smaller than or equal to 32.")
      end

      if @ssid.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"ssid\", the character length must be greater than or equal to 1.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      auth_type_validator = EnumAttributeValidatorForAuthType.new
      if !auth_type_validator.valid?(@auth_type)
        message = auth_type_validator.message
        invalid_properties.push(message)
      end

      auth_cipher_validator = EnumAttributeValidatorForAuthCipher.new
      if !auth_cipher_validator.valid?(@auth_cipher)
        message = auth_cipher_validator.message
        invalid_properties.push(message)
      end

      if !@auth_psk.nil? && @auth_psk.try &.to_s.try &.size.try &.> 64
        invalid_properties.push("invalid value for \"auth_psk\", the character length must be smaller than or equal to 64.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @ssid.try &.to_s.try &.size.try &.> 32
      return false if @ssid.try &.to_s.try &.size.try &.< 1
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      auth_type_validator = EnumAttributeValidatorForAuthType.new
      return false unless auth_type_validator.valid?(@auth_type)
      auth_cipher_validator = EnumAttributeValidatorForAuthCipher.new
      return false unless auth_cipher_validator.valid?(@auth_cipher)
      return false if !@auth_psk.nil? && @auth_psk.try &.to_s.try &.size.try &.> 64
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] ssid Value to be assigned
    def ssid=(ssid)
      if ssid.to_s.size > 32
        raise ArgumentError.new("invalid value for \"ssid\", the character length must be smaller than or equal to 32.")
      end

      if ssid.to_s.size < 1
        raise ArgumentError.new("invalid value for \"ssid\", the character length must be greater than or equal to 1.")
      end

      @ssid = ssid
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] auth_type Object to be assigned
    def auth_type=(auth_type)
      validator = EnumAttributeValidatorForAuthType.new
      unless validator.valid?(auth_type)
        raise ArgumentError.new(validator.message)
      end
      @auth_type = auth_type
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] auth_cipher Object to be assigned
    def auth_cipher=(auth_cipher)
      validator = EnumAttributeValidatorForAuthCipher.new
      unless validator.valid?(auth_cipher)
        raise ArgumentError.new(validator.message)
      end
      @auth_cipher = auth_cipher
    end

    # Custom attribute writer method with validation
    # @param [Object] auth_psk Value to be assigned
    def auth_psk=(auth_psk)
      if !auth_psk.nil? && auth_psk.to_s.size > 64
        raise ArgumentError.new("invalid value for \"auth_psk\", the character length must be smaller than or equal to 64.")
      end

      @auth_psk = auth_psk
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        ssid == other.ssid &&
        description == other.description &&
        group == other.group &&
        status == other.status &&
        vlan == other.vlan &&
        scope_type == other.scope_type &&
        scope_id == other.scope_id &&
        tenant == other.tenant &&
        auth_type == other.auth_type &&
        auth_cipher == other.auth_cipher &&
        auth_psk == other.auth_psk &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [ssid, description, group, status, vlan, scope_type, scope_id, tenant, auth_type, auth_cipher, auth_psk, comments, tags, custom_fields].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["ssid"] = _to_h(ssid)
      hash["description"] = _to_h(description)
      hash["group"] = _to_h(group)
      hash["status"] = _to_h(status)
      hash["vlan"] = _to_h(vlan)
      hash["scope_type"] = _to_h(scope_type)
      hash["scope_id"] = _to_h(scope_id)
      hash["tenant"] = _to_h(tenant)
      hash["auth_type"] = _to_h(auth_type)
      hash["auth_cipher"] = _to_h(auth_cipher)
      hash["auth_psk"] = _to_h(auth_psk)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
