# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class PatchedWritableDataSourceRequest
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "name", type: String?, nillable: true, emit_null: false)]
    property name : String?

    @[JSON::Field(key: "type", type: String?, nillable: true, emit_null: false)]
    property _type : String?

    @[JSON::Field(key: "source_url", type: String?, nillable: true, emit_null: false)]
    property source_url : String?

    @[JSON::Field(key: "enabled", type: Bool?, nillable: true, emit_null: false)]
    property enabled : Bool?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # * `1` - Minutely * `60` - Hourly * `720` - 12 hours * `1440` - Daily * `10080` - Weekly * `43200` - 30 days
    @[JSON::Field(key: "sync_interval", type: Int32?, nillable: true, emit_null: false)]
    property sync_interval : Int32?

    @[JSON::Field(key: "parameters", type: JSON::Any?, nillable: true, emit_null: false)]
    property parameters : JSON::Any?

    # Patterns (one per line) matching files to ignore when syncing
    @[JSON::Field(key: "ignore_rules", type: String?, nillable: true, emit_null: false)]
    property ignore_rules : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForSyncInterval < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "sync_interval"
        @allowable_values = ["1", "60", "720", "1440", "10080", "43200"].map { |value| to(Int32, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@name : String? = nil, @_type : String? = nil, @source_url : String? = nil, @enabled : Bool? = nil, @description : String? = nil, @sync_interval : Int32? = nil, @parameters : JSON::Any? = nil, @ignore_rules : String? = nil, @comments : String? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@name.nil? && @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !@name.nil? && @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if !@_type.nil? && @_type.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"_type\", the character length must be smaller than or equal to 50.")
      end

      if !@_type.nil? && @_type.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"_type\", the character length must be greater than or equal to 1.")
      end

      if !@source_url.nil? && @source_url.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"source_url\", the character length must be smaller than or equal to 200.")
      end

      if !@source_url.nil? && @source_url.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"source_url\", the character length must be greater than or equal to 1.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      sync_interval_validator = EnumAttributeValidatorForSyncInterval.new
      if !sync_interval_validator.valid?(@sync_interval)
        message = sync_interval_validator.message
        invalid_properties.push(message)
      end

      if !@sync_interval.nil? && @sync_interval.try &.> 32767
        invalid_properties.push("invalid value for \"sync_interval\", must be smaller than or equal to 32767.")
      end

      if !@sync_interval.nil? && @sync_interval.try &.< 0
        invalid_properties.push("invalid value for \"sync_interval\", must be greater than or equal to 0.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@name.nil? && @name.try &.to_s.try &.size.try &.> 100
      return false if !@name.nil? && @name.try &.to_s.try &.size.try &.< 1
      return false if !@_type.nil? && @_type.try &.to_s.try &.size.try &.> 50
      return false if !@_type.nil? && @_type.try &.to_s.try &.size.try &.< 1
      return false if !@source_url.nil? && @source_url.try &.to_s.try &.size.try &.> 200
      return false if !@source_url.nil? && @source_url.try &.to_s.try &.size.try &.< 1
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      sync_interval_validator = EnumAttributeValidatorForSyncInterval.new
      return false unless sync_interval_validator.valid?(@sync_interval)
      return false if !@sync_interval.nil? && @sync_interval.try &.> 32767
      return false if !@sync_interval.nil? && @sync_interval.try &.< 0
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if !name.nil? && name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !name.nil? && name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] _type Value to be assigned
    def _type=(_type)
      if !_type.nil? && _type.to_s.size > 50
        raise ArgumentError.new("invalid value for \"_type\", the character length must be smaller than or equal to 50.")
      end

      if !_type.nil? && _type.to_s.size < 1
        raise ArgumentError.new("invalid value for \"_type\", the character length must be greater than or equal to 1.")
      end

      @_type = _type
    end

    # Custom attribute writer method with validation
    # @param [Object] source_url Value to be assigned
    def source_url=(source_url)
      if !source_url.nil? && source_url.to_s.size > 200
        raise ArgumentError.new("invalid value for \"source_url\", the character length must be smaller than or equal to 200.")
      end

      if !source_url.nil? && source_url.to_s.size < 1
        raise ArgumentError.new("invalid value for \"source_url\", the character length must be greater than or equal to 1.")
      end

      @source_url = source_url
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] sync_interval Object to be assigned
    def sync_interval=(sync_interval)
      validator = EnumAttributeValidatorForSyncInterval.new
      unless validator.valid?(sync_interval)
        raise ArgumentError.new(validator.message)
      end
      @sync_interval = sync_interval
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        name == other.name &&
        _type == other._type &&
        source_url == other.source_url &&
        enabled == other.enabled &&
        description == other.description &&
        sync_interval == other.sync_interval &&
        parameters == other.parameters &&
        ignore_rules == other.ignore_rules &&
        comments == other.comments &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [name, _type, source_url, enabled, description, sync_interval, parameters, ignore_rules, comments, custom_fields].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["name"] = _to_h(name)
      hash["type"] = _to_h(_type)
      hash["source_url"] = _to_h(source_url)
      hash["enabled"] = _to_h(enabled)
      hash["description"] = _to_h(description)
      hash["sync_interval"] = _to_h(sync_interval)
      hash["parameters"] = _to_h(parameters)
      hash["ignore_rules"] = _to_h(ignore_rules)
      hash["comments"] = _to_h(comments)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
