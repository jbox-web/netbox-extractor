# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class SiteRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    # Full name of the site
    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    @[JSON::Field(key: "slug", type: String, nillable: false, emit_null: false)]
    property slug : String

    # Optional properties
    # * `planned` - Planned * `staging` - Staging * `active` - Active * `decommissioning` - Decommissioning * `retired` - Retired
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    @[JSON::Field(key: "region", type: PatchedWritableSiteRequestRegion?, nillable: true, emit_null: false)]
    property region : PatchedWritableSiteRequestRegion?

    @[JSON::Field(key: "group", type: PatchedWritableSiteRequestGroup?, nillable: true, emit_null: false)]
    property group : PatchedWritableSiteRequestGroup?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    # Local facility ID or description
    @[JSON::Field(key: "facility", type: String?, nillable: true, emit_null: false)]
    property facility : String?

    @[JSON::Field(key: "time_zone", type: String?, nillable: true, emit_null: false)]
    property time_zone : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # Physical location of the building
    @[JSON::Field(key: "physical_address", type: String?, nillable: true, emit_null: false)]
    property physical_address : String?

    # If different from the physical address
    @[JSON::Field(key: "shipping_address", type: String?, nillable: true, emit_null: false)]
    property shipping_address : String?

    # GPS coordinate in decimal format (xx.yyyyyy)
    @[JSON::Field(key: "latitude", type: Float64?, nillable: true, emit_null: false)]
    property latitude : Float64?

    # GPS coordinate in decimal format (xx.yyyyyy)
    @[JSON::Field(key: "longitude", type: Float64?, nillable: true, emit_null: false)]
    property longitude : Float64?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "asns", type: Array(Int32)?, nillable: true, emit_null: false)]
    property asns : Array(Int32)?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["planned", "staging", "active", "decommissioning", "retired"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@name : String, @slug : String, @status : String? = nil, @region : PatchedWritableSiteRequestRegion? = nil, @group : PatchedWritableSiteRequestGroup? = nil, @tenant : ASNRangeRequestTenant? = nil, @facility : String? = nil, @time_zone : String? = nil, @description : String? = nil, @physical_address : String? = nil, @shipping_address : String? = nil, @latitude : Float64? = nil, @longitude : Float64? = nil, @comments : String? = nil, @asns : Array(Int32)? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if @slug.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"slug\", the character length must be smaller than or equal to 100.")
      end

      if @slug.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"slug\", the character length must be greater than or equal to 1.")
      end

      pattern = /^[-a-zA-Z0-9_]+$/
      if @slug.try &.!~ pattern
        invalid_properties.push("invalid value for \"slug\", must conform to the pattern #{pattern}.")
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      if !@facility.nil? && @facility.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"facility\", the character length must be smaller than or equal to 50.")
      end

      if !@time_zone.nil? && @time_zone.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"time_zone\", the character length must be greater than or equal to 1.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      if !@physical_address.nil? && @physical_address.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"physical_address\", the character length must be smaller than or equal to 200.")
      end

      if !@shipping_address.nil? && @shipping_address.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"shipping_address\", the character length must be smaller than or equal to 200.")
      end

      if !@latitude.nil? && @latitude.try &.> 90.0
        invalid_properties.push("invalid value for \"latitude\", must be smaller than or equal to 90.0.")
      end

      if !@latitude.nil? && @latitude.try &.< -90.0
        invalid_properties.push("invalid value for \"latitude\", must be greater than or equal to -90.0.")
      end

      if !@longitude.nil? && @longitude.try &.> 180.0
        invalid_properties.push("invalid value for \"longitude\", must be smaller than or equal to 180.0.")
      end

      if !@longitude.nil? && @longitude.try &.< -180.0
        invalid_properties.push("invalid value for \"longitude\", must be greater than or equal to -180.0.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 100
      return false if @name.try &.to_s.try &.size.try &.< 1
      return false if @slug.try &.to_s.try &.size.try &.> 100
      return false if @slug.try &.to_s.try &.size.try &.< 1
      return false if @slug.try &.!~ /^[-a-zA-Z0-9_]+$/
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      return false if !@facility.nil? && @facility.try &.to_s.try &.size.try &.> 50
      return false if !@time_zone.nil? && @time_zone.try &.to_s.try &.size.try &.< 1
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      return false if !@physical_address.nil? && @physical_address.try &.to_s.try &.size.try &.> 200
      return false if !@shipping_address.nil? && @shipping_address.try &.to_s.try &.size.try &.> 200
      return false if !@latitude.nil? && @latitude.try &.> 90.0
      return false if !@latitude.nil? && @latitude.try &.< -90.0
      return false if !@longitude.nil? && @longitude.try &.> 180.0
      return false if !@longitude.nil? && @longitude.try &.< -180.0
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] slug Value to be assigned
    def slug=(slug)
      if slug.to_s.size > 100
        raise ArgumentError.new("invalid value for \"slug\", the character length must be smaller than or equal to 100.")
      end

      if slug.to_s.size < 1
        raise ArgumentError.new("invalid value for \"slug\", the character length must be greater than or equal to 1.")
      end

      pattern = /^[-a-zA-Z0-9_]+$/
      if slug !~ pattern
        raise ArgumentError.new("invalid value for \"slug\", must conform to the pattern #{pattern}.")
      end

      @slug = slug
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method with validation
    # @param [Object] facility Value to be assigned
    def facility=(facility)
      if !facility.nil? && facility.to_s.size > 50
        raise ArgumentError.new("invalid value for \"facility\", the character length must be smaller than or equal to 50.")
      end

      @facility = facility
    end

    # Custom attribute writer method with validation
    # @param [Object] time_zone Value to be assigned
    def time_zone=(time_zone)
      if !time_zone.nil? && time_zone.to_s.size < 1
        raise ArgumentError.new("invalid value for \"time_zone\", the character length must be greater than or equal to 1.")
      end

      @time_zone = time_zone
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method with validation
    # @param [Object] physical_address Value to be assigned
    def physical_address=(physical_address)
      if !physical_address.nil? && physical_address.to_s.size > 200
        raise ArgumentError.new("invalid value for \"physical_address\", the character length must be smaller than or equal to 200.")
      end

      @physical_address = physical_address
    end

    # Custom attribute writer method with validation
    # @param [Object] shipping_address Value to be assigned
    def shipping_address=(shipping_address)
      if !shipping_address.nil? && shipping_address.to_s.size > 200
        raise ArgumentError.new("invalid value for \"shipping_address\", the character length must be smaller than or equal to 200.")
      end

      @shipping_address = shipping_address
    end

    # Custom attribute writer method with validation
    # @param [Object] latitude Value to be assigned
    def latitude=(latitude)
      if !latitude.nil? && latitude > 90.0
        raise ArgumentError.new("invalid value for \"latitude\", must be smaller than or equal to 90.0.")
      end

      if !latitude.nil? && latitude < -90.0
        raise ArgumentError.new("invalid value for \"latitude\", must be greater than or equal to -90.0.")
      end

      @latitude = latitude
    end

    # Custom attribute writer method with validation
    # @param [Object] longitude Value to be assigned
    def longitude=(longitude)
      if !longitude.nil? && longitude > 180.0
        raise ArgumentError.new("invalid value for \"longitude\", must be smaller than or equal to 180.0.")
      end

      if !longitude.nil? && longitude < -180.0
        raise ArgumentError.new("invalid value for \"longitude\", must be greater than or equal to -180.0.")
      end

      @longitude = longitude
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        name == other.name &&
        slug == other.slug &&
        status == other.status &&
        region == other.region &&
        group == other.group &&
        tenant == other.tenant &&
        facility == other.facility &&
        time_zone == other.time_zone &&
        description == other.description &&
        physical_address == other.physical_address &&
        shipping_address == other.shipping_address &&
        latitude == other.latitude &&
        longitude == other.longitude &&
        comments == other.comments &&
        asns == other.asns &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [name, slug, status, region, group, tenant, facility, time_zone, description, physical_address, shipping_address, latitude, longitude, comments, asns, tags, custom_fields].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["name"] = _to_h(name)
      hash["slug"] = _to_h(slug)
      hash["status"] = _to_h(status)
      hash["region"] = _to_h(region)
      hash["group"] = _to_h(group)
      hash["tenant"] = _to_h(tenant)
      hash["facility"] = _to_h(facility)
      hash["time_zone"] = _to_h(time_zone)
      hash["description"] = _to_h(description)
      hash["physical_address"] = _to_h(physical_address)
      hash["shipping_address"] = _to_h(shipping_address)
      hash["latitude"] = _to_h(latitude)
      hash["longitude"] = _to_h(longitude)
      hash["comments"] = _to_h(comments)
      hash["asns"] = _to_h(asns)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
