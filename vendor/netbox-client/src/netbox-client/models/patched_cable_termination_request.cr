# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class PatchedCableTerminationRequest
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "cable", type: Int32?, nillable: true, emit_null: false)]
    property cable : Int32?

    # * `A` - A * `B` - B
    @[JSON::Field(key: "cable_end", type: String?, nillable: true, emit_null: false)]
    property cable_end : String?

    @[JSON::Field(key: "termination_type", type: String?, nillable: true, emit_null: false)]
    property termination_type : String?

    @[JSON::Field(key: "termination_id", type: Int64?, nillable: true, emit_null: false)]
    property termination_id : Int64?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForCableEnd < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "cable_end"
        @allowable_values = ["A", "B"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@cable : Int32? = nil, @cable_end : String? = nil, @termination_type : String? = nil, @termination_id : Int64? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      cable_end_validator = EnumAttributeValidatorForCableEnd.new
      if !cable_end_validator.valid?(@cable_end)
        message = cable_end_validator.message
        invalid_properties.push(message)
      end

      if !@termination_id.nil? && @termination_id.try &.> 9223372036854775807
        invalid_properties.push("invalid value for \"termination_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if !@termination_id.nil? && @termination_id.try &.< 0
        invalid_properties.push("invalid value for \"termination_id\", must be greater than or equal to 0.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      cable_end_validator = EnumAttributeValidatorForCableEnd.new
      return false unless cable_end_validator.valid?(@cable_end)
      return false if !@termination_id.nil? && @termination_id.try &.> 9223372036854775807
      return false if !@termination_id.nil? && @termination_id.try &.< 0
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] cable_end Object to be assigned
    def cable_end=(cable_end)
      validator = EnumAttributeValidatorForCableEnd.new
      unless validator.valid?(cable_end)
        raise ArgumentError.new(validator.message)
      end
      @cable_end = cable_end
    end

    # Custom attribute writer method with validation
    # @param [Object] termination_id Value to be assigned
    def termination_id=(termination_id)
      if !termination_id.nil? && termination_id > 9223372036854775807
        raise ArgumentError.new("invalid value for \"termination_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if !termination_id.nil? && termination_id < 0
        raise ArgumentError.new("invalid value for \"termination_id\", must be greater than or equal to 0.")
      end

      @termination_id = termination_id
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        cable == other.cable &&
        cable_end == other.cable_end &&
        termination_type == other.termination_type &&
        termination_id == other.termination_id
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [cable, cable_end, termination_type, termination_id].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["cable"] = _to_h(cable)
      hash["cable_end"] = _to_h(cable_end)
      hash["termination_type"] = _to_h(termination_type)
      hash["termination_id"] = _to_h(termination_id)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
