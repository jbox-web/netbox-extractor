# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Extends the built-in ModelSerializer to enforce calling full_clean() on a copy of the associated instance during validation. (DRF does not do this by default; see https://github.com/encode/django-rest-framework/issues/3144)
  class CustomFieldRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "object_types", type: Array(String), nillable: false, emit_null: false)]
    property object_types : Array(String)

    # * `text` - Text * `longtext` - Text (long) * `integer` - Integer * `decimal` - Decimal * `boolean` - Boolean (true/false) * `date` - Date * `datetime` - Date & time * `url` - URL * `json` - JSON * `select` - Selection * `multiselect` - Multiple selection * `object` - Object * `multiobject` - Multiple objects
    @[JSON::Field(key: "type", type: String, nillable: false, emit_null: false)]
    property _type : String

    # Internal field name
    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # Optional properties
    @[JSON::Field(key: "related_object_type", type: String?, nillable: true, emit_null: false)]
    property related_object_type : String?

    # Name of the field as displayed to users (if not provided, 'the field's name will be used)
    @[JSON::Field(key: "label", type: String?, nillable: true, emit_null: false)]
    property label : String?

    # Custom fields within the same group will be displayed together
    @[JSON::Field(key: "group_name", type: String?, nillable: true, emit_null: false)]
    property group_name : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # This field is required when creating new objects or editing an existing object.
    @[JSON::Field(key: "required", type: Bool?, nillable: true, emit_null: false)]
    property required : Bool?

    # The value of this field must be unique for the assigned object
    @[JSON::Field(key: "unique", type: Bool?, nillable: true, emit_null: false)]
    property unique : Bool?

    # Weighting for search. Lower values are considered more important. Fields with a search weight of zero will be ignored.
    @[JSON::Field(key: "search_weight", type: Int32?, nillable: true, emit_null: false)]
    property search_weight : Int32?

    # * `disabled` - Disabled * `loose` - Loose * `exact` - Exact
    @[JSON::Field(key: "filter_logic", type: String?, nillable: true, emit_null: false)]
    property filter_logic : String?

    # * `always` - Always * `if-set` - If set * `hidden` - Hidden
    @[JSON::Field(key: "ui_visible", type: String?, nillable: true, emit_null: false)]
    property ui_visible : String?

    # * `yes` - Yes * `no` - No * `hidden` - Hidden
    @[JSON::Field(key: "ui_editable", type: String?, nillable: true, emit_null: false)]
    property ui_editable : String?

    # Replicate this value when cloning objects
    @[JSON::Field(key: "is_cloneable", type: Bool?, nillable: true, emit_null: false)]
    property is_cloneable : Bool?

    # Default value for the field (must be a JSON value). Encapsulate strings with double quotes (e.g. \"Foo\").
    @[JSON::Field(key: "default", type: JSON::Any?, nillable: true, emit_null: false)]
    property default : JSON::Any?

    # Filter the object selection choices using a query_params dict (must be a JSON value).Encapsulate strings with double quotes (e.g. \"Foo\").
    @[JSON::Field(key: "related_object_filter", type: JSON::Any?, nillable: true, emit_null: false)]
    property related_object_filter : JSON::Any?

    # Fields with higher weights appear lower in a form.
    @[JSON::Field(key: "weight", type: Int32?, nillable: true, emit_null: false)]
    property weight : Int32?

    # Minimum allowed value (for numeric fields)
    @[JSON::Field(key: "validation_minimum", type: Float64?, nillable: true, emit_null: false)]
    property validation_minimum : Float64?

    # Maximum allowed value (for numeric fields)
    @[JSON::Field(key: "validation_maximum", type: Float64?, nillable: true, emit_null: false)]
    property validation_maximum : Float64?

    # Regular expression to enforce on text field values. Use ^ and $ to force matching of entire string. For example, <code>^[A-Z]{3}$</code> will limit values to exactly three uppercase letters.
    @[JSON::Field(key: "validation_regex", type: String?, nillable: true, emit_null: false)]
    property validation_regex : String?

    @[JSON::Field(key: "choice_set", type: CustomFieldRequestChoiceSet?, nillable: true, emit_null: false)]
    property choice_set : CustomFieldRequestChoiceSet?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForType < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "_type"
        @allowable_values = ["text", "longtext", "integer", "decimal", "boolean", "date", "datetime", "url", "json", "select", "multiselect", "object", "multiobject"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForFilterLogic < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "filter_logic"
        @allowable_values = ["disabled", "loose", "exact"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForUiVisible < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "ui_visible"
        @allowable_values = ["always", "if-set", "hidden"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForUiEditable < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "ui_editable"
        @allowable_values = ["yes", "no", "hidden"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@object_types : Array(String), @_type : String, @name : String, @related_object_type : String? = nil, @label : String? = nil, @group_name : String? = nil, @description : String? = nil, @required : Bool? = nil, @unique : Bool? = nil, @search_weight : Int32? = nil, @filter_logic : String? = nil, @ui_visible : String? = nil, @ui_editable : String? = nil, @is_cloneable : Bool? = nil, @default : JSON::Any? = nil, @related_object_filter : JSON::Any? = nil, @weight : Int32? = nil, @validation_minimum : Float64? = nil, @validation_maximum : Float64? = nil, @validation_regex : String? = nil, @choice_set : CustomFieldRequestChoiceSet? = nil, @comments : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      _type_validator = EnumAttributeValidatorForType.new
      if !_type_validator.valid?(@_type)
        message = _type_validator.message
        invalid_properties.push(message)
      end

      if @name.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 50.")
      end

      if @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      pattern = /^[a-z0-9_]+$/
      if @name.try &.!~ pattern
        invalid_properties.push("invalid value for \"name\", must conform to the pattern #{pattern}.")
      end

      if !@label.nil? && @label.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"label\", the character length must be smaller than or equal to 50.")
      end

      if !@group_name.nil? && @group_name.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"group_name\", the character length must be smaller than or equal to 50.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      if !@search_weight.nil? && @search_weight.try &.> 32767
        invalid_properties.push("invalid value for \"search_weight\", must be smaller than or equal to 32767.")
      end

      if !@search_weight.nil? && @search_weight.try &.< 0
        invalid_properties.push("invalid value for \"search_weight\", must be greater than or equal to 0.")
      end

      filter_logic_validator = EnumAttributeValidatorForFilterLogic.new
      if !filter_logic_validator.valid?(@filter_logic)
        message = filter_logic_validator.message
        invalid_properties.push(message)
      end

      ui_visible_validator = EnumAttributeValidatorForUiVisible.new
      if !ui_visible_validator.valid?(@ui_visible)
        message = ui_visible_validator.message
        invalid_properties.push(message)
      end

      ui_editable_validator = EnumAttributeValidatorForUiEditable.new
      if !ui_editable_validator.valid?(@ui_editable)
        message = ui_editable_validator.message
        invalid_properties.push(message)
      end

      if !@weight.nil? && @weight.try &.> 32767
        invalid_properties.push("invalid value for \"weight\", must be smaller than or equal to 32767.")
      end

      if !@weight.nil? && @weight.try &.< 0
        invalid_properties.push("invalid value for \"weight\", must be greater than or equal to 0.")
      end

      if !@validation_minimum.nil? && @validation_minimum.try &.>= 1000000000000
        invalid_properties.push("invalid value for \"validation_minimum\", must be smaller than 1000000000000.")
      end

      if !@validation_minimum.nil? && @validation_minimum.try &.<= -1000000000000
        invalid_properties.push("invalid value for \"validation_minimum\", must be greater than -1000000000000.")
      end

      if !@validation_maximum.nil? && @validation_maximum.try &.>= 1000000000000
        invalid_properties.push("invalid value for \"validation_maximum\", must be smaller than 1000000000000.")
      end

      if !@validation_maximum.nil? && @validation_maximum.try &.<= -1000000000000
        invalid_properties.push("invalid value for \"validation_maximum\", must be greater than -1000000000000.")
      end

      if !@validation_regex.nil? && @validation_regex.try &.to_s.try &.size.try &.> 500
        invalid_properties.push("invalid value for \"validation_regex\", the character length must be smaller than or equal to 500.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      _type_validator = EnumAttributeValidatorForType.new
      return false unless _type_validator.valid?(@_type)
      return false if @name.try &.to_s.try &.size.try &.> 50
      return false if @name.try &.to_s.try &.size.try &.< 1
      return false if @name.try &.!~ /^[a-z0-9_]+$/
      return false if !@label.nil? && @label.try &.to_s.try &.size.try &.> 50
      return false if !@group_name.nil? && @group_name.try &.to_s.try &.size.try &.> 50
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      return false if !@search_weight.nil? && @search_weight.try &.> 32767
      return false if !@search_weight.nil? && @search_weight.try &.< 0
      filter_logic_validator = EnumAttributeValidatorForFilterLogic.new
      return false unless filter_logic_validator.valid?(@filter_logic)
      ui_visible_validator = EnumAttributeValidatorForUiVisible.new
      return false unless ui_visible_validator.valid?(@ui_visible)
      ui_editable_validator = EnumAttributeValidatorForUiEditable.new
      return false unless ui_editable_validator.valid?(@ui_editable)
      return false if !@weight.nil? && @weight.try &.> 32767
      return false if !@weight.nil? && @weight.try &.< 0
      return false if !@validation_minimum.nil? && @validation_minimum.try &.>= 1000000000000
      return false if !@validation_minimum.nil? && @validation_minimum.try &.<= -1000000000000
      return false if !@validation_maximum.nil? && @validation_maximum.try &.>= 1000000000000
      return false if !@validation_maximum.nil? && @validation_maximum.try &.<= -1000000000000
      return false if !@validation_regex.nil? && @validation_regex.try &.to_s.try &.size.try &.> 500
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] _type Object to be assigned
    def _type=(_type)
      validator = EnumAttributeValidatorForType.new
      unless validator.valid?(_type)
        raise ArgumentError.new(validator.message)
      end
      @_type = _type
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 50
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 50.")
      end

      if name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      pattern = /^[a-z0-9_]+$/
      if name !~ pattern
        raise ArgumentError.new("invalid value for \"name\", must conform to the pattern #{pattern}.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] label Value to be assigned
    def label=(label)
      if !label.nil? && label.to_s.size > 50
        raise ArgumentError.new("invalid value for \"label\", the character length must be smaller than or equal to 50.")
      end

      @label = label
    end

    # Custom attribute writer method with validation
    # @param [Object] group_name Value to be assigned
    def group_name=(group_name)
      if !group_name.nil? && group_name.to_s.size > 50
        raise ArgumentError.new("invalid value for \"group_name\", the character length must be smaller than or equal to 50.")
      end

      @group_name = group_name
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method with validation
    # @param [Object] search_weight Value to be assigned
    def search_weight=(search_weight)
      if !search_weight.nil? && search_weight > 32767
        raise ArgumentError.new("invalid value for \"search_weight\", must be smaller than or equal to 32767.")
      end

      if !search_weight.nil? && search_weight < 0
        raise ArgumentError.new("invalid value for \"search_weight\", must be greater than or equal to 0.")
      end

      @search_weight = search_weight
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] filter_logic Object to be assigned
    def filter_logic=(filter_logic)
      validator = EnumAttributeValidatorForFilterLogic.new
      unless validator.valid?(filter_logic)
        raise ArgumentError.new(validator.message)
      end
      @filter_logic = filter_logic
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] ui_visible Object to be assigned
    def ui_visible=(ui_visible)
      validator = EnumAttributeValidatorForUiVisible.new
      unless validator.valid?(ui_visible)
        raise ArgumentError.new(validator.message)
      end
      @ui_visible = ui_visible
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] ui_editable Object to be assigned
    def ui_editable=(ui_editable)
      validator = EnumAttributeValidatorForUiEditable.new
      unless validator.valid?(ui_editable)
        raise ArgumentError.new(validator.message)
      end
      @ui_editable = ui_editable
    end

    # Custom attribute writer method with validation
    # @param [Object] weight Value to be assigned
    def weight=(weight)
      if !weight.nil? && weight > 32767
        raise ArgumentError.new("invalid value for \"weight\", must be smaller than or equal to 32767.")
      end

      if !weight.nil? && weight < 0
        raise ArgumentError.new("invalid value for \"weight\", must be greater than or equal to 0.")
      end

      @weight = weight
    end

    # Custom attribute writer method with validation
    # @param [Object] validation_minimum Value to be assigned
    def validation_minimum=(validation_minimum)
      if !validation_minimum.nil? && validation_minimum >= 1000000000000
        raise ArgumentError.new("invalid value for \"validation_minimum\", must be smaller than 1000000000000.")
      end

      if !validation_minimum.nil? && validation_minimum <= -1000000000000
        raise ArgumentError.new("invalid value for \"validation_minimum\", must be greater than -1000000000000.")
      end

      @validation_minimum = validation_minimum
    end

    # Custom attribute writer method with validation
    # @param [Object] validation_maximum Value to be assigned
    def validation_maximum=(validation_maximum)
      if !validation_maximum.nil? && validation_maximum >= 1000000000000
        raise ArgumentError.new("invalid value for \"validation_maximum\", must be smaller than 1000000000000.")
      end

      if !validation_maximum.nil? && validation_maximum <= -1000000000000
        raise ArgumentError.new("invalid value for \"validation_maximum\", must be greater than -1000000000000.")
      end

      @validation_maximum = validation_maximum
    end

    # Custom attribute writer method with validation
    # @param [Object] validation_regex Value to be assigned
    def validation_regex=(validation_regex)
      if !validation_regex.nil? && validation_regex.to_s.size > 500
        raise ArgumentError.new("invalid value for \"validation_regex\", the character length must be smaller than or equal to 500.")
      end

      @validation_regex = validation_regex
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        object_types == other.object_types &&
        _type == other._type &&
        related_object_type == other.related_object_type &&
        name == other.name &&
        label == other.label &&
        group_name == other.group_name &&
        description == other.description &&
        required == other.required &&
        unique == other.unique &&
        search_weight == other.search_weight &&
        filter_logic == other.filter_logic &&
        ui_visible == other.ui_visible &&
        ui_editable == other.ui_editable &&
        is_cloneable == other.is_cloneable &&
        default == other.default &&
        related_object_filter == other.related_object_filter &&
        weight == other.weight &&
        validation_minimum == other.validation_minimum &&
        validation_maximum == other.validation_maximum &&
        validation_regex == other.validation_regex &&
        choice_set == other.choice_set &&
        comments == other.comments
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [object_types, _type, related_object_type, name, label, group_name, description, required, unique, search_weight, filter_logic, ui_visible, ui_editable, is_cloneable, default, related_object_filter, weight, validation_minimum, validation_maximum, validation_regex, choice_set, comments].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["object_types"] = _to_h(object_types)
      hash["type"] = _to_h(_type)
      hash["related_object_type"] = _to_h(related_object_type)
      hash["name"] = _to_h(name)
      hash["label"] = _to_h(label)
      hash["group_name"] = _to_h(group_name)
      hash["description"] = _to_h(description)
      hash["required"] = _to_h(required)
      hash["unique"] = _to_h(unique)
      hash["search_weight"] = _to_h(search_weight)
      hash["filter_logic"] = _to_h(filter_logic)
      hash["ui_visible"] = _to_h(ui_visible)
      hash["ui_editable"] = _to_h(ui_editable)
      hash["is_cloneable"] = _to_h(is_cloneable)
      hash["default"] = _to_h(default)
      hash["related_object_filter"] = _to_h(related_object_filter)
      hash["weight"] = _to_h(weight)
      hash["validation_minimum"] = _to_h(validation_minimum)
      hash["validation_maximum"] = _to_h(validation_maximum)
      hash["validation_regex"] = _to_h(validation_regex)
      hash["choice_set"] = _to_h(choice_set)
      hash["comments"] = _to_h(comments)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
