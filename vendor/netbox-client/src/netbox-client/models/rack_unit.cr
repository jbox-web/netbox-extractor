# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # A rack unit is an abstraction formed by the set (rack, position, face); it does not exist as a row in the database.
  class RackUnit
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Float64, nillable: false, emit_null: false)]
    property id : Float64

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    @[JSON::Field(key: "face", type: RackUnitFace, nillable: false, emit_null: false)]
    property face : RackUnitFace

    @[JSON::Field(key: "device", type: BriefDevice, nillable: false, emit_null: false)]
    property device : BriefDevice

    @[JSON::Field(key: "occupied", type: Bool, nillable: false, emit_null: false)]
    property occupied : Bool

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Float64, @name : String, @face : RackUnitFace, @device : BriefDevice, @occupied : Bool, @display : String)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @id.try &.>= 1000
        invalid_properties.push("invalid value for \"id\", must be smaller than 1000.")
      end

      if @id.try &.<= -1000
        invalid_properties.push("invalid value for \"id\", must be greater than -1000.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @id.try &.>= 1000
      return false if @id.try &.<= -1000
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] id Value to be assigned
    def id=(id)
      if id >= 1000
        raise ArgumentError.new("invalid value for \"id\", must be smaller than 1000.")
      end

      if id <= -1000
        raise ArgumentError.new("invalid value for \"id\", must be greater than -1000.")
      end

      @id = id
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        name == other.name &&
        face == other.face &&
        device == other.device &&
        occupied == other.occupied &&
        display == other.display
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, name, face, device, occupied, display].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["name"] = _to_h(name)
      hash["face"] = _to_h(face)
      hash["device"] = _to_h(device)
      hash["occupied"] = _to_h(occupied)
      hash["display"] = _to_h(display)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
