# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Extends the built-in ModelSerializer to enforce calling full_clean() on a copy of the associated instance during validation. (DRF does not do this by default; see https://github.com/encode/django-rest-framework/issues/3144)
  class TokenProvisionRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "username", type: String, nillable: false, emit_null: false)]
    property username : String

    @[JSON::Field(key: "password", type: String, nillable: false, emit_null: false)]
    property password : String

    # Optional properties
    @[JSON::Field(key: "expires", type: Time?, nillable: true, emit_null: false)]
    property expires : Time?

    # Permit create/update/delete operations using this key
    @[JSON::Field(key: "write_enabled", type: Bool?, nillable: true, emit_null: false)]
    property write_enabled : Bool?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@username : String, @password : String, @expires : Time? = nil, @write_enabled : Bool? = nil, @description : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      if @username.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"username\", the character length must be greater than or equal to 1.")
      end

      if @password.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"password\", the character length must be greater than or equal to 1.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      return false if @username.try &.to_s.try &.size.try &.< 1
      return false if @password.try &.to_s.try &.size.try &.< 1
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method with validation
    # @param [Object] username Value to be assigned
    def username=(username)
      if username.to_s.size < 1
        raise ArgumentError.new("invalid value for \"username\", the character length must be greater than or equal to 1.")
      end

      @username = username
    end

    # Custom attribute writer method with validation
    # @param [Object] password Value to be assigned
    def password=(password)
      if password.to_s.size < 1
        raise ArgumentError.new("invalid value for \"password\", the character length must be greater than or equal to 1.")
      end

      @password = password
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        expires == other.expires &&
        write_enabled == other.write_enabled &&
        description == other.description &&
        username == other.username &&
        password == other.password
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [expires, write_enabled, description, username, password].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["expires"] = _to_h(expires)
      hash["write_enabled"] = _to_h(write_enabled)
      hash["description"] = _to_h(description)
      hash["username"] = _to_h(username)
      hash["password"] = _to_h(password)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
