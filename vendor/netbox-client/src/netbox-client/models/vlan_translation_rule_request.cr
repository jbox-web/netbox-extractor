# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class VLANTranslationRuleRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "policy", type: Int32, nillable: false, emit_null: false)]
    property policy : Int32

    # Numeric VLAN ID (1-4094)
    @[JSON::Field(key: "local_vid", type: Int32, nillable: false, emit_null: false)]
    property local_vid : Int32

    # Numeric VLAN ID (1-4094)
    @[JSON::Field(key: "remote_vid", type: Int32, nillable: false, emit_null: false)]
    property remote_vid : Int32

    # Optional properties
    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@policy : Int32, @local_vid : Int32, @remote_vid : Int32, @description : String? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @local_vid.try &.> 4094
        invalid_properties.push("invalid value for \"local_vid\", must be smaller than or equal to 4094.")
      end

      if @local_vid.try &.< 1
        invalid_properties.push("invalid value for \"local_vid\", must be greater than or equal to 1.")
      end

      if @remote_vid.try &.> 4094
        invalid_properties.push("invalid value for \"remote_vid\", must be smaller than or equal to 4094.")
      end

      if @remote_vid.try &.< 1
        invalid_properties.push("invalid value for \"remote_vid\", must be greater than or equal to 1.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @local_vid.try &.> 4094
      return false if @local_vid.try &.< 1
      return false if @remote_vid.try &.> 4094
      return false if @remote_vid.try &.< 1
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] local_vid Value to be assigned
    def local_vid=(local_vid)
      if local_vid > 4094
        raise ArgumentError.new("invalid value for \"local_vid\", must be smaller than or equal to 4094.")
      end

      if local_vid < 1
        raise ArgumentError.new("invalid value for \"local_vid\", must be greater than or equal to 1.")
      end

      @local_vid = local_vid
    end

    # Custom attribute writer method with validation
    # @param [Object] remote_vid Value to be assigned
    def remote_vid=(remote_vid)
      if remote_vid > 4094
        raise ArgumentError.new("invalid value for \"remote_vid\", must be smaller than or equal to 4094.")
      end

      if remote_vid < 1
        raise ArgumentError.new("invalid value for \"remote_vid\", must be greater than or equal to 1.")
      end

      @remote_vid = remote_vid
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        policy == other.policy &&
        local_vid == other.local_vid &&
        remote_vid == other.remote_vid &&
        description == other.description
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [policy, local_vid, remote_vid, description].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["policy"] = _to_h(policy)
      hash["local_vid"] = _to_h(local_vid)
      hash["remote_vid"] = _to_h(remote_vid)
      hash["description"] = _to_h(description)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
