# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class VRF
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display_url", type: String, nillable: false, emit_null: false)]
    property display_url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    @[JSON::Field(key: "created", type: Time, nillable: false, emit_null: false)]
    property created : Time

    @[JSON::Field(key: "last_updated", type: Time, nillable: false, emit_null: false)]
    property last_updated : Time

    @[JSON::Field(key: "ipaddress_count", type: Int64, nillable: false, emit_null: false)]
    property ipaddress_count : Int64

    @[JSON::Field(key: "prefix_count", type: Int64, nillable: false, emit_null: false)]
    property prefix_count : Int64

    # Optional properties
    # Unique route distinguisher (as defined in RFC 4364)
    @[JSON::Field(key: "rd", type: String?, nillable: true, emit_null: false)]
    property rd : String?

    @[JSON::Field(key: "tenant", type: BriefTenant?, nillable: true, emit_null: false)]
    property tenant : BriefTenant?

    # Prevent duplicate prefixes/IP addresses within this VRF
    @[JSON::Field(key: "enforce_unique", type: Bool?, nillable: true, emit_null: false)]
    property enforce_unique : Bool?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "import_targets", type: Array(RouteTarget)?, nillable: true, emit_null: false)]
    property import_targets : Array(RouteTarget)?

    @[JSON::Field(key: "export_targets", type: Array(RouteTarget)?, nillable: true, emit_null: false)]
    property export_targets : Array(RouteTarget)?

    @[JSON::Field(key: "tags", type: Array(NestedTag)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTag)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display_url : String, @display : String, @name : String, @created : Time, @last_updated : Time, @ipaddress_count : Int64, @prefix_count : Int64, @rd : String? = nil, @tenant : BriefTenant? = nil, @enforce_unique : Bool? = nil, @description : String? = nil, @comments : String? = nil, @import_targets : Array(RouteTarget)? = nil, @export_targets : Array(RouteTarget)? = nil, @tags : Array(NestedTag)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !@rd.nil? && @rd.try &.to_s.try &.size.try &.> 21
        invalid_properties.push("invalid value for \"rd\", the character length must be smaller than or equal to 21.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 100
      return false if !@rd.nil? && @rd.try &.to_s.try &.size.try &.> 21
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] rd Value to be assigned
    def rd=(rd)
      if !rd.nil? && rd.to_s.size > 21
        raise ArgumentError.new("invalid value for \"rd\", the character length must be smaller than or equal to 21.")
      end

      @rd = rd
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display_url == other.display_url &&
        display == other.display &&
        name == other.name &&
        rd == other.rd &&
        tenant == other.tenant &&
        enforce_unique == other.enforce_unique &&
        description == other.description &&
        comments == other.comments &&
        import_targets == other.import_targets &&
        export_targets == other.export_targets &&
        tags == other.tags &&
        custom_fields == other.custom_fields &&
        created == other.created &&
        last_updated == other.last_updated &&
        ipaddress_count == other.ipaddress_count &&
        prefix_count == other.prefix_count
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display_url, display, name, rd, tenant, enforce_unique, description, comments, import_targets, export_targets, tags, custom_fields, created, last_updated, ipaddress_count, prefix_count].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["url"] = _to_h(url)
      hash["display_url"] = _to_h(display_url)
      hash["display"] = _to_h(display)
      hash["name"] = _to_h(name)
      hash["rd"] = _to_h(rd)
      hash["tenant"] = _to_h(tenant)
      hash["enforce_unique"] = _to_h(enforce_unique)
      hash["description"] = _to_h(description)
      hash["comments"] = _to_h(comments)
      hash["import_targets"] = _to_h(import_targets)
      hash["export_targets"] = _to_h(export_targets)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash["created"] = _to_h(created)
      hash["last_updated"] = _to_h(last_updated)
      hash["ipaddress_count"] = _to_h(ipaddress_count)
      hash["prefix_count"] = _to_h(prefix_count)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
