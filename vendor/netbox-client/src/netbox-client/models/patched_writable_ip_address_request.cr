# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class PatchedWritableIPAddressRequest
    include JSON::Serializable
    include YAML::Serializable

    # Optional properties
    @[JSON::Field(key: "address", type: String?, nillable: true, emit_null: false)]
    property address : String?

    @[JSON::Field(key: "vrf", type: IPAddressRequestVrf?, nillable: true, emit_null: false)]
    property vrf : IPAddressRequestVrf?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    # The operational status of this IP  * `active` - Active * `reserved` - Reserved * `deprecated` - Deprecated * `dhcp` - DHCP * `slaac` - SLAAC
    @[JSON::Field(key: "status", type: String?, nillable: true, emit_null: false)]
    property status : String?

    # The functional role of this IP  * `loopback` - Loopback * `secondary` - Secondary * `anycast` - Anycast * `vip` - VIP * `vrrp` - VRRP * `hsrp` - HSRP * `glbp` - GLBP * `carp` - CARP
    @[JSON::Field(key: "role", type: String?, nillable: true, emit_null: false)]
    property role : String?

    @[JSON::Field(key: "assigned_object_type", type: String?, nillable: true, emit_null: false)]
    property assigned_object_type : String?

    @[JSON::Field(key: "assigned_object_id", type: Int64?, nillable: true, emit_null: false)]
    property assigned_object_id : Int64?

    # The IP for which this address is the \"outside\" IP
    @[JSON::Field(key: "nat_inside", type: Int32?, nillable: true, emit_null: false)]
    property nat_inside : Int32?

    # Hostname or FQDN (not case-sensitive)
    @[JSON::Field(key: "dns_name", type: String?, nillable: true, emit_null: false)]
    property dns_name : String?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["active", "reserved", "deprecated", "dhcp", "slaac"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForRole < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "role"
        @allowable_values = ["loopback", "secondary", "anycast", "vip", "vrrp", "hsrp", "glbp", "carp", ""].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@address : String? = nil, @vrf : IPAddressRequestVrf? = nil, @tenant : ASNRangeRequestTenant? = nil, @status : String? = nil, @role : String? = nil, @assigned_object_type : String? = nil, @assigned_object_id : Int64? = nil, @nat_inside : Int32? = nil, @dns_name : String? = nil, @description : String? = nil, @comments : String? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if !@address.nil? && @address.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"address\", the character length must be greater than or equal to 1.")
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      role_validator = EnumAttributeValidatorForRole.new
      if !role_validator.valid?(@role)
        message = role_validator.message
        invalid_properties.push(message)
      end

      if !@assigned_object_id.nil? && @assigned_object_id.try &.> 9223372036854775807
        invalid_properties.push("invalid value for \"assigned_object_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if !@assigned_object_id.nil? && @assigned_object_id.try &.< 0
        invalid_properties.push("invalid value for \"assigned_object_id\", must be greater than or equal to 0.")
      end

      if !@dns_name.nil? && @dns_name.try &.to_s.try &.size.try &.> 255
        invalid_properties.push("invalid value for \"dns_name\", the character length must be smaller than or equal to 255.")
      end

      pattern = /^([0-9A-Za-z_-]+|\*)(\.[0-9A-Za-z_-]+)*\.?$/
      if !@dns_name.nil? && @dns_name.try &.!~ pattern
        invalid_properties.push("invalid value for \"dns_name\", must conform to the pattern #{pattern}.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@address.nil? && @address.try &.to_s.try &.size.try &.< 1
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      role_validator = EnumAttributeValidatorForRole.new
      return false unless role_validator.valid?(@role)
      return false if !@assigned_object_id.nil? && @assigned_object_id.try &.> 9223372036854775807
      return false if !@assigned_object_id.nil? && @assigned_object_id.try &.< 0
      return false if !@dns_name.nil? && @dns_name.try &.to_s.try &.size.try &.> 255
      return false if !@dns_name.nil? && @dns_name.try &.!~ /^([0-9A-Za-z_-]+|\*)(\.[0-9A-Za-z_-]+)*\.?$/
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] address Value to be assigned
    def address=(address)
      if !address.nil? && address.to_s.size < 1
        raise ArgumentError.new("invalid value for \"address\", the character length must be greater than or equal to 1.")
      end

      @address = address
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] role Object to be assigned
    def role=(role)
      validator = EnumAttributeValidatorForRole.new
      unless validator.valid?(role)
        raise ArgumentError.new(validator.message)
      end
      @role = role
    end

    # Custom attribute writer method with validation
    # @param [Object] assigned_object_id Value to be assigned
    def assigned_object_id=(assigned_object_id)
      if !assigned_object_id.nil? && assigned_object_id > 9223372036854775807
        raise ArgumentError.new("invalid value for \"assigned_object_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if !assigned_object_id.nil? && assigned_object_id < 0
        raise ArgumentError.new("invalid value for \"assigned_object_id\", must be greater than or equal to 0.")
      end

      @assigned_object_id = assigned_object_id
    end

    # Custom attribute writer method with validation
    # @param [Object] dns_name Value to be assigned
    def dns_name=(dns_name)
      if !dns_name.nil? && dns_name.to_s.size > 255
        raise ArgumentError.new("invalid value for \"dns_name\", the character length must be smaller than or equal to 255.")
      end

      pattern = /^([0-9A-Za-z_-]+|\*)(\.[0-9A-Za-z_-]+)*\.?$/
      if !dns_name.nil? && dns_name !~ pattern
        raise ArgumentError.new("invalid value for \"dns_name\", must conform to the pattern #{pattern}.")
      end

      @dns_name = dns_name
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        address == other.address &&
        vrf == other.vrf &&
        tenant == other.tenant &&
        status == other.status &&
        role == other.role &&
        assigned_object_type == other.assigned_object_type &&
        assigned_object_id == other.assigned_object_id &&
        nat_inside == other.nat_inside &&
        dns_name == other.dns_name &&
        description == other.description &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [address, vrf, tenant, status, role, assigned_object_type, assigned_object_id, nat_inside, dns_name, description, comments, tags, custom_fields].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["address"] = _to_h(address)
      hash["vrf"] = _to_h(vrf)
      hash["tenant"] = _to_h(tenant)
      hash["status"] = _to_h(status)
      hash["role"] = _to_h(role)
      hash["assigned_object_type"] = _to_h(assigned_object_type)
      hash["assigned_object_id"] = _to_h(assigned_object_id)
      hash["nat_inside"] = _to_h(nat_inside)
      hash["dns_name"] = _to_h(dns_name)
      hash["description"] = _to_h(description)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
