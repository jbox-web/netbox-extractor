# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class Circuit
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display_url", type: String, nillable: false, emit_null: false)]
    property display_url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    # Unique circuit ID
    @[JSON::Field(key: "cid", type: String, nillable: false, emit_null: false)]
    property cid : String

    @[JSON::Field(key: "provider", type: BriefProvider, nillable: false, emit_null: false)]
    property provider : BriefProvider

    @[JSON::Field(key: "type", type: BriefCircuitType, nillable: false, emit_null: false)]
    property _type : BriefCircuitType

    @[JSON::Field(key: "termination_a", type: CircuitCircuitTermination?, nillable: true, emit_null: false)]
    property termination_a : CircuitCircuitTermination?

    @[JSON::Field(key: "termination_z", type: CircuitCircuitTermination?, nillable: true, emit_null: false)]
    property termination_z : CircuitCircuitTermination?

    @[JSON::Field(key: "created", type: Time, nillable: false, emit_null: false)]
    property created : Time

    @[JSON::Field(key: "last_updated", type: Time, nillable: false, emit_null: false)]
    property last_updated : Time

    # Optional properties
    @[JSON::Field(key: "provider_account", type: BriefProviderAccount?, nillable: true, emit_null: false)]
    property provider_account : BriefProviderAccount?

    @[JSON::Field(key: "status", type: CircuitStatus?, nillable: true, emit_null: false)]
    property status : CircuitStatus?

    @[JSON::Field(key: "tenant", type: BriefTenant?, nillable: true, emit_null: false)]
    property tenant : BriefTenant?

    @[JSON::Field(key: "install_date", type: Time?, nillable: true, emit_null: false)]
    property install_date : Time?

    @[JSON::Field(key: "termination_date", type: Time?, nillable: true, emit_null: false)]
    property termination_date : Time?

    # Committed rate
    @[JSON::Field(key: "commit_rate", type: Int32?, nillable: true, emit_null: false)]
    property commit_rate : Int32?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "distance", type: Float64?, nillable: true, emit_null: false)]
    property distance : Float64?

    @[JSON::Field(key: "distance_unit", type: CircuitDistanceUnit?, nillable: true, emit_null: false)]
    property distance_unit : CircuitDistanceUnit?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTag)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTag)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    @[JSON::Field(key: "assignments", type: Array(BriefCircuitGroupAssignmentSerializer)?, nillable: true, emit_null: false)]
    property assignments : Array(BriefCircuitGroupAssignmentSerializer)?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display_url : String, @display : String, @cid : String, @provider : BriefProvider, @_type : BriefCircuitType, @termination_a : CircuitCircuitTermination, @termination_z : CircuitCircuitTermination, @created : Time, @last_updated : Time, @provider_account : BriefProviderAccount? = nil, @status : CircuitStatus? = nil, @tenant : BriefTenant? = nil, @install_date : Time? = nil, @termination_date : Time? = nil, @commit_rate : Int32? = nil, @description : String? = nil, @distance : Float64? = nil, @distance_unit : CircuitDistanceUnit? = nil, @comments : String? = nil, @tags : Array(NestedTag)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil, @assignments : Array(BriefCircuitGroupAssignmentSerializer)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @cid.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"cid\", the character length must be smaller than or equal to 100.")
      end

      if !@commit_rate.nil? && @commit_rate.try &.> 2147483647
        invalid_properties.push("invalid value for \"commit_rate\", must be smaller than or equal to 2147483647.")
      end

      if !@commit_rate.nil? && @commit_rate.try &.< 0
        invalid_properties.push("invalid value for \"commit_rate\", must be greater than or equal to 0.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      if !@distance.nil? && @distance.try &.>= 1000000
        invalid_properties.push("invalid value for \"distance\", must be smaller than 1000000.")
      end

      if !@distance.nil? && @distance.try &.<= -1000000
        invalid_properties.push("invalid value for \"distance\", must be greater than -1000000.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @cid.try &.to_s.try &.size.try &.> 100
      return false if !@commit_rate.nil? && @commit_rate.try &.> 2147483647
      return false if !@commit_rate.nil? && @commit_rate.try &.< 0
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      return false if !@distance.nil? && @distance.try &.>= 1000000
      return false if !@distance.nil? && @distance.try &.<= -1000000
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] cid Value to be assigned
    def cid=(cid)
      if cid.to_s.size > 100
        raise ArgumentError.new("invalid value for \"cid\", the character length must be smaller than or equal to 100.")
      end

      @cid = cid
    end

    # Custom attribute writer method with validation
    # @param [Object] commit_rate Value to be assigned
    def commit_rate=(commit_rate)
      if !commit_rate.nil? && commit_rate > 2147483647
        raise ArgumentError.new("invalid value for \"commit_rate\", must be smaller than or equal to 2147483647.")
      end

      if !commit_rate.nil? && commit_rate < 0
        raise ArgumentError.new("invalid value for \"commit_rate\", must be greater than or equal to 0.")
      end

      @commit_rate = commit_rate
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method with validation
    # @param [Object] distance Value to be assigned
    def distance=(distance)
      if !distance.nil? && distance >= 1000000
        raise ArgumentError.new("invalid value for \"distance\", must be smaller than 1000000.")
      end

      if !distance.nil? && distance <= -1000000
        raise ArgumentError.new("invalid value for \"distance\", must be greater than -1000000.")
      end

      @distance = distance
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display_url == other.display_url &&
        display == other.display &&
        cid == other.cid &&
        provider == other.provider &&
        provider_account == other.provider_account &&
        _type == other._type &&
        status == other.status &&
        tenant == other.tenant &&
        install_date == other.install_date &&
        termination_date == other.termination_date &&
        commit_rate == other.commit_rate &&
        description == other.description &&
        distance == other.distance &&
        distance_unit == other.distance_unit &&
        termination_a == other.termination_a &&
        termination_z == other.termination_z &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields &&
        created == other.created &&
        last_updated == other.last_updated &&
        assignments == other.assignments
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display_url, display, cid, provider, provider_account, _type, status, tenant, install_date, termination_date, commit_rate, description, distance, distance_unit, termination_a, termination_z, comments, tags, custom_fields, created, last_updated, assignments].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["url"] = _to_h(url)
      hash["display_url"] = _to_h(display_url)
      hash["display"] = _to_h(display)
      hash["cid"] = _to_h(cid)
      hash["provider"] = _to_h(provider)
      hash["provider_account"] = _to_h(provider_account)
      hash["type"] = _to_h(_type)
      hash["status"] = _to_h(status)
      hash["tenant"] = _to_h(tenant)
      hash["install_date"] = _to_h(install_date)
      hash["termination_date"] = _to_h(termination_date)
      hash["commit_rate"] = _to_h(commit_rate)
      hash["description"] = _to_h(description)
      hash["distance"] = _to_h(distance)
      hash["distance_unit"] = _to_h(distance_unit)
      hash["termination_a"] = _to_h(termination_a)
      hash["termination_z"] = _to_h(termination_z)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash["created"] = _to_h(created)
      hash["last_updated"] = _to_h(last_updated)
      hash["assignments"] = _to_h(assignments)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
