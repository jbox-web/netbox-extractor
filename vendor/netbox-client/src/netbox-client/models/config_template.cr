# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Introduces support for Tag assignment. Adds `tags` serialization, and handles tag assignment on create() and update().
  class ConfigTemplate
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "url", type: String, nillable: false, emit_null: false)]
    property url : String

    @[JSON::Field(key: "display_url", type: String, nillable: false, emit_null: false)]
    property display_url : String

    @[JSON::Field(key: "display", type: String, nillable: false, emit_null: false)]
    property display : String

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # Jinja template code.
    @[JSON::Field(key: "template_code", type: String, nillable: false, emit_null: false)]
    property template_code : String

    # Path to remote file (relative to data source root)
    @[JSON::Field(key: "data_path", type: String, nillable: false, emit_null: false)]
    property data_path : String

    @[JSON::Field(key: "data_synced", type: Time?, nillable: true, emit_null: false)]
    property data_synced : Time?

    @[JSON::Field(key: "created", type: Time, nillable: false, emit_null: false)]
    property created : Time

    @[JSON::Field(key: "last_updated", type: Time, nillable: false, emit_null: false)]
    property last_updated : Time

    # Optional properties
    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # Any <a href=\"https://jinja.palletsprojects.com/en/stable/api/#jinja2.Environment\">additional parameters</a> to pass when constructing the Jinja environment
    @[JSON::Field(key: "environment_params", type: JSON::Any?, nillable: true, emit_null: false)]
    property environment_params : JSON::Any?

    # Defaults to <code>text/plain; charset=utf-8</code>
    @[JSON::Field(key: "mime_type", type: String?, nillable: true, emit_null: false)]
    property mime_type : String?

    # Filename to give to the rendered export file
    @[JSON::Field(key: "file_name", type: String?, nillable: true, emit_null: false)]
    property file_name : String?

    # Extension to append to the rendered filename
    @[JSON::Field(key: "file_extension", type: String?, nillable: true, emit_null: false)]
    property file_extension : String?

    # Download file as attachment
    @[JSON::Field(key: "as_attachment", type: Bool?, nillable: true, emit_null: false)]
    property as_attachment : Bool?

    @[JSON::Field(key: "data_source", type: BriefDataSource?, nillable: true, emit_null: false)]
    property data_source : BriefDataSource?

    @[JSON::Field(key: "data_file", type: BriefDataFile?, nillable: true, emit_null: false)]
    property data_file : BriefDataFile?

    # Enable automatic synchronization of data when the data file is updated
    @[JSON::Field(key: "auto_sync_enabled", type: Bool?, nillable: true, emit_null: false)]
    property auto_sync_enabled : Bool?

    @[JSON::Field(key: "tags", type: Array(NestedTag)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTag)?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @url : String, @display_url : String, @display : String, @name : String, @template_code : String, @data_path : String, @data_synced : Time, @created : Time, @last_updated : Time, @description : String? = nil, @environment_params : JSON::Any? = nil, @mime_type : String? = nil, @file_name : String? = nil, @file_extension : String? = nil, @as_attachment : Bool? = nil, @data_source : BriefDataSource? = nil, @data_file : BriefDataFile? = nil, @auto_sync_enabled : Bool? = nil, @tags : Array(NestedTag)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      if !@mime_type.nil? && @mime_type.try &.to_s.try &.size.try &.> 50
        invalid_properties.push("invalid value for \"mime_type\", the character length must be smaller than or equal to 50.")
      end

      if !@file_name.nil? && @file_name.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"file_name\", the character length must be smaller than or equal to 200.")
      end

      if !@file_extension.nil? && @file_extension.try &.to_s.try &.size.try &.> 15
        invalid_properties.push("invalid value for \"file_extension\", the character length must be smaller than or equal to 15.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 100
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      return false if !@mime_type.nil? && @mime_type.try &.to_s.try &.size.try &.> 50
      return false if !@file_name.nil? && @file_name.try &.to_s.try &.size.try &.> 200
      return false if !@file_extension.nil? && @file_extension.try &.to_s.try &.size.try &.> 15
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Custom attribute writer method with validation
    # @param [Object] mime_type Value to be assigned
    def mime_type=(mime_type)
      if !mime_type.nil? && mime_type.to_s.size > 50
        raise ArgumentError.new("invalid value for \"mime_type\", the character length must be smaller than or equal to 50.")
      end

      @mime_type = mime_type
    end

    # Custom attribute writer method with validation
    # @param [Object] file_name Value to be assigned
    def file_name=(file_name)
      if !file_name.nil? && file_name.to_s.size > 200
        raise ArgumentError.new("invalid value for \"file_name\", the character length must be smaller than or equal to 200.")
      end

      @file_name = file_name
    end

    # Custom attribute writer method with validation
    # @param [Object] file_extension Value to be assigned
    def file_extension=(file_extension)
      if !file_extension.nil? && file_extension.to_s.size > 15
        raise ArgumentError.new("invalid value for \"file_extension\", the character length must be smaller than or equal to 15.")
      end

      @file_extension = file_extension
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        id == other.id &&
        url == other.url &&
        display_url == other.display_url &&
        display == other.display &&
        name == other.name &&
        description == other.description &&
        environment_params == other.environment_params &&
        template_code == other.template_code &&
        mime_type == other.mime_type &&
        file_name == other.file_name &&
        file_extension == other.file_extension &&
        as_attachment == other.as_attachment &&
        data_source == other.data_source &&
        data_path == other.data_path &&
        data_file == other.data_file &&
        auto_sync_enabled == other.auto_sync_enabled &&
        data_synced == other.data_synced &&
        tags == other.tags &&
        created == other.created &&
        last_updated == other.last_updated
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, url, display_url, display, name, description, environment_params, template_code, mime_type, file_name, file_extension, as_attachment, data_source, data_path, data_file, auto_sync_enabled, data_synced, tags, created, last_updated].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["url"] = _to_h(url)
      hash["display_url"] = _to_h(display_url)
      hash["display"] = _to_h(display)
      hash["name"] = _to_h(name)
      hash["description"] = _to_h(description)
      hash["environment_params"] = _to_h(environment_params)
      hash["template_code"] = _to_h(template_code)
      hash["mime_type"] = _to_h(mime_type)
      hash["file_name"] = _to_h(file_name)
      hash["file_extension"] = _to_h(file_extension)
      hash["as_attachment"] = _to_h(as_attachment)
      hash["data_source"] = _to_h(data_source)
      hash["data_path"] = _to_h(data_path)
      hash["data_file"] = _to_h(data_file)
      hash["auto_sync_enabled"] = _to_h(auto_sync_enabled)
      hash["data_synced"] = _to_h(data_synced)
      hash["tags"] = _to_h(tags)
      hash["created"] = _to_h(created)
      hash["last_updated"] = _to_h(last_updated)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
