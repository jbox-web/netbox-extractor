# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Base serializer for group assignments under CircuitSerializer.
  class CircuitGroupAssignmentRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "group", type: BriefCircuitGroupAssignmentSerializerRequestGroup, nillable: false, emit_null: false)]
    property group : BriefCircuitGroupAssignmentSerializerRequestGroup

    @[JSON::Field(key: "member_type", type: String, nillable: false, emit_null: false)]
    property member_type : String

    @[JSON::Field(key: "member_id", type: Int64, nillable: false, emit_null: false)]
    property member_id : Int64

    # Optional properties
    # * `primary` - Primary * `secondary` - Secondary * `tertiary` - Tertiary * `inactive` - Inactive
    @[JSON::Field(key: "priority", type: String?, nillable: true, emit_null: false)]
    property priority : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForPriority < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "priority"
        @allowable_values = ["primary", "secondary", "tertiary", "inactive", ""].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@group : BriefCircuitGroupAssignmentSerializerRequestGroup, @member_type : String, @member_id : Int64, @priority : String? = nil, @tags : Array(NestedTagRequest)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @member_id.try &.> 9223372036854775807
        invalid_properties.push("invalid value for \"member_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if @member_id.try &.< 0
        invalid_properties.push("invalid value for \"member_id\", must be greater than or equal to 0.")
      end

      priority_validator = EnumAttributeValidatorForPriority.new
      if !priority_validator.valid?(@priority)
        message = priority_validator.message
        invalid_properties.push(message)
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @member_id.try &.> 9223372036854775807
      return false if @member_id.try &.< 0
      priority_validator = EnumAttributeValidatorForPriority.new
      return false unless priority_validator.valid?(@priority)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] member_id Value to be assigned
    def member_id=(member_id)
      if member_id > 9223372036854775807
        raise ArgumentError.new("invalid value for \"member_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if member_id < 0
        raise ArgumentError.new("invalid value for \"member_id\", must be greater than or equal to 0.")
      end

      @member_id = member_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] priority Object to be assigned
    def priority=(priority)
      validator = EnumAttributeValidatorForPriority.new
      unless validator.valid?(priority)
        raise ArgumentError.new(validator.message)
      end
      @priority = priority
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        group == other.group &&
        member_type == other.member_type &&
        member_id == other.member_id &&
        priority == other.priority &&
        tags == other.tags
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [group, member_type, member_id, priority, tags].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["group"] = _to_h(group)
      hash["member_type"] = _to_h(member_type)
      hash["member_id"] = _to_h(member_id)
      hash["priority"] = _to_h(priority)
      hash["tags"] = _to_h(tags)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
