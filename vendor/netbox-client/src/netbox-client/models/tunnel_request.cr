# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class TunnelRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # * `planned` - Planned * `active` - Active * `disabled` - Disabled
    @[JSON::Field(key: "status", type: String, nillable: false, emit_null: false)]
    property status : String

    # * `ipsec-transport` - IPsec - Transport * `ipsec-tunnel` - IPsec - Tunnel * `ip-ip` - IP-in-IP * `gre` - GRE * `wireguard` - WireGuard * `openvpn` - OpenVPN * `l2tp` - L2TP * `pptp` - PPTP
    @[JSON::Field(key: "encapsulation", type: String, nillable: false, emit_null: false)]
    property encapsulation : String

    # Optional properties
    @[JSON::Field(key: "group", type: PatchedWritableTunnelRequestGroup?, nillable: true, emit_null: false)]
    property group : PatchedWritableTunnelRequestGroup?

    @[JSON::Field(key: "ipsec_profile", type: PatchedWritableTunnelRequestIpsecProfile?, nillable: true, emit_null: false)]
    property ipsec_profile : PatchedWritableTunnelRequestIpsecProfile?

    @[JSON::Field(key: "tenant", type: ASNRangeRequestTenant?, nillable: true, emit_null: false)]
    property tenant : ASNRangeRequestTenant?

    @[JSON::Field(key: "tunnel_id", type: Int64?, nillable: true, emit_null: false)]
    property tunnel_id : Int64?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    @[JSON::Field(key: "comments", type: String?, nillable: true, emit_null: false)]
    property comments : String?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForStatus < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "status"
        @allowable_values = ["planned", "active", "disabled"].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForEncapsulation < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "encapsulation"
        @allowable_values = ["ipsec-transport", "ipsec-tunnel", "ip-ip", "gre", "wireguard", "openvpn", "l2tp", "pptp"].map { |value| to(String, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@name : String, @status : String, @encapsulation : String, @group : PatchedWritableTunnelRequestGroup? = nil, @ipsec_profile : PatchedWritableTunnelRequestIpsecProfile? = nil, @tenant : ASNRangeRequestTenant? = nil, @tunnel_id : Int64? = nil, @description : String? = nil, @comments : String? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      status_validator = EnumAttributeValidatorForStatus.new
      if !status_validator.valid?(@status)
        message = status_validator.message
        invalid_properties.push(message)
      end

      encapsulation_validator = EnumAttributeValidatorForEncapsulation.new
      if !encapsulation_validator.valid?(@encapsulation)
        message = encapsulation_validator.message
        invalid_properties.push(message)
      end

      if !@tunnel_id.nil? && @tunnel_id.try &.> 9223372036854775807
        invalid_properties.push("invalid value for \"tunnel_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if !@tunnel_id.nil? && @tunnel_id.try &.< 0
        invalid_properties.push("invalid value for \"tunnel_id\", must be greater than or equal to 0.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 100
      return false if @name.try &.to_s.try &.size.try &.< 1
      status_validator = EnumAttributeValidatorForStatus.new
      return false unless status_validator.valid?(@status)
      encapsulation_validator = EnumAttributeValidatorForEncapsulation.new
      return false unless encapsulation_validator.valid?(@encapsulation)
      return false if !@tunnel_id.nil? && @tunnel_id.try &.> 9223372036854775807
      return false if !@tunnel_id.nil? && @tunnel_id.try &.< 0
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidatorForStatus.new
      unless validator.valid?(status)
        raise ArgumentError.new(validator.message)
      end
      @status = status
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] encapsulation Object to be assigned
    def encapsulation=(encapsulation)
      validator = EnumAttributeValidatorForEncapsulation.new
      unless validator.valid?(encapsulation)
        raise ArgumentError.new(validator.message)
      end
      @encapsulation = encapsulation
    end

    # Custom attribute writer method with validation
    # @param [Object] tunnel_id Value to be assigned
    def tunnel_id=(tunnel_id)
      if !tunnel_id.nil? && tunnel_id > 9223372036854775807
        raise ArgumentError.new("invalid value for \"tunnel_id\", must be smaller than or equal to 9223372036854775807.")
      end

      if !tunnel_id.nil? && tunnel_id < 0
        raise ArgumentError.new("invalid value for \"tunnel_id\", must be greater than or equal to 0.")
      end

      @tunnel_id = tunnel_id
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        name == other.name &&
        status == other.status &&
        group == other.group &&
        encapsulation == other.encapsulation &&
        ipsec_profile == other.ipsec_profile &&
        tenant == other.tenant &&
        tunnel_id == other.tunnel_id &&
        description == other.description &&
        comments == other.comments &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [name, status, group, encapsulation, ipsec_profile, tenant, tunnel_id, description, comments, tags, custom_fields].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["name"] = _to_h(name)
      hash["status"] = _to_h(status)
      hash["group"] = _to_h(group)
      hash["encapsulation"] = _to_h(encapsulation)
      hash["ipsec_profile"] = _to_h(ipsec_profile)
      hash["tenant"] = _to_h(tenant)
      hash["tunnel_id"] = _to_h(tunnel_id)
      hash["description"] = _to_h(description)
      hash["comments"] = _to_h(comments)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
