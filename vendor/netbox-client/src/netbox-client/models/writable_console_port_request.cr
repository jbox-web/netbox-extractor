# #NetBox REST API
#
# #No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 4.4.9 (4.4)
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.19.0-SNAPSHOT
#

module NetboxClient
  # Adds support for custom fields and tags.
  class WritableConsolePortRequest
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "device", type: BriefInterfaceRequestDevice, nillable: false, emit_null: false)]
    property device : BriefInterfaceRequestDevice

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    # Optional properties
    @[JSON::Field(key: "module", type: ConsolePortRequestModule?, nillable: true, emit_null: false)]
    property _module : ConsolePortRequestModule?

    # Physical label
    @[JSON::Field(key: "label", type: String?, nillable: true, emit_null: false)]
    property label : String?

    # Physical port type  * `de-9` - DE-9 * `db-25` - DB-25 * `rj-11` - RJ-11 * `rj-12` - RJ-12 * `rj-45` - RJ-45 * `mini-din-8` - Mini-DIN 8 * `usb-a` - USB Type A * `usb-b` - USB Type B * `usb-c` - USB Type C * `usb-mini-a` - USB Mini A * `usb-mini-b` - USB Mini B * `usb-micro-a` - USB Micro A * `usb-micro-b` - USB Micro B * `usb-micro-ab` - USB Micro AB * `other` - Other
    @[JSON::Field(key: "type", type: String?, nillable: true, emit_null: false)]
    property _type : String?

    # Port speed in bits per second  * `1200` - 1200 bps * `2400` - 2400 bps * `4800` - 4800 bps * `9600` - 9600 bps * `19200` - 19.2 kbps * `38400` - 38.4 kbps * `57600` - 57.6 kbps * `115200` - 115.2 kbps
    @[JSON::Field(key: "speed", type: Int32?, nillable: true, emit_null: false)]
    property speed : Int32?

    @[JSON::Field(key: "description", type: String?, nillable: true, emit_null: false)]
    property description : String?

    # Treat as if a cable is connected
    @[JSON::Field(key: "mark_connected", type: Bool?, nillable: true, emit_null: false)]
    property mark_connected : Bool?

    @[JSON::Field(key: "tags", type: Array(NestedTagRequest)?, nillable: true, emit_null: false)]
    property tags : Array(NestedTagRequest)?

    @[JSON::Field(key: "custom_fields", type: Hash(String, JSON::Any)?, nillable: true, emit_null: false)]
    property custom_fields : Hash(String, JSON::Any)?

    abstract class EnumAttributeValidator
      def valid?(value)
        !value || @allowable_values.includes?(value)
      end

      def message
        "invalid value for \"#{@attribute}\", must be one of #{@allowable_values}."
      end

      def to(_type, value)
        case _type
        when Int32
          value.to_i32
        when Int64
          value.to_i64
        when Float32
          value.to_f32
        when Float64
          value.to_f64
        else
          value.to_s
        end
      end
    end

    class EnumAttributeValidatorForType < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "_type"
        @allowable_values = ["de-9", "db-25", "rj-11", "rj-12", "rj-45", "mini-din-8", "usb-a", "usb-b", "usb-c", "usb-mini-a", "usb-mini-b", "usb-micro-a", "usb-micro-b", "usb-micro-ab", "other", ""].map { |value| to(String, value) }
      end
    end

    class EnumAttributeValidatorForSpeed < EnumAttributeValidator
      @attribute : String
      @allowable_values : Array(Int32 | Int64 | Float32 | Float64 | String)

      def initialize
        @attribute = "speed"
        @allowable_values = ["1200", "2400", "4800", "9600", "19200", "38400", "57600", "115200"].map { |value| to(Int32, value) }
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@device : BriefInterfaceRequestDevice, @name : String, @_module : ConsolePortRequestModule? = nil, @label : String? = nil, @_type : String? = nil, @speed : Int32? = nil, @description : String? = nil, @mark_connected : Bool? = nil, @tags : Array(NestedTagRequest)? = nil, @custom_fields : Hash(String, JSON::Any)? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 64
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if @name.try &.to_s.try &.size.try &.< 1
        invalid_properties.push("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      if !@label.nil? && @label.try &.to_s.try &.size.try &.> 64
        invalid_properties.push("invalid value for \"label\", the character length must be smaller than or equal to 64.")
      end

      _type_validator = EnumAttributeValidatorForType.new
      if !_type_validator.valid?(@_type)
        message = _type_validator.message
        invalid_properties.push(message)
      end

      speed_validator = EnumAttributeValidatorForSpeed.new
      if !speed_validator.valid?(@speed)
        message = speed_validator.message
        invalid_properties.push(message)
      end

      if !@speed.nil? && @speed.try &.> 2147483647
        invalid_properties.push("invalid value for \"speed\", must be smaller than or equal to 2147483647.")
      end

      if !@speed.nil? && @speed.try &.< 0
        invalid_properties.push("invalid value for \"speed\", must be greater than or equal to 0.")
      end

      if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
        invalid_properties.push("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 64
      return false if @name.try &.to_s.try &.size.try &.< 1
      return false if !@label.nil? && @label.try &.to_s.try &.size.try &.> 64
      _type_validator = EnumAttributeValidatorForType.new
      return false unless _type_validator.valid?(@_type)
      speed_validator = EnumAttributeValidatorForSpeed.new
      return false unless speed_validator.valid?(@speed)
      return false if !@speed.nil? && @speed.try &.> 2147483647
      return false if !@speed.nil? && @speed.try &.< 0
      return false if !@description.nil? && @description.try &.to_s.try &.size.try &.> 200
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 64
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 64.")
      end

      if name.to_s.size < 1
        raise ArgumentError.new("invalid value for \"name\", the character length must be greater than or equal to 1.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] label Value to be assigned
    def label=(label)
      if !label.nil? && label.to_s.size > 64
        raise ArgumentError.new("invalid value for \"label\", the character length must be smaller than or equal to 64.")
      end

      @label = label
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] _type Object to be assigned
    def _type=(_type)
      validator = EnumAttributeValidatorForType.new
      unless validator.valid?(_type)
        raise ArgumentError.new(validator.message)
      end
      @_type = _type
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] speed Object to be assigned
    def speed=(speed)
      validator = EnumAttributeValidatorForSpeed.new
      unless validator.valid?(speed)
        raise ArgumentError.new(validator.message)
      end
      @speed = speed
    end

    # Custom attribute writer method with validation
    # @param [Object] description Value to be assigned
    def description=(description)
      if !description.nil? && description.to_s.size > 200
        raise ArgumentError.new("invalid value for \"description\", the character length must be smaller than or equal to 200.")
      end

      @description = description
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
        device == other.device &&
        _module == other._module &&
        name == other.name &&
        label == other.label &&
        _type == other._type &&
        speed == other.speed &&
        description == other.description &&
        mark_connected == other.mark_connected &&
        tags == other.tags &&
        custom_fields == other.custom_fields
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [device, _module, name, label, _type, speed, description, mark_connected, tags, custom_fields].hash
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["device"] = _to_h(device)
      hash["module"] = _to_h(_module)
      hash["name"] = _to_h(name)
      hash["label"] = _to_h(label)
      hash["type"] = _to_h(_type)
      hash["speed"] = _to_h(speed)
      hash["description"] = _to_h(description)
      hash["mark_connected"] = _to_h(mark_connected)
      hash["tags"] = _to_h(tags)
      hash["custom_fields"] = _to_h(custom_fields)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      return nil if value.nil?

      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end
  end
end
